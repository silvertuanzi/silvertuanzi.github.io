<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Linux与NTFS的那些事</title>
    <link href="/2026/07/02/linux-ntfs/"/>
    <url>/2026/07/02/linux-ntfs/</url>
    
    <content type="html"><![CDATA[<h1 id="写在前面————关闭Windows快速启动"><a href="#写在前面————关闭Windows快速启动" class="headerlink" title="写在前面————关闭Windows快速启动"></a>写在前面————关闭Windows快速启动</h1><p>Windows快速启动在Windows8引入，且默认开启。如果开启快速启动，则关闭Windows的时候实际上并没有真正关闭，而是进入了休眠状态。<br>众所周知，在双系统的计算机上如果休眠一个系统的时候启动另一个系统，则那些已经被挂载在休眠系统上的硬盘<strong>一定不能</strong>以读&#x2F;写方式被挂载到另一个系统，否则会出现两个系统的文件系统metadata不匹配，文件系统<strong>一定会损坏</strong>。以只读挂载<em>可能</em>是安全的。<br>如果快速启动开启，由于Windows默认挂载所有硬盘，因此意味着几乎永远无法读写Windows的NTFS分区。</p><h1 id="现状记录与考古————前7-0时代"><a href="#现状记录与考古————前7-0时代" class="headerlink" title="现状记录与考古————前7.0时代"></a>现状记录与考古————前7.0时代</h1><h2 id="ntfs-3g————又不是不能用"><a href="#ntfs-3g————又不是不能用" class="headerlink" title="ntfs-3g————又不是不能用"></a>ntfs-3g————又不是不能用</h2><p>稍微翻一下<a href="https://github.com/tuxera/ntfs-3g/wiki/NTFS-3G-Release-History">发布历史</a>就能看出来，ntfs-3g这玩意的历史相当悠久。不过好消息是至今仍在积极维护。<br>在绝大多数的Linux发行版里面，<code>ntfs-3g</code>都在官方软件源提供，且绝大部分开箱即用的发行版安装，都会直接预装<code>ntfs-3g</code>。<br>并且由于<code>ntfs-3g</code>提供了<code>/sbin/mount.ntfs</code>执行文件，对于ntfs分区，直接<code>mount -t ntfs</code>甚至直接不指定类型直接<code>mount</code>走的就是ntfs驱动。<br>缺点嘛，首先它是用户空间的驱动，要走至少一层fuse开销，性能肯定差一些（可以查看<code>mount</code>之后类型显示为<code>fuseblk</code>）；其次驱动的底子毕竟还是比较老旧的实现，功能没那么全，稳定性稍微有点争议，总之不是一个很现代的东西，但是确实是<em>能用就行</em>。</p><h2 id="ntfs3————更好的性能，更大的稳定性争议"><a href="#ntfs3————更好的性能，更大的稳定性争议" class="headerlink" title="ntfs3————更好的性能，更大的稳定性争议"></a>ntfs3————更好的性能，更大的稳定性争议</h2><p><a href="https://docs.kernel.org/filesystems/ntfs3.html">NTFS3驱动</a>在Linux5.15引入，内核态驱动，提供了更强的性能，更完善的功能支持。</p><blockquote><p>Why is NTFS3 called NTFS3 ?<br>NTFS3 is called NTFS3 because it fully implements the latest version 3.1 of NTFS file system specification. NTFS is unlikely to change in future, thus NTFS3 naming reflects that NTFS3 implements the latest revision of NTFS.</p></blockquote><p>NTFS3的主要优势还是在性能上，内核态驱动意味着它不需要走一层fuse转发，因此效率会强得多。功能上实际上ntfs-3g功能也覆盖的相当全，大部分常见的ntfs特性两边都实现了。</p><p>至于二者的取舍，就咱个人的身边统计学来说，似乎很多人都觉得ntfs-3g的实现太老旧，写入不靠谱，建议用ntfs3；但是从发行版社区来看情况似乎不太一样。<br>最值得玩味的就是Debian的态度：<a href="https://bugs.debian.org/cgi-bin/bugreport.cgi?bug=998627">这里</a>可以看到完整的讨论历史，咱大致总结一下要点：</p><ol><li>首先是行为：5.15发布之后debian有不短的时间一直都没有开启ntfs3模块，一直到Debian12发布（使用Linux6.1lts内核），ntfs3是完全不开的：可以直接<code>ls /usr/lib/modules/$(uname -r)/kernel/fs</code>查，然后发现里面根本没有ntfs3内核模块。Debian13 (Linux6.12lts)才开起来。</li><li>看看时间线：Linux5.15发布于2021年11月，Debian12发布于2023年6月，隔了足足一年半的时间Debian社区依然认为”NTFS3 driver is still not in astate making it confident it’s wise to enable it for a stable release. “</li><li>从沟通记录来看，5.15合入ntfs3驱动之后，似乎有很长的一段时间完全没有后续更新，或许这也是Linux社区对它缺乏信任的原因。不过看看现在<a href="https://github.com/torvalds/linux/commits/master/fs/ntfs3">github上记录的日志（github容易看一点）</a>, 看起来至少现在Paragon是有积极维护这个驱动的。</li></ol><p>此外mint社区在mint22.1和LMDE7里面屏蔽了该模块：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ <span class="hljs-built_in">cat</span> /usr/lib/modprobe.d/mint-blacklist-ntfs3.conf <br><span class="hljs-comment"># Blacklist the ntfs3 driver</span><br><span class="hljs-comment"># See https://github.com/orgs/linuxmint/discussions/680</span><br><span class="hljs-comment"># Upstream bug report: https://bugs.launchpad.net/ubuntu/+source/ntfs-3g/+bug/2062972</span><br><br>blacklist ntfs3<br></code></pre></td></tr></table></figure><p>理由写在里面了，大体似乎就是”ntfs3会弄坏一些分区，ntfs-3g至少能用”。不过内核里面模块是在的，解除屏蔽就行。咱喜欢用破坏性最小的方式，在<code>/etc/modprobe.d/</code>目录下创建一个同名的<code>mint-blacklist-ntfs3.conf</code>文件，内容直接留空，按照优先级就能覆盖掉。除此之外的方法就只有删掉文件，没有类似<code>unblacklist</code>这种覆写的语法。</p><div class="note note-secondary">            <p>咱自己的个人经历吧，在用mint21.3（基于Ubuntu22.04, linux6.5hwe内核）的时候，咱那时没有隔离双系统工作区的习惯，直接把Linux的python venv也建在ntfs分区下了，然后平时用的时候没问题，只要删venv，分区几乎必挂，然后重启回Windows需要跑<code>chkdsk</code>修复，且修复之后会有不少文件恢复到<code>found.000</code>类似目录。后来咱自己的判定就是，Linux下尽量避免删除大量零碎文件。<br>后来咱去确认了一下mint21时期的设置，ntfs3驱动没有被屏蔽，测试了一下配置，当时大概率用的就是ntfs3,所以说这或许也是一个可能佐证ntfs3不稳定的实例？</p>           </div><div class="note note-secondary">            <p>Archwiki记录了另一个实例：双系统如果共享同一个游戏文件夹（Windows原生运行+Linux proton），那这个共享文件夹如果位于NTFS分区，Linux侧会经常发生着色器缓存损坏，也很有可能是和ntfs3驱动有关。参见<a href="https://wiki.archlinux.org/title/Steam#NTFS">英文</a>&#x2F;<a href="https://wiki.archlinuxcn.org/wiki/Steam#NTFS">中文</a></p>           </div><div class="note note-info">            <p>ntfs3内核驱动没有提供任何用户空间工具，因此即使主用ntfs3驱动，通常也需要用到ntfs-3g来提供用户空间工具，包括<code>mkfs.ntfs</code> <code>ntfsresize</code> <code>ntfsfix</code>等等。有的发行版就是ntfs-3g一个包，有的发行版则把ntfs-3g的驱动部分和工具部分拆成两个包并共同依赖libntfs-3g共享库包，具体看各自发行版配置。</p>           </div><h2 id="我到底用的是哪个驱动？"><a href="#我到底用的是哪个驱动？" class="headerlink" title="我到底用的是哪个驱动？"></a>我到底用的是哪个驱动？</h2><p>对于手动使用<code>mount</code>命令的其实很简单，显式<code>-t ntfs3</code>就是用的ntfs3驱动，不加<code>-t</code>就会被识别出文件类型是ntfs后，使用默认<code>-t ntfs</code>然后落到ntfs-3g驱动。</p><p>对于大部分桌面环境，大部分GUI文件管理器，用的都是<a href="https://www.freedesktop.org/wiki/Software/udisks/">udisks2</a>，所以配置遵循<code>/etc/udisks2</code>下的配置。<br><code>/etc/udisks2/mount_options.conf</code>存放个性化配置，<code>/etc/udisks2/mount_options.conf.example</code>里面都是被注释掉的配置，但是默认选项基本遵循这个文件。</p><p>可以留意到ntfs部分是这样配置的（多截取一点，保留一个完整的overwrite层次结构）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs conf">### Simple global overrides<br># [defaults]<br># # common options, applied to any filesystem, always merged with specific filesystem type options<br># defaults=ro<br># allow=exec,noexec,nodev,nosuid,atime,noatime,nodiratime,ro,rw,sync,dirsync,noload<br><br>### Specific filesystem type options<br># ntfs_defaults=uid=$UID,gid=$GID,windows_names<br># ntfs_allow=uid=$UID,gid=$GID,umask,dmask,fmask,locale,norecover,ignore_case,windows_names,compression,nocompression,big_writes<br><br>### For the reference, these are the builtin mount options:<br># [defaults]<br># allow=exec,noexec,nodev,nosuid,atime,noatime,nodiratime,relatime,strictatime,lazytime,ro,rw,sync,dirsync,noload,acl,nosymfollow<br><br># # &#x27;ntfs&#x27; signature, definitions for the legacy ntfs kernel driver and the ntfs-3g fuse driver<br># ntfs:ntfs_defaults=uid=$UID,gid=$GID,windows_names<br># ntfs:ntfs_allow=uid=$UID,gid=$GID,umask,dmask,fmask,locale,norecover,ignore_case,windows_names,compression,nocompression,big_writes<br># <br># # &#x27;ntfs&#x27; signature, the new &#x27;ntfs3&#x27; kernel driver<br># ntfs:ntfs3_defaults=uid=$UID,gid=$GID<br># ntfs:ntfs3_allow=uid=$UID,gid=$GID,umask,dmask,fmask,iocharset,discard,nodiscard,sparse,nosparse,hidden,nohidden,sys_immutable,nosys_immutable,showmeta,noshowmeta,prealloc,noprealloc,hide_dot_files,nohide_dot_files,windows_names,nocase,case<br># <br># # define order of filesystem driver priorities for the actual mount call,<br># # required definition for non-matching driver names<br># ntfs_drivers=ntfs3,ntfs<br></code></pre></td></tr></table></figure><div class="note note-info">            <p>咱故意保留了完整的结构，这样才能便于看出来配置生效的层次顺序。</p>           </div><p>其中与驱动选择相关的其实只有最后一行，<code>ntfs_drivers=ntfs3,ntfs</code>优先选择ntfs3驱动挂载，如果ntfs3挂载失败，就改用ntfs-3g，这应该是几乎所有发行版的默认配置。</p><p>咱自己当然也直接遵循了这个默认配置。</p><p>不过最权威的复查方式是在挂载完成之后使用<code>mount</code>命令直接查挂载类型，<code>ntfs3</code>自然不用说，<code>fuseblk</code>就是走的ntfs-3g。<br>看起来这里的字样似乎不太好抓，所以直接读完整列表，反正这种手动挂载的分区通常都会位于最底下。<br>不过通常来说，需要复查的都是通过udisks2挂载的才需要复查，而udisks2会在挂载参数里面留下自己的痕迹，所以用<code>mount | grep udisks2</code>来抓一般是可行的。</p><h2 id="几乎不会触及，但是其实还存在的老内核驱动"><a href="#几乎不会触及，但是其实还存在的老内核驱动" class="headerlink" title="几乎不会触及，但是其实还存在的老内核驱动"></a>几乎不会触及，但是其实还存在的老内核驱动</h2><p>Linux内核中存在一个历史更为久远的ntfs驱动，历史甚至可以追溯到1997年，一直存在到Linux6.9版本被移除。这个驱动的写入支持极其有限，约等于只读驱动。由于ntfs-3g占据了<code>mount.ntfs</code>的位置，因此几乎不可能有机会使用到它，除非使用<code>mount -i -t ntfs</code>方式，禁用mount helper进行挂载，或者卸载掉ntfs-3g，才能见到这个老驱动。一般它以只读驱动的形式存在，<code>mount</code>命令会显示类型为<code>ntfs</code>。在Linux6.9移除这个驱动之后，这个行为被ntfs3接管，ntfs3会在相同的挂载选项下，模拟这个旧的只读驱动的行为，并表现出几乎完全相同的现象（类型<code>ntfs</code>且只读）。</p><hr><p>直到————Linux7.1, 基于这个只读驱动完全重写的ntfs读写驱动，以完全取代ntfs-3g为目标的内核驱动，也是咱这篇文的饺子醋，登场</p><hr><h1 id="新的里程碑…吗？————NTFSPLUS，后改名为NTFS"><a href="#新的里程碑…吗？————NTFSPLUS，后改名为NTFS" class="headerlink" title="新的里程碑…吗？————NTFSPLUS，后改名为NTFS"></a>新的里程碑…吗？————NTFSPLUS，后改名为NTFS</h1><div class="note note-info">            <p>本节涉及到的软件包，安装方式上只有Arch AUR和Debian&#x2F;Ubuntu咱自己利用github action打的包，其它发行版要么手动make要么自己包吧，学一个debian打包已经学到头大了…</p>           </div><p>全新的NTFS驱动在2025年10月以NTFSPLUS的名字官宣，12月改名为NTFS，最后在Linux7.1进入内核主线。CachyOS提前把它移植到了自家的7.0内核上。<br>从<a href="https://lore.kernel.org/lkml/20260213081804.13351-1-linkinjeon@kernel.org/">Linux邮件列表</a>可以看到作者对这个驱动的介绍。</p><p>简而言之，新驱动的设计目标是既要又要————更强的性能，更齐全的功能，以及更高的稳定性。至少目前来看，它<em>似乎</em>已经完成了对NTFS3驱动的覆盖式超越。</p><p>稍微对比一下维护者吧：</p><table><thead><tr><th>NTFS-3G</th><th>NTFS3</th><th>NTFS(PLUS)</th></tr></thead><tbody><tr><td><a href="https://www.tuxera.com/community/open-source-ntfs-3g/">Tuxera</a></td><td><a href="https://www.paragon-software.com/home/ntfs3-driver-faq/">Paragon</a></td><td><a href="https://github.com/namjaejeon">Namjae Jeon</a>&#x2F;<a href="https://github.com/hclee">Hyunchul Lee</a></td></tr></tbody></table><p>前二者在文件系统上经验都相当丰富，都有不少跨系统访问文件系统的工具，例如什么Windows访问APFS&#x2F;HFS+，以及Mac访问NTFS等等。但是NTFS-3G是个基于FUSE的用户态驱动，NTFS3虽然是内核驱动，但是和Paragon的另一商业NTFS驱动完全不同源。<br>而<a href="https://github.com/namjaejeon">Namjae Jeon</a>和<a href="https://github.com/hclee">Hyunchul Lee</a>常驻Linux内核开发，在此NTFS之前，Linux内核的exFAT驱动也是这二位贡献的，从<a href="https://lore.kernel.org/lkml/?q=linkinjeon@kernel.org">Linux内核邮件列表</a>也能看出，这二位既是高技术力又是Linux社区活跃开发者。所以从作者靠谱程度来看，还是比较放心的。</p><p>对于这个新驱动，Namjae Jeon提供了<a href="https://github.com/namjaejeon/linux-ntfs">内核驱动</a>和<a href="https://github.com/ntfsprogs-plus/ntfsprogs-plus">用户空间工具</a>两个仓库。</p><h2 id="ntfsprogs-plus"><a href="#ntfsprogs-plus" class="headerlink" title="ntfsprogs-plus"></a>ntfsprogs-plus</h2><p>用户空间工具可聊的不多，简单介绍一下。<a href="https://github.com/ntfsprogs-plus/ntfsprogs-plus">ntfsprogs-plus</a>定位仍然是补强ntfs-3g提供的用户空间工具，建议配套新ntfs驱动使用，替代ntfs-3g。</p><table><thead><tr><th>工具包</th><th>提供的命令</th></tr></thead><tbody><tr><td>共有</td><td>mkfs.ntfs mkntfs ntfsclone ntfscluster ntfsinfo ntfslabel ntfsresize</td></tr><tr><td>ntfs-3g独有</td><td>ntfscat ntfscmp ntfscp ntfsdecrypt ntfsfix ntfsls ntfsrecover ntfssecaudit ntfstruncate ntfsundelete ntfsusermap ntfswipe</td></tr><tr><td>ntfsprogs-plus独有</td><td>fsck.ntfs ntfsck</td></tr></tbody></table><p>从对比表来看，ntfsprogs-plus精简掉了ntfs-3g时代大量依赖FUSE的用户态修补工具；<br>与此同时新增了fsck.ntfs&#x2F;ntfsck，且这个应该是<a href="https://github.com/ntfsprogs-plus/ntfsprogs-plus">ntfsprogs-plus</a>的重头戏：</p><blockquote><p>ntfsck include basic filesystem checking features, like as below:</p><ul><li>Boot sector check</li><li>Meta system files check</li><li>Inode and LCN Bitmap consistency check</li><li>Inode structure check<ul><li>Essential attribute check</li><li>Runlist check</li><li>Attribute list check</li></ul></li><li>Directory structure check<ul><li>All checks for inode strcuture above</li><li>Directory index check</li><li>Directory index bitmap check</li></ul></li><li>Cluster duplication check : If found cluster duplication. it copied to new clusters.</li></ul></blockquote><p>它能在Linux上修复NTFS文件系统问题，接近在Windows上执行<code>chkdsk</code>的结果，功能远强于ntfs-3g提供的<code>ntfsfix</code>。</p><h3 id="如何安装"><a href="#如何安装" class="headerlink" title="如何安装"></a>如何安装</h3><p>从发布策略来看，仓库使用了<code>ntfs-next</code>分支作开发分支，主分支更新频率很低，因此直接把<code>main</code>分支当成发布分支都行。至于release tag上一个已经是2025年12月了，实在有点太老旧。<br>因此如果自行编译安装，咱觉得直接拉git主线来编译是个相当不错的选择。</p><ol><li>Arch直接安装<a href="https://aur.archlinux.org/packages/ntfsprogs-plus-git">AUR包ntfsprogs-plus-git</a></li><li>Debian&#x2F;Ubuntu用户可以移步咱的<a href="https://github.com/silvertuanzi/ntfsprogs-plus-debpackage">打包repo</a>，为了避免C abi不兼容，能源码安装就源码安装；release里面作为实验性打包，咱打算长期标注为<code>pre-release</code>，且只用Ubuntu22.04作为打包基底系统————<del>由于该项目是纯C的，<code>debhelper</code>自动检测的运行依赖列表里面只有<code>libc6</code>一项，</del>由于glibc有单向向上兼容，因此理论上选择一个具有最旧glibc的系统，在上面编译的包可以适用于未来的所有新系统。目前deb系里面适配新ntfs内核驱动的最旧发行版就是Ubuntu22.04了，因此先用它；Debian12的版本作为Ubuntu22.04停止支持之后的打包实验。值得一提的是，Ubuntu22.04和Debian12输出的均为<code>libc6 (&gt;= 2.34)</code>，而从Ubuntu24.04开始的发行版，输出的均为<code>libc6 (&gt;= 2.38)</code>，这里刚刚好形成一条兼容性分界线了。后续如果有空做兼容性测试且确实新系统上有问题，咱再来研究每个系统分别打包。<br> 更新：由于漏了两个<em>非必需，但是会在configure阶段跳警告</em>的构建依赖，补齐之后Depends列表里面多了几项，但是<em>应该</em>也保持了向上兼容性，所以目前还是只保持Ubuntu2204和Debian12双线打包；二进制包如果有问题咱始终推荐自己源码编译，本来二进制发布包就是实验性质。</li></ol><div class="note note-warning">            <p>由于工具冲突，因此咱只能设定<code>Conflicts: ntfs-3g</code>否则会撞同名文件；<br>与此同时考虑到有一些磁盘管理应用会硬依赖ntfs-3g，因此咱考虑再三之后设定了<code>Provides: ntfs-3g</code>，以免因为使用新工具破坏已有的依赖，甚至导致部分软件被apt拒绝安装。<br>为了保留兼容性，咱补充了链接<code>ntfsfix-&gt;fsck.ntfs</code>，并增设脚本<code>mount.ntfs-3g</code>将<code>mount -t ntfs-3g</code>重定向到<code>mount -t ntfs</code>（这里没有使用ntfs3而是使用ntfs），同时增设<code>mount.lowntfs-3g-&gt;mount.ntfs-3g</code>。</p>           </div><h2 id="ntfs新驱动尝鲜指北"><a href="#ntfs新驱动尝鲜指北" class="headerlink" title="ntfs新驱动尝鲜指北"></a>ntfs新驱动尝鲜指北</h2><p>新的NTFS驱动在<code>linux-cachyos&gt;=7.0</code>，主线内核<code>linux&gt;=7.1</code>上线，但是这个版本存在一个<a href="https://github.com/namjaejeon/linux-ntfs/issues/62">软链接不兼容</a>的bug, 主要影响的是相对链接（绝对链接就算能认，存的路径也不一样，因此也不指望能用）。<br>这个bug的修复要到<code>linux&gt;=7.2</code>的主线才能看到（目前还是rc2），因此对于所有内核<code>&gt;=6.1, &lt;=7.1</code>的发行版，单独编译驱动本身作为外置模块是最优尝鲜方案。</p><p>首先讲一下这个驱动引入的两个新的挂载选项，都是在7.2的补丁才引入的：</p><ol><li><code>symlink=&lt;native|wsl&gt;</code>:<br> <code>wsl(默认)</code>: 在创建符号链接时，这些链接无法被Windows系统或者Linux ntfs3驱动程序读取，只能被WSL读取。由于利用一些Linux特性，创建和解析速度都会快一些。<br> <code>native</code>: 在创建符号链接时，这些链接可以被Windows系统或者Linux ntfs3驱动程序读取。因此，这些符号链接的行为应该与在Windows系统中创建的符号链接完全相同。<br> 无论激活哪个，都可以正常读取Windows原生创建的符号链接。</li><li><code>native_symlink=&lt;raw|rel&gt;</code>:<br> <code>raw(默认)</code>: 记录绝对路径的符号链接，路径直接按原样解析。<br> <code>rel</code>: ntfs_translate_junction()函数会被激活，然后把符号链接记录的绝对路径重写成相对于挂载点的相对符号链接。</li></ol><h3 id="如何安装-1"><a href="#如何安装-1" class="headerlink" title="如何安装"></a>如何安装</h3><p>对于<a href="https://github.com/namjaejeon/linux-ntfs">官方repo</a>介绍的<code>make</code>编译方法，只能为当前正在运行的内核编译，每次内核切换版本，都需要手动编译。<br>因此最方便的使用方法自然是打成dkms包，这样每次安装新内核的时候，dkms就能自动编译；卸载内核的时候模块也能自动一起移除。</p><ol><li>Arch直接安装<a href="https://aur.archlinux.org/packages/ntfsplus-dkms-git">AUR包ntfsplus-dkms-git</a>，附带的<a href="https://aur.archlinux.org/packages/ntfsplus-udev">udev rules包</a>看起来作用不明，可选安装；</li><li>Debian&#x2F;Ubuntu: 适用系统版本：Debian12及以上，Ubuntu22.04及以上；其中Ubuntu22.04需要检查当前运行的内核版本： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">uname</span> -r<br></code></pre></td></tr></table></figure> 如果是出厂的5.15内核则无法兼容，需要装hwe内核，内核版本是6.1以上即可；<br> 从咱的<a href="https://github.com/silvertuanzi/linux-ntfs-dkms-deb/releases">打包repo</a>获取到dkms驱动包安装即可。由于dkms包本质是把源码安装到指定位置，然后在用户的本机上根据用户系统内安装的内核对应编译，因此不分系统兼容性，甚至不分指令集架构，ARM64都能用。<br> 由于打包使用到了一点新版本dh-dkms的特性，因此只能在Debian13&#x2F;Ubuntu26.04上才能完成打包，低一点的系统版本都会导致<code>dkms.conf</code>被打包到错误的位置。包好的就能通用了。</li></ol><h3 id="Udisks2配置"><a href="#Udisks2配置" class="headerlink" title="Udisks2配置"></a>Udisks2配置</h3><p>查看<code>/etc/udisks2/mount_options.conf.example</code>可以看到ntfs区的示例配置：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs conf"># # &#x27;ntfs&#x27; signature, definitions for the legacy ntfs kernel driver and the ntfs-3g fuse driver<br># ntfs:ntfs_defaults=uid=$UID,gid=$GID,windows_names<br># ntfs:ntfs_allow=uid=$UID,gid=$GID,umask,dmask,fmask,locale,norecover,ignore_case,windows_names,compression,nocompression,big_writes<br># <br># # &#x27;ntfs&#x27; signature, the new &#x27;ntfs3&#x27; kernel driver<br># ntfs:ntfs3_defaults=uid=$UID,gid=$GID<br># ntfs:ntfs3_allow=uid=$UID,gid=$GID,umask,dmask,fmask,iocharset,discard,nodiscard,sparse,nosparse,hidden,nohidden,sys_immutable,nosys_immutable,showmeta,noshowmeta,prealloc,noprealloc,hide_dot_files,nohide_dot_files,windows_names,nocase,case<br># <br># # define order of filesystem driver priorities for the actual mount call,<br># # required definition for non-matching driver names<br># ntfs_drivers=ntfs3,ntfs<br></code></pre></td></tr></table></figure><p>所以自定义的<code>/etc/udisks2/mount_options.conf</code>可以作以下调整：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs conf"># 新驱动改为首选驱动<br>ntfs_drivers=ntfs,ntfs3<br><br># 新驱动的挂载选项，主要是添加symlink的配置<br>ntfs:ntfs_defaults=uid=$UID,gid=$GID,windows_names,symlink=native<br>ntfs:ntfs_allow=uid=$UID,gid=$GID,umask,dmask,fmask,locale,norecover,ignore_case,windows_names,compression,nocompression,big_writes,symlink<br></code></pre></td></tr></table></figure><p>如果添加其它defaults选项，确保它同时也在allow选项里面。</p><h3 id="与ntfs-3g共存"><a href="#与ntfs-3g共存" class="headerlink" title="与ntfs-3g共存"></a>与ntfs-3g共存</h3><p>新驱动本身是不与ntfs-3g冲突的，因此如果不使用<a href="https://github.com/ntfsprogs-plus/ntfsprogs-plus">ntfsprogs-plus</a>，那三个驱动共存是没问题的。<br>但是ntfs-3g会注册一个可执行文件<code>/usr/sbin/mount.ntfs</code>，从而<code>mount -t ntfs</code>会被ntfs-3g抢占；使用<code>mount -i -t ntfs</code>就能让<code>mount</code>命令无视<code>/sbin</code>目录下的mount helper，使用新的ntfs驱动挂载。</p><p>但是Udisks2没法加<code>-i</code>参数，因此需要曲线救国，给新ntfs驱动准备一个新的名字，例如<code>ntfsplus</code>；然后在<code>/usr/sbin</code>目录下创建一个<code>mount.ntfsplus</code>文件：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-meta">#!/bin/sh</span><br><span class="hljs-built_in">exec</span> /usr/bin/mount -i -t ntfs <span class="hljs-string">&quot;<span class="hljs-variable">$@</span>&quot;</span><br></code></pre></td></tr></table></figure><p>这样之后<code>mount -t ntfsplus</code>就会被新ntfs驱动接管。接下来配置Udisks2的<code>/etc/udisks2/mount_options.conf</code>驱动顺序就行了：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs conf">ntfs_drivers=ntfsplus,ntfs3,ntfs<br><br>ntfs:ntfsplus_defaults=uid=$UID,gid=$GID,windows_names,symlink=native<br>ntfs:ntfsplus_allow=uid=$UID,gid=$GID,umask,dmask,fmask,locale,norecover,ignore_case,windows_names,compression,nocompression,big_writes,symlink<br></code></pre></td></tr></table></figure><p>实测这样配置的挂载选项可以完全正常生效，Udisks2在这一块留的接口还是挺灵活的。</p><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>配完默认驱动之后就该收工了，之后该怎么用怎么用。没有存在感的驱动才是好驱动，如果驱动有存在感了，那八成是出问题了。<br>好好尝鲜就完事了~</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Rime输入法配置</title>
    <link href="/2025/12/24/rime-input/"/>
    <url>/2025/12/24/rime-input/</url>
    
    <content type="html"><![CDATA[<h1 id="0-为什么用rime？"><a href="#0-为什么用rime？" class="headerlink" title="0.为什么用rime？"></a>0.为什么用rime？</h1><p>开源，完全离线，不上传用户数据；自定义配置词库，并且可以本地记录高频词。<br>又希望输入法聪明又希望保留隐私，rime就是个不错的选择。如果喜欢DIY就更合适了。</p><h1 id="1-词库方案选择"><a href="#1-词库方案选择" class="headerlink" title="1. 词库方案选择"></a>1. 词库方案选择</h1><ol><li><a href="https://github.com/iDvel/rime-ice">rime-ice雾凇拼音</a>: 老牌经典选择，热门稳健</li><li><a href="https://github.com/gaboolic/rime-frost">rime-frost白霜拼音</a>: 号称从雾凇拼音优化而来，先删除大量废词，然后重新分词优化</li><li><a href="https://github.com/amzxyz/rime_wanxiang.git">rime-wanxiang万象拼音</a>: 功能特别特别丰富，对双拼和辅助码做了多种支持方案，与此同时还内嵌了一个小模型用来解决词库盲区</li></ol><p>以上方案无优劣之分，最合适的就是最适合自己的。<br>不要问咱选了什么，咱几台设备每个都用用（笑）。<br>下文部署方案举例不代表推荐，想换成别的方案的自己改链接就行，使用方式完全一样。</p><p>部署方案是完全通用的，唯一需要注意的就是万象拼音的各种方案位于不同的git branch，如果想借助git管理，建议在<code>git clone</code>时开启<code>--single-branch</code>选项，并用<code>-b/--branch</code>选择好辅助码方案。</p><div class="note note-info">            <p>老二次元还可以自己额外添加<a href="https://github.com/outloudvi/mw2fcitx/releases">萌娘百科词库</a>，添加方式后面一并会介绍。</p>           </div><h1 id="2-部署rime并加载词库"><a href="#2-部署rime并加载词库" class="headerlink" title="2. 部署rime并加载词库"></a>2. 部署rime并加载词库</h1><h2 id="2-1-自动化工具"><a href="#2-1-自动化工具" class="headerlink" title="2.1 自动化工具"></a>2.1 自动化工具</h2><h3 id="东风破"><a href="#东风破" class="headerlink" title="东风破"></a>东风破</h3><p><a href="https://github.com/rime/plum">东风破plum</a>是rime推出的自动化管理工具，但是咱懒得多研究一个工具，就选择不用它，而是手动管理。<br>它原生适用于Linux&#x2F;MacOS平台，在Windows下最好借助git bash提供的bash环境使用。<br>上述提到的词库都提供了借助东风破部署的方式，都是一行命令解决，参看上游文档即可。</p><div class="note note-warning">            <p>使用东风破之前还是要先安装好对应平台的rime输入法。</p>           </div><p>rime输入法在<a href="https://rime.im/download/">官网</a>已经提供了各个平台的安装方式，直接选择相应的安装即可，后面会展开讲。</p><div class="note note-info">            <p>除了Arch社区打包特殊之外，大部分Linux使用东风破plum部署词库都比手动方便得多。</p>           </div><h3 id="万象拼音专属"><a href="#万象拼音专属" class="headerlink" title="万象拼音专属"></a>万象拼音专属</h3><p>万象拼音提供了一个专门的<a href="https://github.com/amzxyz/RIME-LMDG/releases/download/tool/wanxiang-tools">gui管理工具</a>，由于万象打包过于多样，使用这个工具似乎比下文的手动方式方便？</p><h2 id="2-2-Windows"><a href="#2-2-Windows" class="headerlink" title="2.2 Windows"></a>2.2 Windows</h2><p>Windows直接下载安装小狼毫<a href="https://github.com/rime/weasel/releases/latest">weasel</a>即可，但是为了方便起见，建议先部署词库，再安装输入法。</p><div class="note note-info">            <p>安装输入法的时候就会立即在<code>%APPDATA\Rime</code>生成一些基本的配置文件，而如果用git部署词库便于后续更新的话，是要直接把词库<code>git clone</code>到<code>Rime</code>文件夹下的，因此意味着如果先安装了输入法，就还需要先把<code>Rime</code>文件夹先移走，部署完词库再移动回来。</p>           </div><ol><li>进入数据目录，一般是<code>C:\Users\&lt;username&gt;\AppData\Roaming</code>，或者最快的进入方式是win+R运行<code>%appdata%</code>。<br> 如果还没有安装小狼毫，那这里应该不存在Rime文件夹；如果已经安装了，就把Rime文件夹重命名备份。</li><li>使用<code>git clone</code>获取输入方案，这里给两个示例，第一个是雾凇拼音，第二个是万象拼音基础全拼方案（万象辅助码全是双拼）： <figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs crmsh">git <span class="hljs-keyword">clone</span> <span class="hljs-title">https</span>://github.com/gaboolic/rime-frost.git Rime --depth <span class="hljs-number">1</span><br>git <span class="hljs-keyword">clone</span> <span class="hljs-title">https</span>://github.com/amzxyz/rime_wanxiang.git Rime --depth <span class="hljs-number">1</span> --single-branch -b wanxiang-base<br></code></pre></td></tr></table></figure> 使用<code>--depth 1</code>很有必要性，词库仓库通常有相当厚重的commit历史，只获取最新版本可以节省大量无用下载量。<br> 如果使用万象拼音，根据<a href="https://github.com/amzxyz/rime_wanxiang/releases">release界面的说明</a>获取模型，同样放置在Rime目录中。</li><li>如果在第一步中重命名备份了Rime文件夹，那就把里面的文件合并到刚刚<code>git clone</code>获取的Rime文件夹中，通常来说不会有同名文件冲突。</li><li>如果没有安装小狼毫，现在安装，安装结束后会自动弹出设置界面；已经安装的，在托盘处右击小狼毫图标，选择输入法设置；<br> 只勾选自己拉取的方案即可，其它输入方案可以完全不选。然后选择一个自己喜欢的配色方案，设置完成。<br> <img src="/img/rime-input/weasel-settings.png"></li><li>如果希望小狼毫作为默认输入法，可以前往Windows的输入法设置，删除微软拼音，再重新添加微软拼音，这样小狼毫的顺位就会在微软拼音前面。</li></ol><h2 id="2-3-Linux"><a href="#2-3-Linux" class="headerlink" title="2.3 Linux"></a>2.3 Linux</h2><h3 id="输入法框架-ibus还是fcitx？"><a href="#输入法框架-ibus还是fcitx？" class="headerlink" title="输入法框架: ibus还是fcitx？"></a>输入法框架: ibus还是fcitx？</h3><ul><li>Gnome: 已经和ibus深度集成，ibus的设置直接嵌入系统内的键盘设置；并且gnome wayland没有适配fcitx5需要的输入法协议，fcitx5如果要在gnome wayland下正确工作，需要依赖<a href="https://github.com/wengxt/gnome-shell-extension-kimpanel">gnome插件</a>；因此gnome桌面强烈建议搭配ibus；</li><li>KDE Plasma: 理论上均可选择，但是KDE官方推荐使用fcitx5；</li><li>Cinnamon: 本来应该位于其它桌面里面，但是Cinnamon6.6更新开始添加ibus集成。<br>  目前cinnamon的ibus集成尚未完善，暂时还是fcitx5更方便。<br>  咱自己目前是cinnamon+fcitx5的方案。<br>  在cinnamon wayland成熟之后，或许应该开始评估迁移到ibus的可能性了；</li><li>其它桌面：均可选择，并且有ibus更适合gtk，fcitx更适合qt的说法；但是从易用性看，或许fcitx5会更好一些。</li></ul><h3 id="安装配置输入法框架"><a href="#安装配置输入法框架" class="headerlink" title="安装配置输入法框架"></a>安装配置输入法框架</h3><p>首先检测是否有预装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">ibus version <span class="hljs-comment">#ibus</span><br>fcitx --version <span class="hljs-comment">#fcitx4,老旧且不支持wayland,不建议使用</span><br>fcitx5 --version <span class="hljs-comment">#fcitx5</span><br></code></pre></td></tr></table></figure><p>虽然ibus和fcitx5并不会冲突（在很多发行版上会选择同时预装这两个），但是正常来说，一个输入法框架一直用就行了，没道理搞两个切来切去的，所以为了避免冲突的隐患，卸掉其中一个。<br>Ubuntu可以试探一下（仅作演示，实际上Ubuntu保留ibus卸载fcitx5应该是更好的）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt autoremove ibus<br></code></pre></td></tr></table></figure><p><img src="/img/rime-input/remove-ibus.png"><br>从输出列表来看，ibus可以安全卸载而不会因此干掉整个gnome，其它发行版就更不需要担心这个问题了。</p><p>接下来，检查保留的输入法框架能否正常开机自启动，如果不能，可以稍微参考ArchWiki的方法配置环境变量。<br><a href="https://wiki.archlinux.org/title/IBus">ibus wiki仅英文，中文wiki长期未更新且质量不佳</a>&#x2F;<a href="https://wiki.archlinux.org/title/Fcitx5">fcitx5英文wiki</a>&#x2F;<a href="https://wiki.archlinuxcn.org/wiki/Fcitx5">fcitx5中文wiki</a><br>fcitx5的中文wiki并非是翻译英文页面，而是另外重写的，所以可以两边结合看。</p><div class="note note-success">            <p>tldr: </p> <ol> <li>Gnome+iBus无需配置；</li> <li>KDE plasma Wayland+fcitx5需要: <ul> <li>前往系统设置 &gt; 输入与输出 &gt; 键盘 &gt; 虚拟键盘，选择 Fcitx 5 Wayland 启动器。</li> <li>对于XWayland应用，为应用单独设置环境变量<code>XMODIFIERS=@im=fcitx</code></li> </ul> </li> <li>其它任意x11桌面+fcitx5组合：<br> 设置全局环境变量，可以填入<code>/etc/environment</code>文件： <figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-attr">GTK_IM_MODULE</span>=fcitx<br><span class="hljs-attr">QT_IM_MODULE</span>=fcitx<br><span class="hljs-attr">XMODIFIERS</span>=@im=fcitx<br><span class="hljs-attr">SDL_IM_MODULE</span>=fcitx<br><span class="hljs-attr">GLFW_IM_MODULE</span>=ibus<br></code></pre></td></tr></table></figure></li> <li>其它桌面和WM用户默认自己有能力折腾。</li> <li>LinuxMint+fcitx5用户在系统设置 &gt; 输入法里面，顶部页面选择输入法框架为fcitx5，选择完成之后直接离开页面，注销或者重启即可。</li> </ol>           </div><h3 id="安装rime"><a href="#安装rime" class="headerlink" title="安装rime"></a>安装rime</h3><p>ibus用户安装<code>ibus-rime</code>，fcitx5用户安装<code>fcitx5-rime</code>，主流发行版均有收录：</p><p>Debian&#x2F;Ubuntu:<code>apt install ibus-rime</code>或者<code>apt install fcitx5-rime</code><br>Fedora:<code>dnf install ibus-rime</code>或者<code>dnf install fcitx5-rime</code><br>Fedora 18&#x2F;19或者CentOS等旧版:<code>yum install ibus-rime</code>或者<code>yum install fcitx5-rime</code><br>OpenSUSE:<code>zypper in rime</code>，然后<code>zypper in ibus-rime</code>或者<code>zypper in fcitx5-rime</code><br>Gentoo:<code>emerge ibus-rime</code>或者<code>emerge fcitx5-rime</code><br>Arch:<code>pacman -S ibus-rime</code>或者<code>pacman -S fcitx5-rime</code></p><p>安装完成之后，可能需要去fcitx5或者ibus的设置面板里面把rime添加进输入法列表。<br>然后可以暂时不急切出rime输入法，配置完词库之后再启用可以节省一次rime部署配置的时间。</p><h3 id="配置词库"><a href="#配置词库" class="headerlink" title="配置词库"></a>配置词库</h3><h4 id="ArchLinux"><a href="#ArchLinux" class="headerlink" title="ArchLinux"></a>ArchLinux</h4><div class="note note-info">            <p>万象系列已经不在AUR中；archlinuxcn的打包有点乱，并且启用似乎不太方便，因此手动部署万象方案用下文<a href="#%E5%85%B6%E5%AE%83%E5%8F%91%E8%A1%8C%E7%89%88">其它发行版</a>的方法应该更好。</p>           </div><p>直接通过AUR或者pacman安装：<br><code>rime-ice-git</code>已经被收录进archlinuxcn仓库，可以直接通过pacman安装；<br><code>rime-frost-git</code>可以通过AUR安装。<br>安装完成之后通过patch的方式启用：<br>创建或者编辑<code>$HOME/.config/ibus/rime/default.custom.yaml</code>或者<code>$HOME/.local/share/fcitx5/rime/default.custom.yaml</code>，填入以下内容：</p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-05eef982" role="button" aria-expanded="false" aria-controls="collapse-05eef982">        <div class="fold-arrow">▶</div>雾凇      </div>      <div class="fold-collapse collapse" id="collapse-05eef982">        <div class="fold-content">          <figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">patch:</span><br>  <span class="hljs-attr">__include:</span> <span class="hljs-string">rime_ice_suggestion:/</span><br></code></pre></td></tr></table></figure>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-90469042" role="button" aria-expanded="false" aria-controls="collapse-90469042">        <div class="fold-arrow">▶</div>白霜      </div>      <div class="fold-collapse collapse" id="collapse-90469042">        <div class="fold-content">          <figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">patch:</span><br>  <span class="hljs-attr">__include:</span> <span class="hljs-string">rime_frost_suggestion:/</span><br></code></pre></td></tr></table></figure>        </div>      </div>    </div><div class="note note-info">            <p>核心是检查<code>/usr/share/rime-data</code>目录下是否存在<code>rime_*_suggestion.yaml</code>文件：<br>如果是，就可以使用这种方法启用，并根据文件名填写<code>__include</code>字段；<br>如果否，就需要考虑其它方法（雾凇和白霜都可以这样启用）</p>           </div><p>如果需要其它配置可以参考<a href="https://github.com/iDvel/rime-ice?tab=readme-ov-file#arch-linux">rime-ice官方文档</a>或者<a href="https://arch.icekylin.online/guide/advanced/optional-cfg-1.html#%E9%9B%BE%E5%87%87%E6%8B%BC%E9%9F%B3-rime-ice-%E8%BE%93%E5%85%A5%E6%B3%95">这里</a></p><h4 id="其它发行版"><a href="#其它发行版" class="headerlink" title="其它发行版"></a>其它发行版</h4><p>前往输入法的用户配置目录：<br>fcitx5前往<code>$HOME/.local/share/fcitx5</code><br>ibus前往<code>~/.config/ibus</code></p><div class="note note-danger">            <p>不要进入rime目录；如果原本存在rime文件夹，则备份里面的所有文件，之后暂时删除或者移走rime文件夹。</p>           </div><p>为了方便更新，直接使用<code>git clone</code>获取完整的词库文件，并用<code>--depth 1</code>减少拉取大小（词库往往都有着极为厚重的commit历史）</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#根据自己的选择更换词库链接</span><br>git <span class="hljs-built_in">clone</span> https://github.com/gaboolic/rime-frost.git rime --depth 1 <span class="hljs-comment">#白霜拼音</span><br>git <span class="hljs-built_in">clone</span> https://github.com/amzxyz/rime_wanxiang.git rime --depth 1 --single-branch -b wanxiang-base <span class="hljs-comment">#万象基础方案</span><br></code></pre></td></tr></table></figure><div class="note note-info">            <p>万象用户还要前往<a href="https://github.com/amzxyz/rime_wanxiang/releases">官方release</a>下载语法模型，放到上述的rime文件夹中。</p>           </div><p>更新的时候直接<code>git pull</code>即可。</p><div class="note note-warning">            <p>如果前面备份了rime默认配置文件，现在可以移回来了。一般情况下，不会有同名文件冲突。</p>           </div><p>然后可以注销重启以完全重新启动输入法引擎，只要输入法切换到中州韵(rime)，就能自动启用安装好的词库。<br>如果没有的话，前往rime菜单点击重新部署，稍等片刻之后就完成了。</p><h2 id="2-4-Android"><a href="#2-4-Android" class="headerlink" title="2.4 Android"></a>2.4 Android</h2><p>Android目前有三种基于Rime的输入法：</p><ol><li><a href="https://github.com/osfans/trime">同文输入法</a>：Rime官网推荐的输入法之一，直接本体就是Rime，安装包大小很小，最简洁，在咱的设备(EMUI11, Android 10)上会不明原因闪退；</li><li><a href="https://github.com/fcitx5-android/fcitx5-android">小企鹅输入法</a>：Rime官网推荐的输入法之二，本体是fcitx的安卓移植，因此还需要另外安装Rime插件(直接在release页面下载即可)；当前咱正在使用的方案；</li><li><a href="https://github.com/gurecn/YuyanIme">语燕输入法</a>（<a href="https://gitee.com/gurecn/YuyanIme">gitee</a>）：非Rime官方支持方案，基于Rime开发，进行了大量开箱即用的配置，例如内置雾凇词库和白霜词库，从readme来看似乎功能最为丰富，但是咱已经配好小企鹅了才发现这个项目，就懒得再下下来体验实用性了。由于词库均为内置，不确定导入万象方案，或者导入自定义词库（例如萌百词库）是否方便。</li></ol><p>选择其中一个下载，然后语燕使用雾凇或者白霜词库的话直接去app设置里面应该就行。<br>同文输入法的数据位于<code>/rime</code>目录下（可能Android11以后的版本此目录不容易出现权限问题？）<br>小企鹅输入法的数据位于<code>/Android/data/org.fcitx.fcitx5.android/files/data/rime</code>目录下（Android11及更新的系统版本，编辑此目录下的文件可能需要一点提权操作）</p>    <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-dc9f9183" role="button" aria-expanded="false" aria-controls="collapse-dc9f9183">        <div class="fold-arrow">▶</div>小企鹅数据提权的思路      </div>      <div class="fold-collapse collapse" id="collapse-dc9f9183">        <div class="fold-content">          <ol> <li>最简单粗暴：MT管理器+shizuku暴力提权就完事了;</li> <li>另一种简单粗暴：USB数据线插电脑操作;</li> <li>无需借助外力的方式：把下载好的词库文件放在公共目录（例如Downloads目录）下，然后打开小企鹅的软件，进入 高级 &gt; 浏览用户数据目录 ，这样打开的文件管理器是安卓内置的底层文件管理器，只拥有对小企鹅输入法自己的访问权限，然后在这里把公共目录的词库文件复制进来即可。</li> </ol> <p>上述三种方法中，按照万象拼音的发布说明，前两种方案可能会遇到权限问题，第三种方案最麻烦但是最稳妥。</p>         </div>      </div>    </div><p>由于手机端难以使用git，雾凇和万象可以通过release下载，白霜建议下载源码zip。或者，如果有termux环境，在termux里面执行<code>git clone</code>也行，但是记得把词库仓库放置在外部软件可访问的公共文件夹。<br>接下来把完整的仓库全部复制到输入法的数据目录，具体的数据目录根据上文。<br>然后启动输入法等待片刻，部署完成。</p><h2 id="2-5-萌娘百科词库"><a href="#2-5-萌娘百科词库" class="headerlink" title="2.5 萌娘百科词库"></a>2.5 萌娘百科词库</h2><p>适用于二次元补充额外词库。也可以参考这种方式自行添加其它词库。</p><h3 id="ArchLinux-1"><a href="#ArchLinux-1" class="headerlink" title="ArchLinux"></a>ArchLinux</h3><p>同样是archlinuxcn仓库有直接收录，可以直接<code>pacman -S rime-pinyin-moegirl</code>安装；</p><h3 id="其它系统"><a href="#其它系统" class="headerlink" title="其它系统"></a>其它系统</h3><p>前往<a href="https://github.com/outloudvi/mw2fcitx/releases">github release</a>，下载<code>moegirl.dict.yaml</code>一个文件即可，放置在词库同级目录。</p><h3 id="共同后续步骤"><a href="#共同后续步骤" class="headerlink" title="共同后续步骤"></a>共同后续步骤</h3><p>接下来有两种应用方式：</p><ol><li>传统改文件（最稳，维护起来稍麻烦但也不难）<br> 修改词库的主配置文件：雾凇是<code>rime_ice.dict.yaml</code>，白霜是<code>rime_frost.dict.yaml</code>，万象基础base版是<code>wanxiang.dict.yaml</code>，带辅助码的分支根据文件名自己看着办。<br> 对于Arch用户如果通过pacman安装，建议先把<code>/usr/share/rime-data/*.dict.yaml</code>文件复制到<code>~/.local/share/fcitx5/rime</code>或者<code>~/.config/ibus/rime</code>目录下，再修改，不要动全局配置。<br> 在<code>import_tables</code>段里面加一行： <figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs diff">import_tables:<br>  - cn_dicts/base #或者其它什么的，总之就是自带的词典<br>  ...<br><span class="hljs-addition">+  - moegirl</span><br><br>...<br></code></pre></td></tr></table></figure> 修改完之后，为了让以后<code>git pull</code>可以正常拉取更新，可以commit这次改动： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git add .<br>git commit <span class="hljs-comment">#弹出编辑器的时候随便写点提交说明，add moegirl就行</span><br></code></pre></td></tr></table></figure> 之后<code>git pull</code>拉取更新的时候，就可以自动合并这次改动（合并过程会创建一个merge commit，因此会弹一次编辑器出来，但是有默认的提交信息，因此直接保存退出即可）</li><li>补丁方式（可能无效）<br> 以雾凇为例，其它输入方案根据主词典文件修改对应文件名：<br> 创建<code>rime_ice.custom.yaml</code>填入： <figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">patch:</span><br>  <span class="hljs-string">translator/packs/+:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">moegirl</span><br></code></pre></td></tr></table></figure> <div class="note note-warning">            <p>yaml格式很严格，这个patch的每级缩进都是两个英文空格，不要用tab</p>           </div> <div class="note note-info">            <p>记录目前通过补丁方式应用的情况（不会只有万象不行吧）：</p> <ol> <li>Windows+小狼毫+万象：失败</li> <li>Android+小企鹅+Rime+雾凇&#x2F;白霜：成功</li> <li>Linux+fcitx5-rime+雾凇：成功</li> </ol>           </div></li></ol><p>完成之后，在rime菜单点击重新部署即可生效。</p><h1 id="杂tips"><a href="#杂tips" class="headerlink" title="杂tips"></a>杂tips</h1><div class="note note-info">            <p>记录一点其它的通用配置。<br>凡是提到数据文件夹，根据自己系统对应：<br>Windows: <code>%appdata%\Rime</code><br>Linux fcitx5-rime: <code>~/.local/share/fcitx5/rime</code><br>Linux ibus-rime: <code>~/.config/ibus/rime</code><br>MacOS鼠须管和安卓根据实际情况判断。</p>           </div><h2 id="修改候选词数目"><a href="#修改候选词数目" class="headerlink" title="修改候选词数目"></a>修改候选词数目</h2><p>在数据文件夹下，如果没有<code>default.custom.yaml</code>就创建，如果有就编辑：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">patch:</span><br>  <span class="hljs-comment"># 以下根据自己所需自行定义</span><br>  <span class="hljs-attr">__patch:</span><br>    <span class="hljs-attr">menu/page_size:</span> <span class="hljs-number">5</span>   <span class="hljs-comment">#候选词个数</span><br></code></pre></td></tr></table></figure><p>保持这个三级结构即可，如果原先有其它文件就自己看着插。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>一些编译cuda的笔记</title>
    <link href="/2025/12/03/nvcc-notes/"/>
    <url>/2025/12/03/nvcc-notes/</url>
    
    <content type="html"><![CDATA[<h1 id="环境配置"><a href="#环境配置" class="headerlink" title="环境配置"></a>环境配置</h1><div class="note note-info">            <p>Linux由于各个发行版包管理不同，且几乎99%的发行版一定会自带<code>gcc</code>，因此参见<a href="https://developer.nvidia.com/cuda-downloads">CUDA</a>和<a href="https://developer.nvidia.com/cudnn-downloads">cuDNN</a>安装即可，以及可能需要参见<a href="https://docs.nvidia.com/cuda/cuda-installation-guide-linux/index.html#mandatory-actions">CUDA文档</a>设置环境变量。<br>本节只介绍Windows.</p>           </div><div class="note note-info">            <p>CUDA支持离卡编译，因此CUDA Toolkit和其它各路编译套件完全可以装在没有N卡的机器上，然后完成所有编译工作。但是编译完的程序当然还是需要在有N卡的机器上才能运行</p>           </div><ol><li>Visual Studio: C&#x2F;C++编译器<br> 如果电脑原先没有安装VS，那只安装VS Build Tools就足够了：从微软<a href="https://visualstudio.microsoft.com/zh-hans/downloads/">官网</a>获取Visual Studio 2026生成工具（页面下拉，在所有下载里面选择“用于Visual Studio的工具”就能找到。或者，如果需要兼容性，可以选择<a href="https://learn.microsoft.com/zh-cn/visualstudio/releases/2022/release-history#release-dates-and-build-numbers">VS2022</a>或者<a href="https://learn.microsoft.com/zh-cn/visualstudio/releases/2019/history#release-dates-and-build-numbers">VS2019</a> <div class="note note-info">            <p>截至当前的cuda版本(13.0)，NVIDIA官方支持2019和2022</p>           </div> 然后在安装的时候，选择“使用C++的桌面开发”。组件可以适当精简，比如<code>MSVC AddressSanitizer</code>和<code>vcpkg包管理器</code>可以不需要。如下图所示：<br> <img src="/img/nvcc-notes/msvc.png"><br> 安装完成后，编译器默认并不会被添加到环境变量，也不建议直接把编译器添加到PATH。<br> 在开始菜单中可以看到专用的工具项，从这里启动就可以进入相应的cmd和powershell：<br> <img src="/img/nvcc-notes/tool-prompt.png"><br> 为了简单快捷，可以自己在喜欢的位置创建一个空文件夹，里面编写一个<code>setcl.bat</code>内容如下（假设VS Build Tools安装到默认路径）： <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cmd">@<span class="hljs-built_in">echo</span> off<br>&quot;C:\Program Files (x86)\Microsoft Visual Studio\<span class="hljs-number">18</span>\BuildTools\VC\Auxiliary\Build\vcvars64.bat&quot;<br></code></pre></td></tr></table></figure> 然后把这个文件夹添加进环境变量<code>PATH</code>，之后只要在任意位置运行<code>setcl</code>命令，就能等效于立即启动<code>x64 Native Tools Command Prompt for VS</code> <div class="note note-info">            <p>一般都是在64位主机上编译目标为64位的程序，所以干脆直接启动这个，免得混淆。<br>如果希望在命令行快捷启动其它的，可以按照<code>开始菜单的启动项右键→打开文件位置→右键快捷方式→属性</code>查看对应的启动项到底是在执行什么指令，然后用类似的方式搓成脚本。</p>           </div> 如果想要测试，可以编译一个示例，看看是否正常工作。示例代码来自<a href="https://learn.microsoft.com/zh-cn/cpp/build/building-on-the-command-line?view=msvc-180">Microsoft官方文档</a>     <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-2bceb5a4" role="button" aria-expanded="false" aria-controls="collapse-2bceb5a4">        <div class="fold-arrow">▶</div>C      </div>      <div class="fold-collapse collapse" id="collapse-2bceb5a4">        <div class="fold-content">          <p>hello.c</p> <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;stdio.h&gt;</span></span><br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;Hello, World! This is a native C program compiled on the command line.\n&quot;</span>);<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure> <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cmd">cl hello.c<br>hello.exe<br></code></pre></td></tr></table></figure>        </div>      </div>    </div>     <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-c8c43135" role="button" aria-expanded="false" aria-controls="collapse-c8c43135">        <div class="fold-arrow">▶</div>C++      </div>      <div class="fold-collapse collapse" id="collapse-c8c43135">        <div class="fold-content">          <p>hello.cpp</p> <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;iostream&gt;</span></span><br><span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    cout &lt;&lt; <span class="hljs-string">&quot;Hello, world, from Microsoft C++!&quot;</span> &lt;&lt; endl;<br>&#125;<br></code></pre></td></tr></table></figure> <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cmd">cl /EHsc hello.cpp<br>hello.exe<br></code></pre></td></tr></table></figure>        </div>      </div>    </div></li><li>Cuda Toolkit: Cuda编译器<br> 前往<a href="https://developer.nvidia.com/cuda-downloads">NVIDIA Cuda Toolkit官网</a>下载，安装的时候自定义组件仅选择<code>Runtime</code>和<code>Development</code>两项即可：<br> <img src="/img/nvcc-notes/cuda.png"> <div class="note note-info">            <p>截至cuda 13.0 update 2版本，nvcc尚未官方支持vs2026。如果使用vs2026，会提示编译器不受支持，需要在编译时添加参数<code>-allow-unsupported-compiler</code>。可以使用环境变量把编译参数固定下来，<code>NVCC_PREPEND_FLAGS</code>将编译参数插入在其它参数的开头，<code>NVCC_APPEND_FLAGS</code>将编译参数追加在其它参数的末尾。</p>           </div> 安装完成后，同样可以编译一个示例测试一下。注意虽然nvcc直接添加进PATH了，但是仍然需要<code>cl.exe</code>提供C&#x2F;C++编译器，所以仍然需要确保VS环境处于激活状态。     <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-a47c0d1c" role="button" aria-expanded="false" aria-controls="collapse-a47c0d1c">        <div class="fold-arrow">▶</div>CUDA      </div>      <div class="fold-collapse collapse" id="collapse-a47c0d1c">        <div class="fold-content">          <p>hello.cu</p> <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;stdio.h&gt;</span></span><br><br><span class="hljs-function">__global__ <span class="hljs-type">void</span> <span class="hljs-title">hello_from_gpu</span> <span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br> <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;Hello from GPU! \n&quot;</span>);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span> <span class="hljs-params">(<span class="hljs-type">void</span>)</span></span><br><span class="hljs-function"></span>&#123;<br> hello_from_gpu&lt;&lt;&lt;<span class="hljs-number">4</span>,<span class="hljs-number">4</span>&gt;&gt;&gt;();<br> <span class="hljs-built_in">cudaDeviceSynchronize</span>();<br><br> <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure> <p>编译后会输出16行”Hello from GPU!”</p> <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cmd">nvcc hello.cu<br>hello.exe<br></code></pre></td></tr></table></figure>        </div>      </div>    </div></li><li>cuDNN库：可能需要<br> 前往<a href="https://developer.nvidia.com/cudnn-downloads">NVIDIA cuDNN官网</a>，下载安装即可，可以在自定义里面选择是需要cuda13的版本还是cuda12的版本。 <div class="note note-info">            <p>虽然写的是12.9，但是对cuda12全兼容，如果cuda用了12.8也可以放心使用。</p>           </div></li><li>其它可选工具<br> 建议全局安装一个cmake和ninja，如果不安装也问题不大，pip里面也有这两个工具。<br> 喜欢scoop的当然可以用scoop来安装。 <figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmake">winget <span class="hljs-keyword">install</span> Kitware.CMake Ninja-build.Ninja<br></code></pre></td></tr></table></figure></li></ol><h1 id="编译记录"><a href="#编译记录" class="headerlink" title="编译记录"></a>编译记录</h1><div class="note note-info">            <p>2026年3月8日：更新自编译推荐度，以满分5星为标准，但是实际上只能评出接近满分和接近0分两种极端……</p>           </div><h2 id="xformers"><a href="#xformers" class="headerlink" title="xformers"></a>xformers</h2><div class="note note-warning">            <p>推荐自编译指数：<del>1.5星</del> 2.5星<br>更新理由：<br>xformers已自刎归天（参见这个<a href="https://github.com/facebookresearch/xformers/commit/ca6d2aa0d43241fc8a8dcd872debc2406160160d">commit</a>）<br>xformers现在强推<a href="https://github.com/meta-pytorch/MSLK">mslk</a>并且删光了<code>xformers/ops/fmha</code>里面的所有东西，全部改成了从<code>mslk</code>包里面引；但是mslk的readme给咱看气笑了：</p> <ul> <li>不支持Windows</li> <li>只支持Ampere以上，问题来了，Ampere以上用flash attn不好还是用sageattention不好去用这个破mslk，50系甚至还有flash attn4吃</li> <li>Blackwell全部用了<code>sm_xxa</code>的声明，并且只写了<code>sm_100a</code>和<code>sm_120a</code>，所以甚至sm_103的尊贵的B300用不了，虽然不影响咱这种穷鬼普通用户，但是这种草台班子就很想笑</li> </ul> <p>总之就是一股巨大的草台班子气息。<br>所以，把这个commit之前的最后一次提交留存下来，并把下面的patch提前打进去，编译一版最终版xformers归档留存看起来挺不错。<br>原理由：</p> <ol> <li>xformers当前版本已经适配了pytorch稳定abi，因此现在xformers不再需要与torch版本一对一绑定，最新的<code>xformers 0.0.35</code>的依赖已经变成了<code>torch&gt;=2.10.0</code>，因此意味着以后torch更新可以直接使用现有xformers，而无需在等待官方适配之前先自己编译一份顶上(直接用torch nightly都大概率没问题)；</li> <li>xformers官方开摆了：fa2和fa3都直接用torch的，xformers现在这个轮子大小剩不了多少了；不过xformers底层的mem eff优化还是更省显存，xformers和sdp现在的关系是，sdp更快但是更费显存，看需求用，但是自编译也没多少模块了；<br> 不过这也意味着，现在编译xformers速度可以相当快；</li> <li>对于新显卡xformers官方更是摆：<a href="https://github.com/facebookresearch/xformers/issues/1374#issuecomment-3853439874">官方不会正式支持Blackwell了</a><br> 所以xformers现在没有正常内置cutlass-Blackwell的bug估计也很难等到修复了；<br> 如果Blackwell GPU使用xformers遇到类似这种报错： <figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">NotImplementedError: <span class="hljs-keyword">No</span> <span class="hljs-keyword">operator</span> <span class="hljs-built_in">found</span> <span class="hljs-keyword">for</span> `memory_efficient_attention_forward` <span class="hljs-keyword">with</span> inputs<br></code></pre></td></tr></table></figure>  有一个临时的修复方案，无需编译。<br> 前往python环境里面的xformers目录，通常可以在环境目录底下的<code>Lib/site-packages</code>里面找到<code>xformers</code>文件夹。修改里面的<code>ops/fmha/cutlass.py</code>，定位到170行左右： <figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs diff">    OPERATOR = (<br>        get_operator(&quot;aten&quot;, &quot;_efficient_attention_forward&quot;)<br>        if is_pt_cutlass_compatible()<br>        else None<br>    )<br><span class="hljs-deletion">-   CUDA_MAXIMUM_COMPUTE_CAPABILITY = (9, 0)</span><br><span class="hljs-addition">+   CUDA_MAXIMUM_COMPUTE_CAPABILITY = (12, 1)</span><br>    SUPPORTED_DEVICES: Set[str] = &#123;&quot;cuda&quot;&#125;<br>    SUPPORTED_DTYPES: Set[torch.dtype] = &#123;torch.float, torch.half, torch.bfloat16&#125;<br></code></pre></td></tr></table></figure>  这样就能正确启用<code>cutlass-pt</code>模块，由于这个cutlass是pytorch提供，因此无需担心对新卡的适配不好。</li> <li>对于未来更新的显卡（Rubin及以后），按照xformers现在这种摆烂方式，有可能直接就不可用了，到那个时候想自己尝试尝试编译也行，但是大概率意义不大。</li> </ol>           </div><div class="note note-danger">            <p>由于flash-attn2目前最后一个能在Windows上编译的版本是2.7.4，从2.8开始截止当前版本2.8.3在Windows上均会编译失败，因此xformers内置的flash-attn2在升级到2.8之后也无法在Windows上通过编译。<br>xformers最后一个绑定flash-attn2.7.4的版本是0.0.30，已知可以稳定配合pytorch2.7，因此如果官方包遇到兼容性问题（尤其是Blackwell GPU），自己编译一版0.0.30去配合torch2.7.1+cu128应该是个不错的选择。<br>从0.0.31开始，Windows上无法编译出含flash-attn2的xformers轮子。不是很建议尝试在旧xformers上强行为新版torch编译，适配大概率会比较差。<br>或者，编译一版不含flash attention的xformers，只使用mem eff优化也存在可行性，并且这样的话，编译所需时间会极大幅度缩短，内存压力也会极大幅度减小。即使没有fa2，单纯的xformers经测试也会比torch sdp更省显存。</p>           </div><ol><li>获取源码：从<a href="https://github.com/facebookresearch/xformers">github</a>获取最新源码，或者从<a href="https://pypi.org/project/xformers">pypi</a>获取指定版本的源码压缩包。 <div class="note note-warning">            <p>从<a href="https://github.com/facebookresearch/xformers/commit/6d6e6f0a07e5771b40b0814adac7f07aae946beb">这个commit</a>开始，xformers主动提前适配了torch2.10nightly版本的API，弃用了对torch2.9版本的支持。<br>因此，如果需要为torch2.9编译，那就不能直接连同submodules一起clone，而需要使用<code>git checkout</code>或者<code>git reset --hard</code>先切回旧版本：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> https://github.com/facebookresearch/xformers.git<br><span class="hljs-built_in">cd</span> xformers<br>git reset --hard 513e7a43aba20b22b6b52a32a5d17896ff0e2bd7<br></code></pre></td></tr></table></figure> <p>然后再执行下文的<code>git submodule update --init --recursive</code></p>           </div> 从github获取的时候要clone submodule: <figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs crmsh">git <span class="hljs-keyword">clone</span> <span class="hljs-title">https</span>://github.com/facebookresearch/xformers.git --recurse-submodules<br></code></pre></td></tr></table></figure> 或者如果<code>clone</code>的时候忘记，那么进入xformers源码根目录下执行： <figure class="highlight brainfuck"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs brainfuck"><span class="hljs-comment">git submodule update</span> <span class="hljs-literal">--</span><span class="hljs-comment">init</span> <span class="hljs-literal">--</span><span class="hljs-comment">recursive</span><br></code></pre></td></tr></table></figure> 对于pypi获取的压缩包，解压并且删除里面的<code>xformers.egg-info</code>文件夹。 <div class="note note-info">            <p>git在默认设置下使用Windows的旧版API，其不支持长路径，需要通过git config开启长路径支持：</p> <figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">git config --global core.longpaths <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure> <p>或者，如果有洁癖不想动全局配置，那就<code>clone</code>的时候不要<code>--recursive-submodules</code>，然后为仓库单独开启长路径之后再执行<code>git submodule update</code>。<br>如果没有开启长路径，然后clone期间出现Filename is too long报错，不要抱有任何侥幸心理，把源码文件夹整个删除，开启长路径支持之后再重新整个拉取，这是确保文件夹里的源码和预期一致的最稳妥的方法。</p>           </div></li><li>准备环境：建议使用venv虚拟环境，然后<code>pip</code>安装计划和xformers搭配的<code>torch</code>以及xformers的依赖<code>numpy</code>。如果前面没有安装全局的ninja，这里就一起安装一个，否则没有并行编译会纯纯龟速。<br> 与此同时，如果有用<code>uv</code>的习惯，那后面启动编译直接用uv就好（也可以<code>pip install uv</code>）；或者用更<em>官方</em>一点的方式，<code>pip install build setuptools wheel</code>。</li><li>微调<code>setup.py</code>(可选)：<ul><li>首先咱不喜欢xformers在版本号里面记录编译日期；用git commit hash就足以指示编译的准确版本。因此删除开头的<code>import datetime</code>；然后定位到<code>get_local_version_suffix()</code>函数，修改：  <figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs diff">     def get_local_version_suffix() -&gt; str:    <br>         if not (Path(__file__).parent / &quot;.git&quot;).is_dir():<br>             # Most likely installing from a source distribution<br>             return &quot;&quot;<br><span class="hljs-deletion">-        date_suffix = datetime.datetime.now().strftime(&quot;%Y%m%d&quot;)</span><br>         git_hash = subprocess.check_output(<br>             [&quot;git&quot;, &quot;rev-parse&quot;, &quot;--short&quot;, &quot;HEAD&quot;], cwd=Path(__file__).parent<br>         ).decode(&quot;ascii&quot;)[:-1]<br><span class="hljs-deletion">-        return f&quot;+&#123;git_hash&#125;.d&#123;date_suffix&#125;&quot;</span><br><span class="hljs-addition">+        return f&quot;+&#123;git_hash&#125;&quot;</span><br></code></pre></td></tr></table></figure></li><li>然后，由于flash attention v3仅支持A100,H100等计算卡，不支持Geforce游戏卡；计算卡要用也是在Linux上用，而Linux上既有满血的torch flash attn2也有xformers官方的满血fa3，因此Linux上自编译xformers的必要性也小些。所以修改源码直接屏蔽fa3编译：  <figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs diff">     def get_flash_attention3_nvcc_archs_flags(cuda_version: int):<br><span class="hljs-addition">+        return []</span><br>         if os.getenv(&quot;XFORMERS_DISABLE_FLASH_ATTN&quot;, &quot;0&quot;) != &quot;0&quot;:<br>             return []<br></code></pre></td></tr></table></figure></li><li>下调nvcc threads:由于nvcc编译flash attn部分极其费内存，把nvcc threads调小降低内存压力:  <figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs diff">         if cuda_version &gt;= 1102:<br>             nvcc_flags += [<br>                 &quot;--threads&quot;,<br><span class="hljs-deletion">-                &quot;4&quot;,</span><br><span class="hljs-addition">+                &quot;1&quot;,</span><br>                 &quot;--ptxas-options=-v&quot;,<br>             ]<br>         if sys.platform == &quot;win32&quot;:<br></code></pre></td></tr></table></figure></li></ul></li><li>设置环境变量：<ul><li><code>XFORMERS_DISABLE_FLASH_ATTN</code>根据编译需求设置：<br>  设为0：开启xformers内置的flash-attn，包括fa2和fa3；此时这个选项仅是开启fa的必要条件，开启后上文屏蔽fa3的设置依旧有效；在高版本下编译fa2极大可能失败。<br>  设为1：直接禁用fa2和fa3，只编译最基础的xformers mem eff优化。<br>  对于旧版本xformers0.0.30，这个环境变量默认会取0；当前版本0.0.33已经改为默认取1</li><li><code>XFORMERS_PT_FLASH_ATTN</code>设置为0<br>  Windows下pytorch根本没有编译flash attn组件，此选项让xformers编译自己打包的的flash attn，如果xformers版本高于0.0.30则不要开启，大概率会导致编译失败。<br>  如果上一个环境变量已经设为1禁用了flash attn，那么这个环境变量会被无视。</li><li><code>TORCH_CUDA_ARCH_LIST</code>按以下规则设置：比较重要<br>  首先根据<a href="https://developer.nvidia.com/cuda-gpus">NVIDIA文档</a>确定GPU与cuda arch版本号的关系。  <div class="note note-info">            <p>不在此表上的GPU已经不在cuda13的支持范围；如果需要在cuda12或者旧版本上为老显卡编译，参看<a href="https://developer.nvidia.com/cuda-legacy-gpus">NVIDIA文档</a></p>           </div>  然后确定编译目标：<ul><li>如果只想自用，那就只填自己的（例如RTX50系填<code>12.0</code>）；</li><li>如果想自己选择几代不同的架构，就用分号分隔（例如如果想同时支持RTX40和50系，填<code>8.9;12.0</code>）；</li><li>如果想搞成类似官方的泛用包，<a href="https://huggingface.co/silvertuanzi/xformers_build_archive/blob/main/environment_variables.txt">这里(在huggingface上)</a>记录了一些xformers官方用过的环境变量可以照抄；</li><li>如果不设置，xformers的默认行为会有所差异：flash attn2模块里面设定了一个默认列表，会根据cuda版本的不同启用不同的默认列表，然后将flash attn2的模块为列表中的所有架构编译；flash attn3模块则是检测本机是否是8.0(A100等)或者9.0(H100等)，如果是，就针对本机架构编译，否就直接不编译。其它模块不确定，但是应该是针对本机架构编译。为了确保精准控制自己的需求，建议设置；</li><li>如果手动设置的列表里面不含8.0和9.0，那么fa3模块会自动不启用，因此上文的手动屏蔽此时不再需要。</li></ul></li><li><code>XFORMERS_PT_CUTLASS_ATTN</code>可选设置，控制是否借用torch的cutlass的开关，设置为0禁用，默认启用。这个影响不大，不设保持默认行为也可，稍微节约一点点编译时间；设为0是模拟旧版本的行为</li><li><code>MAX_JOBS</code>控制ninja的并行数：特别需要注意，因为xformers编译过程中，部分环节特别特别吃内存；而ninja的默认行为是把并行数直接开到CPU线程数+2，对于常规的消费级电脑，如果编译fa2，这个设置几乎一定会爆内存。<br>  作为参考，在fa2能通过编译的时候，咱自己的个人配置<code>MAX_JOBS=2</code>能不爆16GB内存，开到3就会开始触发大量虚拟内存交换，甚至会导致中断退出。根据自己的内存调整即可。咱测试的时候没动过nvcc threads，如果按照上文把nvcc threads降低，那有可能这个数值可以开高点，这样至少纯C++无CUDA的部分可以编译快点。<br>  如果选择不编译flash attention部分，在前面把nvcc threads减到1的前提下16G内存可以直接开8，编译很快就会完成。</li><li><code>NVCC_FLAGS</code>：对于vs2026如果上文没有配置<code>-allow-unsupported-compiler</code>，xformers的编译程序接受这个环境变量作为追加编译参数，设置在这也是可以的。</li></ul></li><li>启动编译： <div class="note note-info">            <p>可以使用<code>chcp 65001</code>把输出切换到UTF-8编码，这样msvc编译器输出的中文不会乱码，可以更清晰地查看日志输出。</p>           </div> 导航到xformers源码的根路径，如果使用uv，就执行 <figure class="highlight n1ql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs n1ql">uv <span class="hljs-keyword">build</span> . --no-<span class="hljs-keyword">build</span>-isolation<br></code></pre></td></tr></table></figure> 如果使用build，在以下两种命令选一种；build的默认日志可能输出没那么多，如果缺少安全感就加个<code>-v</code>多看看日志刷屏 <figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs excel">python -m build . -<span class="hljs-built_in">n</span><br>pyproject-build . -<span class="hljs-built_in">n</span><br></code></pre></td></tr></table></figure> 注意上述命令的默认行为是，先把源码打包成源码包(sdist)，就像pip的源码压缩包那样；然后对源码包执行编译。<br> 如果想直接只编译whl，那就额外添加<code>--wheel</code>参数。<br> 经过漫长的编译之后，产物会出现在<code>dist</code>文件夹，妥善保存。 <div class="note note-info">            <p>   按照python现行规范，直接用<code>python</code>执行<code>setup.py</code>的行为都应当被替代：</p> <table> <thead> <tr> <th>旧命令</th> <th>新命令</th> </tr> </thead> <tbody><tr> <td><code>python setup.py install</code></td> <td><code>pip install .</code></td> </tr> <tr> <td><code>python setup.py develop</code></td> <td><code>pip install -e .</code></td> </tr> <tr> <td><code>python setup.py sdist</code>，<code>python setup.py build</code>以及<code>python setup.py bdist_wheel</code></td> <td><code>python -m build</code></td> </tr> </tbody></table> <p>其中后者会默认在TEMP目录下创建隔离的编译环境，视情况添加类似<code>--no-build-isolation</code>的参数改变这个行为。    </p>           </div></li></ol><h3 id="上游flash-attn可能要注意的"><a href="#上游flash-attn可能要注意的" class="headerlink" title="上游flash-attn可能要注意的"></a>上游flash-attn可能要注意的</h3><div class="note note-warning">            <p>推荐自编译指数：2<br>flash-attn的问题是官方自己没搞二进制轮子；但是各种民间轮子已经满天飞，如果有适配的轮子肯定直接用就好，如果没有再来考虑编译。</p>           </div><p>额外提一下编译<a href="https://pypi.org/project/flash-attn/">flash-attn2</a>可能需要注意的事项，和编译xformers无关，编译xformers不用看本节。</p><p>由于咱的设备不支持flash attention2，环境变量的注意事项完全来自于手撕源码<code>setup.py</code>。</p><ol><li>flash-attn同样支持且建议使用<code>ninja</code>并行编译加速，如果想使用<code>pip install ninja</code>的话注意build isolation问题。</li><li><code>NVCC_THREADS</code><br> <code>nvcc</code>的线程数，默认为4，当编译内存不够时可以先考虑压低这个数值，从而尽量减少对编译纯C&#x2F;C++部分的速度影响（纯C&#x2F;C++编译通常吃不了多少内存）</li><li><code>FLASH_ATTN_CUDA_ARCHS</code><br> flash-attn使用这个环境变量来控制编译的目标GPU架构，默认值是<code>80;90;100;110;120</code>（也就是全编译）。这样非常浪费时间，因此强烈建议设置该环境变量，仅编译自己的GPU架构。另外该值只接受0结尾，因此对于30系(sm86)和40系(sm89)也应该使用80</li><li><code>MAX_JOBS</code>和xformers部分相同。</li></ol><h2 id="llama-cpp-python"><a href="#llama-cpp-python" class="headerlink" title="llama-cpp-python"></a>llama-cpp-python</h2><div class="note note-success">            <p>推荐自编译指数：5<br>llama.cpp是非常推荐自行编译的。llama.cpp的源码里面有大量检测本机硬件针对优化的流程，自编译的轮子的性能会明显强于一些预编译的通用轮子。<br>至少在咱这里，用预编译轮子，在cuda版本确认正确的情况下，GPU根本起不来，速度和CPU完全一样；自编译就能正确调用上。<br>对于AMD的zen4以上用户，llama.cpp会检测CPU支持的指令集，并开启AVX512优化；由于llama.cpp的推理是CPU和GPU都有参与工作，因此开启AVX512也能得到收益。<br>再者，llama-cpp-python并不检测pytorch内置的cuda运行库，因此即使使用预编译包，也需要从cuda toolkit至少安装cuda runtime，那cuda toolkit装都装了不如自己编译一下得了。<br>最后，项目不大，只要正确配置好并行加速，编译时间并不会很长。<br>llama-cpp-python的官方文档也是优先推荐自行编译。</p>           </div><p>llama-cpp-python现在有两个源码来源：一个是最开始的上游<a href="https://github.com/abetlen/llama-cpp-python">abetlen&#x2F;llama-cpp-python</a>，一个是基于它的fork<a href="https://github.com/JamePeng/llama-cpp-python">JamePeng&#x2F;llama-cpp-python</a><br>目前二者均在积极维护，pypi上的<a href="https://pypi.org/project/llama-cpp-python">llama-cpp-python</a>由上游的abetlen维护。<br>fork会出现大概只是因为上游原版从2025年8月停更到2026年3月才复活吧。用哪个应该都差不多，反正核心都是<a href="https://github.com/ggml-org/llama.cpp">llama.cpp</a><br>为了方便后续使用，还是走获取源码+构建轮子的方式，便于把构建的wheel存起来复用。直接<code>pip install</code>装源码包一是可控性太低，二是日志太少，不太喜欢。</p><ol><li><p>准备源码</p> <figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs crmsh">git <span class="hljs-keyword">clone</span> <span class="hljs-title">--recursive</span> [链接二选一]<br></code></pre></td></tr></table></figure><p> 或者可以从<a href="https://pypi.org/project/llama-cpp-python">pypi</a>获取abetlen版本的发布包，也可以通过任意pypi镜像站获取，例如<a href="https://help.mirrors.cernet.edu.cn/pypi/">cernet镜像</a>给出的链接是<code>https://mirrors.cernet.edu.cn/pypi/web/simple</code>，那就访问<a href="https://mirrors.cernet.edu.cn/pypi/web/simple/llama-cpp-python">https://mirrors.cernet.edu.cn/pypi/web/simple/llama-cpp-python</a></p><p> 然后如果选择的是JamePeng版本的分支，可以打开<code>pyproject.toml</code>稍作修改：</p> <figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs diff">  [tool.scikit-build]<br>  wheel.packages = [&quot;llama_cpp&quot;]<br><span class="hljs-addition">+ wheel.py-api = &quot;py3&quot;</span><br>  cmake.verbose = true<br>  cmake.minimum-version = &quot;3.21&quot;<br>  minimum-version = &quot;0.5.1&quot;<br>  sdist.include = [&quot;.git&quot;, &quot;vendor/llama.cpp/*&quot;]<br></code></pre></td></tr></table></figure><p> 由于llama-cpp-python使用的是ctypes，编译一个wheel对py3各版本通用是完全可行的。实际上，<a href="https://github.com/abetlen/llama-cpp-python/pull/2166">上游abetlen版本已经这么做了</a></p></li><li><p>准备一个python虚拟环境，以及构建工具<a href="https://pypi.org/project/build/">build</a>或者<a href="https://pypi.org/project/uv/">uv</a></p></li><li><p>配置环境变量</p><ul><li>开启CUDA  <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmd"><span class="hljs-built_in">set</span> CMAKE_ARGS=-DGGML_CUDA=on<br></code></pre></td></tr></table></figure>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> CMAKE_ARGS=<span class="hljs-string">&#x27;-DGGML_CUDA=on&#x27;</span><br></code></pre></td></tr></table></figure></li><li>如果有<strong>全局可用</strong>的<code>ninja</code>（不能是<code>pip install ninja</code>，编译会新建一个隔离环境吃不到。不过<code>conda install ninja</code>或许可以），设置让cmake使用ninja加速编译：  <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmd"><span class="hljs-built_in">set</span> CMAKE_GENERATOR=Ninja<br></code></pre></td></tr></table></figure>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> CMAKE_GENERATOR=Ninja<br></code></pre></td></tr></table></figure>  <div class="note note-warning">            <p>在Windows MSVC下，如果<code>CMAKE_GENERATOR=Ninja</code>则需要手动提前激活vs编译器环境，ninja只会在PATH里面找可用的编译器<code>cl</code>；而如果不设置，默认的msbuild调度就不需要，但是在咱的场景下msbuild会只调单核，超绝龟速编译。</p>           </div>  <div class="note note-info">            <p>建议使用和ninja类似的方式安装一个类似<strong>全局可用</strong>的<code>ccache</code>，它可以缓存已经编译过的结果，从而加快编译速度。<br>llama.cpp会直接检测有是否可用的ccache，如果有会自动使用，无需配置。<br>在Windows环境下，ccache只有配合ninja的时候才比较可用，因为ninja是直接绕开msbuild直接调<code>ccache cl</code>命令，如果走默认的msbuild路径ccache就很难调出来。</p>           </div> llama-cpp-python内部是完整的llama.cpp，因此接受和上游一样的cmake变量，通过<code>CMAKE_ARGS</code>环境变量传进去，参见<a href="https://github.com/ggml-org/llama.cpp/blob/master/docs/build.md#cuda">llama.cpp文档</a><br> 如果是在cmd，直接传类似<code>set CMAKE_ARGS=-DGGML_CUDA=ON -DCMAKE_CUDA_ARCHITECTURES=&quot;86;89&quot;</code>即可，cmd环境变量以行为单位解析不需要包外层引号；<br> 如果是在bash，外层传单引号内层传双引号以避免引号解析混乱，类似<code>export CMAKE_ARGS=&#39;-DGGML_CUDA=ON -DCMAKE_CUDA_ARCHITECTURES=&quot;86;89&quot;&#39;</code>即可。</li></ul></li><li><p>启动编译: 当前工作目录位于源码根目录</p> <figure class="highlight ada"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ada">uv build . <span class="hljs-comment">--wheel</span><br></code></pre></td></tr></table></figure><p> 或者</p> <figure class="highlight ada"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ada">python -m build . <span class="hljs-comment">--wheel</span><br></code></pre></td></tr></table></figure></li></ol><h3 id="相似项目"><a href="#相似项目" class="headerlink" title="相似项目"></a>相似项目</h3><ol><li><a href="https://github.com/ggml-org/llama.cpp">llama.cpp</a><br> 和python无关了，纯粹的C++项目且用cmake构建，直接参见<a href="https://github.com/ggml-org/llama.cpp/blob/master/docs/build.md#cuda">官方文档</a>使用cmake命令即可，同样可以考虑加入<code>-G Ninja</code></li><li><a href="https://github.com/leejet/stable-diffusion.cpp">stable-diffusion.cpp</a>，Windows下同时强烈建议添加<code>-DCMAKE_CXX_FLAGS=&quot;/EHsc&quot;</code>对齐MSVC标准。<br> 同样是纯粹的C++项目，推理t2i的，直接参见<a href="https://github.com/leejet/stable-diffusion.cpp/blob/master/docs/build.md#build-with-cuda">官方文档</a>，同样可以考虑加入<code>cmake -G Ninja</code><br> 注意在Windows下必须<code>-DCMAKE_CXX_FLAGS=&quot;/bigobj&quot;</code>否则编译报错，Windows下同时强烈建议添加<code>-DCMAKE_CXX_FLAGS=&quot;/EHsc&quot;</code>对齐MSVC标准。<br> 目前sd.cpp在Windows下推理仍有明显的<a href="https://github.com/leejet/stable-diffusion.cpp/issues/1473">性能问题</a>，在Linux下非常正常，可以作为一个后备推理方案（量化，gguf这些功能在一些老卡上还有一点就业空间）</li><li><a href="https://github.com/william-murray1204/stable-diffusion-cpp-python">stable-diffusion-cpp-python</a><br> 上述sd.cpp项目的python封装，同样是使用ctypes，因此和llama-cpp-python流程高度相似，可以使用相同的patch适配py3所有版本，且同样通过环境变量<code>CMAKE_ARGS</code>等传递cmake选项。</li></ol><h2 id="onnxruntime-gpu"><a href="#onnxruntime-gpu" class="headerlink" title="onnxruntime-gpu"></a>onnxruntime-gpu</h2><div class="note note-danger">            <p>推荐自编译指数：0<br>项目过大，编译时间过长，非常非常折磨。<br>咱会想到自编译，纯粹是因为torch的cu130到来之后，咱想冲版本，但是onnxruntime-gpu的cu13适配来的实在太慢，所以没忍住自己搞了一版；<br>在cu14到来之前，没有任何理由再开一次编译，这里仅做一下记录。<br>现在pypi提供的onnxruntime-gpu仍为cu12版, 如需cu13，安装方式：</p> <figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">pip install onnxruntime-gpu -i https:<span class="hljs-regexp">//</span>aiinfra.pkgs.visualstudio.com<span class="hljs-regexp">/PublicPackages/</span>_packaging<span class="hljs-regexp">/onnxruntime-cuda-13/</span>pypi/simple<br></code></pre></td></tr></table></figure>          </div><ol><li>获取源码：</li></ol><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs crmsh">git <span class="hljs-keyword">clone</span> <span class="hljs-title">https</span>://github.com/microsoft/onnxruntime.git --depth <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><ol start="2"><li>根据目标python版本准备好一个对应python版本的环境（虚拟环境venv或者conda什么的皆可），确保这个环境处于激活状态。也就是说，执行<code>python</code>命令可以正确启用想要版本的python。</li><li>确保已经安装好cuda和cudnn；其中Windows下的cudnn不能使用安装版，因为安装版的目录结构会是这样：     <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-d5c89cf3" role="button" aria-expanded="false" aria-controls="collapse-d5c89cf3">        <div class="fold-arrow">▶</div>tree      </div>      <div class="fold-collapse collapse" id="collapse-d5c89cf3">        <div class="fold-content">          <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><code class="hljs tree">NVIDIA<br>└── CUDNN<br>└── v9.20<br>├── bin<br>│   └── 13.2<br>│       └── x64<br>│           ├── cudnn64_9.dll<br>│           ├── cudnn_adv64_9.dll<br>│           ├── cudnn_cnn64_9.dll<br>│           ├── cudnn_engines_precompiled64_9.dll<br>│           ├── cudnn_engines_runtime_compiled64_9.dll<br>│           ├── cudnn_graph64_9.dll<br>│           ├── cudnn_heuristic64_9.dll<br>│           └── cudnn_ops64_9.dll<br>├── include<br>│   └── 13.2<br>│       ├── cudnn.h<br>│       ├── cudnn_adv.h<br>│       ├── cudnn_backend.h<br>│       ├── cudnn_cnn.h<br>│       ├── cudnn_graph.h<br>│       ├── cudnn_ops.h<br>│       └── cudnn_version.h<br>├── lib<br>│   └── 13.2<br>│       └── x64<br>│           ├── cudnn.lib<br>│           ├── cudnn64_9.lib<br>│           ├── cudnn_adv.lib<br>│           ├── cudnn_adv64_9.lib<br>│           ├── cudnn_cnn.lib<br>│           ├── cudnn_cnn64_9.lib<br>│           ├── cudnn_engines_precompiled.lib<br>│           ├── cudnn_engines_precompiled64_9.lib<br>│           ├── cudnn_engines_runtime_compiled.lib<br>│           ├── cudnn_engines_runtime_compiled64_9.lib<br>│           ├── cudnn_graph.lib<br>│           ├── cudnn_graph64_9.lib<br>│           ├── cudnn_heuristic.lib<br>│           ├── cudnn_heuristic64_9.lib<br>│           ├── cudnn_ops.lib<br>│           └── cudnn_ops64_9.lib<br>└── LICENSE<br></code></pre></td></tr></table></figure>        </div>      </div>    </div> 安装版的在<code>bin</code>,<code>lib</code>等目录下都至少套了一层CUDA版本的路径，有的在底下还套了一层<code>x64</code>，onnxruntime的编译不能识别这样的路径。<br> 因此需要用软链接的方式，或者用cudnn的tarball包，组织成这样的结构： <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs tree">cudnn-windows-x86_64-9.20.0.48_cuda13-archive<br>├── bin<br>│   └── x64<br>│       └── ...<br>├── include<br>│   └── ...<br>├── lib<br>│   └── x64<br>│       └── ...<br>└── LICENSE<br></code></pre></td></tr></table></figure> 同样的，对于.deb系的Linux，由于会分布在<code>/usr/lib/x86_64-linux-gnu</code>和<code>/usr/include/x86_64-linux-gnu</code>也会变成非标路径，因此如果遇到识别问题，也应该考虑改用tarball包重新组织路径结构。</li><li>根据平台，Windows使用<code>build.bat</code>启动，Linux使用<code>build.sh</code>启动，且需要的所有前置环境和编译参数如下：<ul><li><code>cmake</code>: 核心，项目是cmake项目当然必须需要cmake启动</li><li><code>ninja</code>: 强烈建议使用，对于这种重型项目，ninja的调度优势无论是对比msbuild还是对比GNU make都强了不知道多少；且windows下用ninja调度cl能更好的配合ccache加速，msbuild测试几乎无法命中ccache;<br>  另外强调一下，如果是默认的msbuild，那么msbuild自己就能找到编译器；但是Ninja并不能自己找到<code>cl.exe</code>，因此ninja需要在vs command prompt下使用。为了保险，建议直接使用x64 native command prompt</li><li><code>ccache</code>: 同样强烈建议使用。onnxruntime项目本身是大型的C++&#x2F;CUDA混合项目，ccache对两种的编译都能起到缓存作用；而且必须提及的一点是，CUDA部分会有一些模块的编译相当吃内存，但是为了这个模块直接限制总并行数也很亏；所以高效的选择肯定是，先火力全开，到了吃内存的部分，<code>Ctrl+C</code>干掉，然后调整并行数继续开。编译进度万一在重开的时候丢失了（Windows上概率很大）就是ccache缓存垫回来的时候了。</li><li>启用以下编译参数，由于项目过于重型，因此编译的目标是：只编译最基础的包含CUDA&#x2F;cudnn的模块，不编译TensorRT和NCCL（反正一般用不上），不编译其它语言，并且跳过所有测试，关闭PGO和LTO（太耗时）：<ul><li><code>--config Release</code></li><li><code>--compile_no_warning_as_error</code></li><li><code>--use_cache</code> 本项启用ccache</li><li><code>--cmake_generator Ninja</code></li><li><code>--skip_tests --skip_onnx_tests --skip_winml_tests --skip_nodejs_tests</code> 保险起见，全加</li><li><code>--build_wheel</code> 核心，编译python wheel</li><li><code>--parallel 0</code> 控制并行编译数，0的时候会取CPU最大线程数，nvcc爆内存了需要适当调一个比较小的正数</li><li><code>--nvcc_threads 1</code> nvcc的线程数，开1就好，这个体感收益似乎并不高？而且nvcc阶段的模块都是内存杀手，压制一下挺好</li><li><code>--use_cuda</code> 启用cuda编译<ul><li><code>--cuda_version CUDA_VERSION --cuda_home CUDA_HOME</code>这两个可以不用指定，cuda版本会自动检测，然后只要<code>nvcc</code>在PATH里面，就不需要手动指定路径；</li><li><code>--cudnn_home CUDNN_HOME</code> 指定cudnn路径，如前文所言，需要确保路径如下：  <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs tree">CUDNN_HOME<br>├── bin<br>│   └── ...<br>├── include<br>│   └── ...<br>└── lib<br>└── ...<br></code></pre></td></tr></table></figure></li></ul></li><li><code>--cmake_extra_defines</code>杂项，用来额外指定附加给cmake的选项，包含：<br>  <code>--cmake_extra_defines CMAKE_CUDA_ARCHITECTURES=75;86;89;120</code> 指定编译的目标CUDA架构，这个列表包含了20~50四代GeForce游戏卡，可以自己根据需求调整，节省时间只给自己的GPU编译也是可以的<br>  <code>--cmake_extra_defines onnxruntime_BUILD_UNIT_TESTS=OFF --cmake_extra_defines onnxruntime_BUILD_CSHARP_TESTS=OFF --cmake_extra_defines onnxruntime_BUILD_JAVA_TESTS=OFF</code> 把无关的测试禁用的更彻底一点，求稳妥，反正不会负负得正</li></ul></li></ul></li><li>对于Linux桌面系统，通常来说系统都会存在一个系统级别的<code>protobuf</code>，并且99%的概率下与onnxruntime需要的protobuf不同。编译脚本会自己拉取合适的protobuf版本，因此只需要避免全局的protobuf干扰。咱选择了最简单粗暴的方式：直接破坏依赖来卸载掉全局的protobuf，编译完成再装回来。或者，用喜欢的方式起一个干净的编译环境也行，可以是docker，甚至可以是一个全新的wsl环境。下面列举一下破坏依赖强制卸载的方法： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#apt/dpkg</span><br>dpkg --force depends -r libprotobuf32t64 libprotobuf-lite32t64 libprotobuf-dev protobuf-compiler <span class="hljs-comment">#break</span><br>apt install -f <span class="hljs-comment">#restore</span><br><br><span class="hljs-comment">#pacman</span><br>pacman -Rdd protobuf <span class="hljs-comment">#break</span><br>pacman -S --asdeps protobuf <span class="hljs-comment">#restore</span><br></code></pre></td></tr></table></figure> <div class="note note-info">            <ol> <li>apt系包拆的太散，卸载前先<code>apt search protobuf</code>看一眼到底有哪些包，哪些是已安装的；</li> <li>dnf&#x2F;rpm系可以用<code>rpm -e --nodeps</code>卸载，但是不方便直接恢复，至少咱不知道怎样让包恢复成“作为依赖安装”这个标记。或许有一个思路是，找到依赖protobuf的随便一个包，把它卸掉再重装，这样间接把依赖引回来。</li> </ol>           </div></li></ol><p>完成之后，保存.whl文件，上传，然后这辈子不会再想编译第二次。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>搞python环境杂七杂八那些事</title>
    <link href="/2025/10/01/python-envs/"/>
    <url>/2025/10/01/python-envs/</url>
    
    <content type="html"><![CDATA[<h1 id="多版本python环境管理"><a href="#多版本python环境管理" class="headerlink" title="多版本python环境管理"></a>多版本python环境管理</h1><div class="note note-secondary">            <p>可能是本篇唯一有点用的一章</p>           </div><h2 id="纯官方方法"><a href="#纯官方方法" class="headerlink" title="纯官方方法"></a>纯官方方法</h2><div class="note note-info">            <p>无法做到python版本号第三位(micro version)的不同版本共存（例如3.10.6和3.10.11共存）。不过这似乎也没有实用价值，一般前两位符合需求就没问题。</p>           </div><h3 id="Windows-py-launcher-py-exe"><a href="#Windows-py-launcher-py-exe" class="headerlink" title="Windows: py launcher(py.exe)"></a>Windows: py launcher(py.exe)</h3><p>Windows下，如果同时安装多个版本的python，则添加进PATH的只能有<strong>最多一个</strong>，添加多个的话，<code>python</code>命令只会调用PATH中位置靠前的那个。<br>为了便于在命令行下管理多版本，python官方默认安装了py launcher，并且用管理员权限安装到全局目录；也可以在自定义安装选项中选择：</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/python-envs/py-default1.png"></div><div class="group-image-wrap"><img src="/img/python-envs/py-options1.png"></div></div></div><p>已经安装过py launcher之后，再安装第二版本的python选项就不可用了：</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/python-envs/py-default2.png"></div><div class="group-image-wrap"><img src="/img/python-envs/py-options2.png"></div></div></div><p>使用例：<br><img src="/img/python-envs/py-launcher.png"><br><code>py -X.Y</code>等价于直接使用相应的<code>python.exe</code>，所以诸如<code>py -X.Y -m pip install foo</code>或者<code>py -X.Y -m venv venv</code>这样的命令都是合法的操作。</p><h3 id="Linux-直接共存"><a href="#Linux-直接共存" class="headerlink" title="Linux: 直接共存"></a>Linux: 直接共存</h3><p>Linux下是可以非常方便地直接同时安装多版本的python的。</p><ol><li><p>ubuntu<br> 直接添加ppa源即可：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> add-apt-repository ppa:deadsnakes/ppa<br><span class="hljs-built_in">sudo</span> apt update<br></code></pre></td></tr></table></figure><p> 需要注意的是这个ppa源按照debian系拆包的习惯，把每个版本的python都拆分成了多个小包，类似于：</p><blockquote><p><strong>Built packages</strong><br> <strong>idle-python3.11</strong> IDE for Python (v3.11) using Tkinter<br> <strong>libpython3.11</strong> Shared Python runtime library (version 3.11)<br> <strong>libpython3.11-dbg</strong> Debug Build of the Python Interpreter (version 3.11)<br> <strong>libpython3.11-dev</strong> Header files and a static library for Python (v3.11)<br> <strong>libpython3.11-minimal</strong> Minimal subset of the Python language (version 3.11)<br> <strong>libpython3.11-stdlib</strong> Interactive high-level object-oriented language (standard library, version 3.11)<br> <strong>libpython3.11-testsuite</strong> Testsuite for the Python standard library (v3.11)<br> <strong>python3.11</strong> Interactive high-level object-oriented language (version 3.11)<br> <strong>python3.11-dbg</strong> Debug Build of the Python Interpreter (version 3.11)<br> <strong>python3.11-dev</strong> Header files and a static library for Python (v3.11)<br> <strong>python3.11-distutils</strong> distutils package for Python (version 3.11)<br> <strong>python3.11-examples</strong> Examples for the Python language (v3.11)<br> <strong>python3.11-full</strong> Python Interpreter with complete class library (version 3.11)<br> <strong>python3.11-gdbm</strong> GNU dbm database support for Python (version 3.11)<br> <strong>python3.11-gdbm-dbg</strong> GNU dbm database support for Python (version 3.11 debug extension)<br> <strong>python3.11-lib2to3</strong> lib2to3 package for Python (version 3.11)<br> <strong>python3.11-minimal</strong> Minimal subset of the Python language (version 3.11)<br> <strong>python3.11-tk</strong> Tkinter - Writing Tk applications with Python (version 3.11)<br> <strong>python3.11-tk-dbg</strong> Tkinter - Writing Tk applications with Python (version 3.11 debug extension)<br> <strong>python3.11-venv</strong> Interactive high-level object-oriented language (pyvenv binary, version 3.11)</p></blockquote><p> 按需取用，如果图省事就直接安装<code>python3.*-full</code></p> <div class="note note-secondary">            <p>Debian不支持ppa，不过把ppa源的deb包倒腾过来用也不是不行…吧？</p>           </div></li><li><p>Arch系<br> AUR都有，用喜欢的AUR助手直接装就行；不过AUR的本质也是拉取python源码然后按照预设选项全自动编译，感觉上还没有手动编译好…</p></li><li><p>Linux通用法<br> 根据<a href="https://github.com/python/cpython">官方github</a>的方法直接编译就行，调整下<code>--prefix</code>的位置以便管理，然后最后使用<code>make altinstall</code>安装即可。<br> 后面会有大量篇幅碎碎念编译<del>（或者说这篇就是为折腾编译cpython这盘醋包的饺子）</del></p></li></ol><p>共存安装后，直接使用对应的可执行文件调用即可。<br>例如<code>python3</code>调用系统自带python，<code>python3.11</code>调用安装的3.11，<code>pip3.12</code>调用安装的3.12版本的<code>pip</code></p><h2 id="第三方工具uv"><a href="#第三方工具uv" class="headerlink" title="第三方工具uv"></a>第三方工具uv</h2><div class="note note-info">            <p>不喜欢conda系(anaconda&#x2F;miniconda&#x2F;micromamba)</p> <ol> <li>虽然conda系支持安装非python软件包（比如可以通过<code>conda install</code>安装<code>cmake</code>甚至是<code>nvcc</code>）但是咱不需要；这些工具用官方方案感觉更稳。</li> <li>虽然conda系支持细分到小版本的python版本管理（比如可以同时管理<code>3.10.11</code>和<code>3.10.6</code>），但是同个<code>3.X</code>内的包是兼容的，对于咱普通使用者来说不需要这么细。</li> <li>conda环境管理，如果使用<code>conda create -n &lt;name&gt;</code>就只能强制塞在c盘，如果用<code>-p</code>指定路径，则<code>conda activate</code>之后，终端开头会把完整的绝对路径前缀上去，又臭又长观感极差。</li> <li>conda不认自行安装的python，常用的大部分工具都来自conda-forge的重打包，心理上用起来不如第一方包舒适。</li> <li>conda环境只能通过conda命令激活，与很多开源项目启动脚本默认的<code>venv</code>方法不兼容。</li> <li>conda环境需要专门通过conda prompt终端进入，麻烦。</li> </ol>           </div><p><code>uv</code>并不能算是conda的替代品，只是它也提供了管理多版本python环境的功能。<br>uv的全部功能参见<a href="https://docs.astral.sh/uv/getting-started/features/">uv文档</a>，官方支持的所有安装方式参见<a href="https://docs.astral.sh/uv/getting-started/installation/">文档</a><br>uv的文档另有<a href="https://uv.doczh.com/">中译</a>，但任何时候不建议过多依赖非官方中译文档。<br>简单列举下咱常用的功能：</p><h3 id="安装和更新"><a href="#安装和更新" class="headerlink" title="安装和更新"></a>安装和更新</h3><p>Windows推荐使用<code>winget</code>，在cmd和powershell下均可用：</p><figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmd">winget install astral-sh.uv<br></code></pre></td></tr></table></figure><p>更新：</p><figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmd">winget upgrade astral-sh.uv<br></code></pre></td></tr></table></figure><p>Arch Linux直接从官方软件源装还能吃到镜像加速就是爽：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S uv<br></code></pre></td></tr></table></figure><div class="note note-info">            <p>另有额外的<code>python-uv</code>包，安装后可以在python脚本中<code>import uv</code>使用，效果等同于在python环境中<code>pip install uv</code></p>           </div><p>其它Linux如果软件源中没有（例如ubuntu），则建议使用官方安装脚本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">curl -LsSf https://astral.sh/uv/install.sh | sh<br></code></pre></td></tr></table></figure><p>更新：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">uv self upgrade<br></code></pre></td></tr></table></figure><p>有时会遇到github api限制，要求提供token的问题。建议配合<a href="https://cli.github.com/">github-cli</a>，安装并登录后使用<code>gh auth token</code>获取token。</p><h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><ol><li>管理python环境 <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">uv python list<br></code></pre></td></tr></table></figure> 输出所有可用的python版本（本地+远程），其中本地python只在PATH中搜索，Windows下如果PATH中搜索到py launcher(py.exe)，则还检测py launcher能检测到的python <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">uv python install X.Y<br></code></pre></td></tr></table></figure> 从远程源拉取最简python环境。安装的远程源来自<a href="https://github.com/astral-sh/python-build-standalone">github</a>。<br> 选择的文件是<code>cpython-*-install_only_stripped.tar.gz</code> <div class="note note-info">            <p>似乎不区分过于具体的指令集。在Linux上只使用x86_64版本，即使该平台支持x86_64_v2(SSE4)&#x2F;v3(AVX2)&#x2F;v4(AVX512)</p>           </div> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">uv venv &lt;name&gt; -p X.Y<br></code></pre></td></tr></table></figure> 使用指定的X.Y版本python创建虚拟环境。如果省略name，则目录名为<code>.venv</code> <div class="note note-info">            <p><code>uv</code>创建的虚拟环境里面不包含任何初始包(即使是<code>pip</code>,<code>setuptools</code>和<code>wheel</code>)。<br>使用<code>UV_VENV_SEED=true</code>环境变量可以覆盖这个行为（在3.12以上的版本中只会包含<code>pip</code>）。等价于使用<code>uv venv &lt;name&gt; --seed &lt;other options&gt;</code>。</p>           </div></li><li>管理pip包 <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">uv pip install foo<br></code></pre></td></tr></table></figure> 用<code>uv</code>安装pip包<code>foo</code>。接受<code>-i/--index-url</code>（uv推荐使用<code>--default-index</code>代替）和<code>--extra-index-url</code>（uv推荐使用<code>--index</code>代替），但是对<code>-f/--find-links</code>的兼容<em>似乎</em>不好。<br> uv支持设置环境变量<code>UV_DEFAULT_INDEX</code>,<code>UV_INDEX</code>和<code>UV_FIND_LINKS</code> <div class="note note-info">            <p>uv默认状态下，会使用硬链接(hardlink)管理缓存，即：<br>uv会把whl包的内容直接在缓存目录里面展开，然后如果目标环境和uv cache目录位于同一硬盘分区，则会直接把包内容hardlink到环境里面。如果不在同一分区则会回退到copy模式。<br>这意味着在多个不同虚拟环境下安装同一个whl包的话，实际上每个环境里的包都和缓存包互为hardlink，只占一份空间。<br>如果想要使用更激进的省空间方案，可以使用symlink模式，该模式下，所有包本体会被置于缓存目录，venv中的包均为指向缓存的symlink。<br>uv不推荐此模式，因为<code>uv cache clean</code>会破坏所有安装。<br>管理方式为设置<code>UV_LINK_MODE</code>环境变量，或者在<code>pip install</code>时使用<code>--link-mode</code>参数。<br>完整的原文在下面附上。<br>碎碎念：Linux上使用具有copy-on-write功能的文件系统感觉也可以试试clone模式？（btrfs,xfs等等）</p>           </div>     <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-8e159bd8" role="button" aria-expanded="false" aria-controls="collapse-8e159bd8">        <div class="fold-arrow">▶</div>uv_link_mode      </div>      <div class="fold-collapse collapse" id="collapse-8e159bd8">        <div class="fold-content">          <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs help">--link-mode &lt;LINK_MODE&gt;<br>The method to use when installing packages from the global cache.<br><br>Defaults to `clone` (also known as Copy-on-Write) on macOS, and `hardlink` on<br>Linux and Windows.<br><br>WARNING: The use of symlink link mode is discouraged, as they create tight<br>coupling between the cache and the target environment. For example, clearing the<br>cache (`uv cache clean`) will break all installed packages by way of removing the<br>underlying source files. Use symlinks with caution.<br><br>Possible values:<br>- clone:    Clone (i.e., copy-on-write) packages from the wheel into the<br>  `site-packages` directory<br>- copy:     Copy packages from the wheel into the `site-packages` directory<br>- hardlink: Hard link packages from the wheel into the `site-packages` directory<br>- symlink:  Symbolically link packages from the wheel into the `site-packages`<br>  directory<br><br>[env: UV_LINK_MODE=]<br><br></code></pre></td></tr></table></figure>        </div>      </div>    </div> <div class="note note-warning">            <p>uv默认状态下会拒绝在系统全局环境下装包。如果venv未激活，uv会试图在当前目录查找名为<code>.venv</code>的虚拟环境，如果未找到则会返回报错。<br>使用<code>--system</code>参数或者<code>UV_SYSTEM_PYTHON</code>环境变量覆盖这一行为。</p>           </div> <div class="note note-info">            <p>使用uv安装pytorch的时候，可以使用<code>--torch-backend</code>或者<code>UV_TORCH_BACKEND=</code>指定安装的torch后端（例如，<code>cpu/cu128/rocm6.3/xpu</code>，也可以设定为<code>auto</code>让uv根据驱动自主选择）。<br>指定后uv将完全忽略所有设定的index，而是使用<a href="https://download.pytorch.org/whl">torch源</a>，因此如果想使用torch镜像（目前仍在同步的有<a href="https://mirrors.aliyun.com/pytorch-wheels/">阿里源(findlinks)</a>&#x2F;<a href="https://mirrors.nju.edu.cn/pytorch/whl/">南京大学源(index)</a>）的话就不能使用这个参数。</p>           </div></li></ol><p>理论而言，使用uv管理环境可以覆盖几乎全部的需求，但是还有两个小缺憾：<br>首先是使用<code>uv python install</code>安装的python不方便往全局环境塞包，有时想使用<code>--system-site-packages</code>节约空间的方法就不可行了；<br>在这个前提下，python官方又不为安全更新阶段之后的版本发安装包，想追个小版本最新都追不了。<br>再加上看这种怪一点的版本号咱有种奇妙的舒适感：<br><img src="/img/python-envs/pyplus.png"><br>于是，折腾python源码编译就提上了日程…</p><h1 id="编译cpython源码的那些事"><a href="#编译cpython源码的那些事" class="headerlink" title="编译cpython源码的那些事"></a>编译cpython源码的那些事</h1><h2 id="获取源码"><a href="#获取源码" class="headerlink" title="获取源码"></a>获取源码</h2><h3 id="发布包"><a href="#发布包" class="headerlink" title="发布包"></a>发布包</h3><p><a href="https://www.python.org/downloads/">python官网</a>每个版本的发布都可以在<a href="https://www.python.org/ftp/python/">这里</a>快捷地找到。<br>除了官网之外，也有镜像源可以使用：<a href="https://mirrors.aliyun.com/python-release/">阿里源</a>&#x2F;<a href="https://mirrors.huaweicloud.com/python/">华为源</a>&#x2F;<a href="https://registry.npmmirror.com/binary.html?path=python/">CNPM源</a>&#x2F;<a href="https://mirrors.ustc.edu.cn/python/">中科大源</a><br>按照官方的发布规则，bugfix阶段的既提供源码包，也提供Windows&#x2F;MacOS的安装包；security阶段的仅提供源码包。<code>.tgz</code>和<code>.tar.xz</code>两种格式的源码内容完全相同，仅压缩格式不同。</p><h3 id="git源码"><a href="#git源码" class="headerlink" title="git源码"></a>git源码</h3><p><a href="https://github.com/python/cpython">cpython的官方仓库</a>可以获取到任何版本以及还未加入正式发布的源码。<br>整个仓库很大，因此建议有选择性的clone，或者使用<code>--filter=blob:none</code>甚至是<code>--filter=tree:0</code><br>比如如果想要获取3.11版本的最新代码：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">git</span> clone https://github.com/python/cpython.git -b <span class="hljs-number">3</span>.<span class="hljs-number">11</span> --depth <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><p>其它需求同样，灵活使用<code>--single-branch</code> <code>--shallow-since</code>和<code>--shallow-exclude</code>等参数。<br>其中<code>--shallow since</code>接受格式为<code>YYYY-MM-DD</code>的日期，只保留该日之后（不含当日）；</p><div class="note note-info">            <p>可以进一步指定时分秒，例如<code>--shallow-since 2025-07-21-12-09-00</code>，但是意义不大，github上面又不方便看具体的提交时间。</p>           </div><p><code>--shallow-exclude</code>接受tag作为参数，仅保留该tag之后的提交（不包含该tag）。</p><div class="note note-info">            <p>官方发布的安装包，版本信息是这样的：<br><img src="/img/python-envs/tag.png"><br>但是用发布的源码包编译的版本信息是这样的：<br><img src="/img/python-envs/main.png"><br>因此可以判断官方发布的包采用了从git仓库使用<code>git checkout &lt;tag&gt;</code>的源码编译。</p>           </div><h2 id="Windows"><a href="#Windows" class="headerlink" title="Windows"></a>Windows</h2><h3 id="准备编译环境"><a href="#准备编译环境" class="headerlink" title="准备编译环境"></a>准备编译环境</h3><ol><li>需要安装<a href="https://git-scm.com/downloads/win">git-for-windows</a>，用于自动拉取编译必需的一些其它依赖；有镜像源（<a href="https://registry.npmmirror.com/binary.html?path=git-for-windows">CNPM</a>&#x2F;<a href="https://mirrors.huaweicloud.com/git-for-windows/">华为源</a>）可用。</li><li>需要<a href="https://visualstudio.microsoft.com/zh-hans/downloads/">Visual Studio或者Visual Studio Build Tools</a>。对于最简安装，下拉到“所有下载”，然后下载“Visual Studio 生成工具”即可。<br> 安装的时候只需要选择这些项即可：<br> 首先选择使用C++的桌面开发，保留核心的生成工具和Windows SDK即可（Windows SDK测试过为必需）：<br> <img src="/img/python-envs/vsbuild.png"><br> 然后对于python3.11及以上，前往单个组件页面，搜索ARM64，安装ARM64生成工具（必需，按官方说法是wix3.14以上强制要求）：<br> <img src="/img/python-envs/vsbuildarm.png"> <div class="note note-info">            <p>ARM64工具选择能找到的最新版本，以与x86&#x2F;x64版本匹配。</p>           </div></li><li>Windows功能：对于3.10及以下，需要启用.NET Framework 3.5（新系统默认不启用）；对于3.11及以上，需要启用.NET Framework 4.8（新系统默认启用无需修改）：<br> <img src="/img/python-envs/dotnet.png"></li><li>可选：需要有一个已有的python安装；如果编译python3.12安装包，需要3.11及以上。如果系统没有安装，则后续会使用nuget拉取一个python环境；但是需要注意，如果有python但是当前激活的环境版本过低会编译失败。</li></ol><h3 id="编译"><a href="#编译" class="headerlink" title="编译"></a>编译</h3><p>获取源码后，建一个虚拟环境（以3.11为例）并激活，进入<code>Tools/msi</code>目录（两步不分先后，虚拟环境建在哪都一样）。</p><figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cmd">py -<span class="hljs-number">3</span>.<span class="hljs-number">11</span> venv venv<br>venv\Scripts\activate<br><span class="hljs-built_in">cd</span> Tools\msi<br></code></pre></td></tr></table></figure><p>然后直接执行<code>buildrelease.bat</code>即可，需要使用的参数列在下面：</p><figure class="highlight llvm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs llvm">-D: 跳过帮助文档构建。如果没有已构建的帮助文档，会报错；<br>-B: 跳过已经编译的内容。如果不是第一次执行buildrelease.bat可以加上这个；<br>-o &lt;dir&gt;: 指定编译的输出路径。-o输出的目录结构与原始编译产物的目录结构不同，-o输出的更便于整理，推荐使用；<br>-<span class="hljs-keyword">x</span><span class="hljs-number">64</span>: 仅编译<span class="hljs-keyword">x</span><span class="hljs-number">64</span>；也可以指定针对<span class="hljs-keyword">x</span><span class="hljs-number">86</span>或者ARM<span class="hljs-number">64</span>编译；如果不显式指明，默认同时编译<span class="hljs-keyword">x</span><span class="hljs-number">86</span>和<span class="hljs-keyword">x</span><span class="hljs-number">64</span>的安装包。<br></code></pre></td></tr></table></figure>    <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-d8532be7" role="button" aria-expanded="false" aria-controls="collapse-d8532be7">        <div class="fold-arrow">▶</div>buildrelease.bat的工作流程      </div>      <div class="fold-collapse collapse" id="collapse-d8532be7">        <div class="fold-content">          <ol> <li>准备编译帮助文档的依赖：<br> 一部分需要从github拉取；<br> 另一部分为python依赖，这部分首先会检查当前是否有可用的python环境，如果没有就用nuget获取一个；如果有，就用pip获取必需工具<code>sphinx</code>。<br> pip获取的依赖按照<code>Doc/requirements.txt</code>指定。</li> <li>编译帮助文档；</li> <li>从github拉取编译安装包的依赖；</li> <li>编译安装包本体；</li> <li>如果指定了<code>-o</code>参数，编译完成后将编译产物<strong>复制</strong>到指定目录。<br>其中所有从github获取的依赖均会位于<code>externals</code>目录内。</li> </ol>         </div>      </div>    </div><div class="note note-info">            <p>2026年3月14日更新：如果遇到这样的报错：</p> <blockquote> <p>C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\MSBuild\Microsoft\VC\v180\Microsoft.CppBuild.targets(473,5): error MSB8020: 无法找到 Visual Studio 2015 的生成工具(平台工具集 &#x3D;“v140”)。若要使用 v140 生成工具进行生成，请安装 Visual Studio 2015 生成工具。或者，可以升级到当前 Visual Studio 工具，方式是通过选择“项目”菜单或右键单击该解决方案，然后选择“重定解决方案目标”。 [C:\Users\pc\Desktop\MyFile\python\src\Python-3.10.20\PCbuild\pythoncore.vcxproj]</p> </blockquote> <p>说明<code>buildrelease.bat</code>没有正确获取到安装的vs工具集版本。用环境变量强制指定：</p> <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmd"><span class="hljs-built_in">set</span> PlatformToolset=v143<br></code></pre></td></tr></table></figure> <p>对于vs2022通常是v143, 对于vs2026通常是v145。<br>如果不确定实际安装的版本，可以打开vs安装目录里面的<code>VC\Auxiliary\Build</code>路径，根据文件名大致判断。<br>有的时候，vs会提供两套工具集（例如2026年5月的vs build tools 2026里面就放了一份v143兼容工具集，且通过阅读配置<code>Microsoft.VCToolsVersion.v143.default.props</code>可知实际上对应的是vs2022 lts）。<br>如果使用v145, 会收到警告：<code>warning : Toolset v145 is not used for official builds. Your build may have errors or incompatibilities.</code><br>实际上，官方提供的python3.14安装包仍然是基于v143编译的，通过启动显示的版本号<code>[MSC v.1944 64 bit (AMD64)]</code>可知。相应的，使用v145会得到<code>[MSC v.1951 64 bit (AMD64)]</code>。<br><del>但是都自己编译源码了，工具链不冲最新怎么行</del></p>           </div><h4 id="编译产物整理"><a href="#编译产物整理" class="headerlink" title="编译产物整理"></a>编译产物整理</h4><p>如果未指定<code>-o</code>目录，前往<code>PCBuild</code>目录，x64的位于<code>amd64/en-us</code>目录内。<br>目录结构应该是这样：<br><img src="/img/python-envs/no-o.png"><br>如果指定了<code>-o</code>目录，前往对应目录，x64的位于<code>amd64</code>目录内。<br>目录结构应该是这样：<br><img src="/img/python-envs/with-o.png"><br>其中：</p><ol><li><code>*-amd64.exe</code>是完整安装包，可以单独完成默认安装；</li><li><code>*-embeded-amd64.zip</code>等效于官网的<code>Windows embeddable package (64-bit)</code>；</li><li>其它的所有msi安装包，可以留作备用。当需要使用到自定义安装中，描述中带有”Download”选项的，会用到这其中的<em>一部分</em>msi文件，需要用到这些msi的时候，放在和exe同一个目录内即可，exe自己会调用；不需要手动运行这些msi.<br> 可以在命令行中，执行<code>*-amd64.exe /layout &lt;output dir&gt;</code>获取所有必需msi，其它的就不用保留了。对于官网下载的安装包也可以使用这种方式获取离线安装包。</li><li>python3.10的输出还会有一个<code>*webinstall.exe</code>，也是调用这些msi，但是没必要留下，直接留完整安装包就行<br><img src="/img/python-envs/download.png"></li></ol><p>咱编译过的安装包已经全部上传到<a href="https://huggingface.co/silvertuanzi/python-windows-build/tree/main">huggingface</a>上留档。</p><h2 id="Linux"><a href="#Linux" class="headerlink" title="Linux"></a>Linux</h2><h3 id="依赖准备"><a href="#依赖准备" class="headerlink" title="依赖准备"></a>依赖准备</h3><p>由于个人喜好，不记录rpm系(<code>yum/dnf</code>)的必需依赖。<br>信息存在部分冲突，所以分信息来源收录。</p><div class="note note-warning">            <p>特别注意：如果需要使用tcl&#x2F;tk（<code>import tkinter</code>），必须在编译之前准备好必需的头文件，例如apt系的<code>tk-dev</code>, pacman系的<code>tk</code>。<br>否则，当需要tkinter功能的时候，需要重新编译，仅仅补充依赖是不够的。</p>           </div><h4 id="apt系"><a href="#apt系" class="headerlink" title="apt系"></a>apt系</h4><ol><li><a href="https://devguide.python.org/getting-started/setup-building/#build-dependencies">python官方编译文档</a><br> 首先需要启用源码仓库：Ubuntu等有软件源管理GUI的，直接开启即可；Debian没有GUI管理，需要编辑<code>/etc/apt/source.list</code>(旧格式)或者<code>/etc/apt/sources.list.d/debian.sources</code>(新DEB822格式)，将<code>deb-src</code>源取消注释（有的版本默认已经启用）。<br> 然后执行： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">apt update<br>apt build-dep python3<br>apt install pkg-config<br></code></pre></td></tr></table></figure> 执行过<code>apt build-dep</code>之后源码仓库即不再需要，可以关回去而不影响使用；以上为必需依赖。<br> 如果需要编译完整功能，则： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash">apt install build-essential gdb lcov pkg-config \<br>     libbz2-dev libffi-dev libgdbm-dev libgdbm-compat-dev liblzma-dev \<br>     libncurses5-dev libreadline6-dev libsqlite3-dev libssl-dev \<br>     lzma lzma-dev tk-dev uuid-dev zlib1g-dev libmpdec-dev libzstd-dev \<br>     inetutils-inetd<br></code></pre></td></tr></table></figure> <div class="note note-info">            <ol> <li>Debian12和Ubuntu24.04因为未知原因，仓库中不包含<code>libmpdec-dev</code>。可以不安装，自动调用python内置的版本；<br> 或者，可以调用<a href="https://launchpad.net/~ondrej/+archive/ubuntu/php">Ubuntu ppa源</a>以及<a href="https://packages.sury.org/php/">Debian DPA源</a>，参见<a href="https://deb.sury.org/">https://deb.sury.org/</a><br> Ubuntu添加方式：<code>add-apt-repository ppa:ondrej/php</code><br> <a href="https://packages.sury.org/php/README.txt">Debian添加方式参看仓库readme</a><br> Debian13(trixie)和Debian unstable(sid)恢复了这个包，因此正常安装即可。<br> 有关这个问题的所有讨论可以从<a href="https://github.com/python/cpython/issues/119114">这里</a>开始了解，内部的其它链接也可以进一步拓展信息来源。<br> <strong>按照cpython开发计划，对内置mpdecimal的支持将在python3.16终止。尽量使用系统提供的全局mpdecimal。</strong></li> <li><code>libzstd-dev</code>和<code>inetutils-inetd</code>貌似是在python3.14新引入的。</li> </ol>           </div></li><li><a href="https://github.com/python/cpython/blob/main/.github/workflows/posix-deps-apt.sh">cpython官方github action工作流</a> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><code class="hljs bash">apt-get update<br><br>apt-get -yq install \<br>build-essential \<br>pkg-config \<br>ccache \<br>cmake \<br>gdb \<br>lcov \<br>libb2-dev \<br>libbz2-dev \<br>libffi-dev \<br>libgdbm-dev \<br>libgdbm-compat-dev \<br>liblzma-dev \<br>libncurses5-dev \<br>libreadline6-dev \<br>libsqlite3-dev \<br>libssl-dev \<br>libzstd-dev \<br>lzma \<br>lzma-dev \<br>strace \<br>tk-dev \<br>uuid-dev \<br>xvfb \<br>zlib1g-dev<br><br><span class="hljs-comment"># Workaround missing libmpdec-dev on ubuntu 24.04:</span><br><span class="hljs-comment"># https://launchpad.net/~ondrej/+archive/ubuntu/php</span><br><span class="hljs-comment"># https://deb.sury.org/</span><br><span class="hljs-built_in">sudo</span> add-apt-repository ppa:ondrej/php<br>apt-get update<br>apt-get -yq install libmpdec-dev<br></code></pre></td></tr></table></figure></li><li><a href="https://github.com/pyenv/pyenv/wiki#suggested-build-environment">pyenv文档的建议</a> <div class="note note-info">            <p>pyenv提供了一套管理多版本python共存的方案，但是是通过一键式拉取源码编译的方式完成的，所以感觉不如手动编译可控性强。</p>           </div> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">apt update<br>apt install make build-essential libssl-dev zlib1g-dev \<br>libbz2-dev libreadline-dev libsqlite3-dev curl git \<br>libncursesw5-dev xz-utils tk-dev libxml2-dev libxmlsec1-dev libffi-dev liblzma-dev<br></code></pre></td></tr></table></figure> 对于python3.14及以上，额外安装<code>libzstd-dev</code>编译<code>compress.zstd</code>模块。但是ubuntu20.04及更旧的系统版本不具有足够版本。<br> <a href="https://github.com/pyenv/pyenv/wiki/Common-build-problems">可选建议，不在推荐集合中，通常只有在有特殊需求或其中存在问题的情况下才应考虑使用：</a> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">apt install libedit-dev <span class="hljs-comment">#libreadline-dev的替代品</span><br>apt install libncurses5-dev <span class="hljs-comment">#libncursesw5更差的替代品</span><br></code></pre></td></tr></table></figure></li></ol><h4 id="pacman系"><a href="#pacman系" class="headerlink" title="pacman系"></a>pacman系</h4><p>Arch可以参照的文档就没那么多，主打一个自己找吃的：</p><ol><li>从<a href="https://devguide.python.org/getting-started/setup-building/#build-dependencies">python官方编译文档</a>的deb包转写： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S base-devel <span class="hljs-comment">#必需，不会有Arch人没装这个吧</span><br>pacman -S gdb lcov bzip2 libffi gdbm xz ncurses readline sqlite openssl tk utils-linux-lib zlib mpdecimal zstd inetutils --needed<br></code></pre></td></tr></table></figure> 其中<code>zstd</code>和<code>inetutils</code>貌似是在python3.14新引入的。</li><li>从<a href="https://github.com/python/cpython/blob/main/.github/workflows/posix-deps-apt.sh">cpython官方github action工作流</a>的deb包转写： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S --needed base-devel ccache cmake gdb lcov libb2 bzip2 libffi gdbm xz ncurses readline sqlite openssl zstd strace tk utils-linux-lib xorg-server-xvfb zlib mpdecimal<br></code></pre></td></tr></table></figure></li><li><a href="https://github.com/pyenv/pyenv/wiki#suggested-build-environment">pyenv文档的建议</a> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S --needed base-devel openssl zlib xz tk zstd<br></code></pre></td></tr></table></figure> <a href="https://github.com/pyenv/pyenv/wiki/Common-build-problems">可选建议，不在推荐集合中，通常只有在有特殊需求或其中存在问题的情况下才应考虑使用：</a><br> 如果需要旧版本ncurses(<code>ncurses5</code>)，需要通过AUR安装<code>ncurses5-compat-libs</code>。archlinuxcn源和arch4edu源都提供了该包。</li><li>原先的官方编译脚本（AUR的就不搬了）：<ul><li><a href="https://gitlab.archlinux.org/archlinux/packaging/packages/python/-/blob/3.10.10-1/PKGBUILD?ref_type=tags">python3.10.10-1</a>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S --needed bzip2 expat gdbm libffi libnsl libxcrypt openssl zlib <span class="hljs-comment">#depends</span><br>pacman -S --needed tk sqlite bluez-libs mpdecimal llvm gdb xorg-server-xvfb ttf-font <span class="hljs-comment">#makedepends</span><br></code></pre></td></tr></table></figure></li><li><a href="https://gitlab.archlinux.org/archlinux/packaging/packages/python/-/blob/3.11.8-1/.SRCINFO?ref_type=tags">python3.11.8-1</a>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S --needed bzip2 expat gdbm libffi libnsl libxcrypt openssl zlib tzdata <span class="hljs-comment">#depends</span><br>pacman -S --needed tk sqlite bluez-libs mpdecimal llvm gdb xorg-server-xvfb ttf-font <span class="hljs-comment">#makedepends</span><br></code></pre></td></tr></table></figure></li><li><a href="https://gitlab.archlinux.org/archlinux/packaging/packages/python/-/blob/3.12.7-1/.SRCINFO?ref_type=tags">python3.12.7-1</a>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S --needed bzip2 expat gdbm libffi libnsl libxcrypt openssl zlib tzdata mpdecimal <span class="hljs-comment">#depends</span><br>pacman -S --needed tk sqlite bluez-libs llvm gdb xorg-server-xvfb ttf-font <span class="hljs-comment">#makedepends</span><br></code></pre></td></tr></table></figure></li></ul></li></ol><h3 id="土法编译"><a href="#土法编译" class="headerlink" title="土法编译"></a>土法编译</h3><p>依赖和源码准备好之后，进入源码根目录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">./configure &lt;编译参数&gt;<br></code></pre></td></tr></table></figure><p>编译参数这里，通常来说会需要这些：</p><figure class="highlight crystal"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs crystal">--prefix=<span class="hljs-regexp">/path/to</span><span class="hljs-regexp">/dir: 安装路径，默认是/usr</span><span class="hljs-regexp">/local，也就是把文件分列进/usr</span><span class="hljs-regexp">/local/</span>&#123;bin,<span class="hljs-class"><span class="hljs-keyword">lib</span>,<span class="hljs-title">include</span>,<span class="hljs-title">share</span>&#125;等等；修改<span class="hljs-title">prefix</span>为单独目录会便于卸载。如果想要使用系统包管理打包，就指定为/<span class="hljs-title">usr</span></span><br>--enable-<span class="hljs-symbol">optimizations:</span> 启用pgo优化，并在部分平台自动开启lto优化<br>--<span class="hljs-keyword">with</span>-<span class="hljs-symbol">lto:</span> 强制开启lto优化<br></code></pre></td></tr></table></figure><p>上面这些最好是都用上的；<br>对于手动管理的版本，比较好的选择是<code>--prefix=/opt/python/python3.X</code>或者<code>--prefix=/usr/local/python/python3.X</code><br>Debian12和Ubuntu24.04，如果没补装<code>libmpdec-dev</code>，编译python3.12及以下版本时，保险起见最好显式指定使用python内置的mpdec：<code>--with-system-mpdec=no</code>或者<code>--without-system-libmpdec</code><br>对于其它有系统mpdecimal头文件的，python官方推荐使用系统内置的版本：<code>--with-system-mpdec</code><br>在python3.12及以前，<code>--with-system-libmpdec</code>的默认值均为<code>no</code>；3.13开始，默认值变为<code>yes</code>，并且在未检测到必需文件时自动回退到使用内置版本</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">./configure --enable-optimizations --with-lto --prefix=/opt/python/python3.X<br></code></pre></td></tr></table></figure><p>除此之外，收录部分发行版使用的所有configure参数：</p><div class="note note-info">            <p>从distro里面抄参数可能会在configure阶段遇到缺依赖的报错，照着报错补就行。<br>例如Ubuntu24.04遇到</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">checking <span class="hljs-keyword">for</span> dtrace... not found<br>configure: error: dtrace <span class="hljs-built_in">command</span> not found on <span class="hljs-variable">$PATH</span><br></code></pre></td></tr></table></figure> <p>直接输入dtrace检测：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">找不到命令 “dtrace”，但可以通过以下软件包安装它：<br><span class="hljs-built_in">sudo</span> apt install systemtap-sdt-dev<br></code></pre></td></tr></table></figure> <p>补装即可。<br>或者保险起见可以把系统里面默认版本的编译集合全拉下来：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">apt build-dep python3.12 <span class="hljs-comment">#Ubuntu24.04</span><br>apt build-dep python3.13 <span class="hljs-comment">#Debian13</span><br></code></pre></td></tr></table></figure> <p>至于Arch，从PKGBUILD全抄了完事。</p>           </div><div class="note note-warning">            <p><code>--prefix=/usr</code>不要抄！</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-7c2d22a6" role="button" aria-expanded="false" aria-controls="collapse-7c2d22a6">        <div class="fold-arrow">▶</div>Arch      </div>      <div class="fold-collapse collapse" id="collapse-7c2d22a6">        <div class="fold-content">          <p><a href="https://gitlab.archlinux.org/archlinux/packaging/packages/python/-/blob/3.10.10-1/PKGBUILD?ref_type=tags">参考源码-3.10.10</a><br><a href="https://gitlab.archlinux.org/archlinux/packaging/packages/python/-/blob/3.11.8-1/PKGBUILD?ref_type=tags">参考源码-3.11.8</a><br><a href="https://gitlab.archlinux.org/archlinux/packaging/packages/python/-/blob/3.12.7-1/PKGBUILD?ref_type=tags">参考源码-3.12.7</a></p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># Disable bundled pip &amp; setuptools</span><br>./configure --prefix=/usr \<br>            --enable-shared \<br>            --with-computed-gotos \<br>            --enable-optimizations \<br>            --with-lto \<br>            --enable-ipv6 \<br>            --with-system-expat \<br>            --with-dbmliborder=gdbm:ndbm \<br>            --with-system-libmpdec \<br>            --enable-loadable-sqlite-extensions \<br>            --without-ensurepip \<br>            --with-tzpath=/usr/share/zoneinfo<br></code></pre></td></tr></table></figure>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-03e0d628" role="button" aria-expanded="false" aria-controls="collapse-03e0d628">        <div class="fold-arrow">▶</div>Debian12(bookworm)-3.11.2，源文件巨长，由deepseek解析      </div>      <div class="fold-collapse collapse" id="collapse-03e0d628">        <div class="fold-content">          <p><a href="https://sources.debian.org/src/python3.11/3.11.2-6%2Bdeb12u6/debian/rules">参考源码</a></p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs bash">./configure \<br>  --prefix=/usr \<br>  --libdir=/usr/lib/x86_64-linux-gnu \<br>  --enable-ipv6 \<br>  --enable-loadable-sqlite-extensions \<br>  --with-dbmliborder=bdb:gdbm \<br>  --with-computed-gotos \<br>  --without-ensurepip \<br>  --with-system-expat \<br>  --with-system-ffi \<br>  --with-dtrace \<br>  --with-ssl-default-suites=openssl \<br>  --with-wheel-pkg-dir=/usr/share/python-wheels/ \<br></code></pre></td></tr></table></figure>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-5b0f0617" role="button" aria-expanded="false" aria-controls="collapse-5b0f0617">        <div class="fold-arrow">▶</div>Debian13(trixie)-3.13.5，源文件巨长，由deepseek解析      </div>      <div class="fold-collapse collapse" id="collapse-5b0f0617">        <div class="fold-content">          <p><a href="https://sources.debian.org/src/python3.13/3.13.5-2/debian/rules">参考源码</a></p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs bash">./configure \<br>  --enable-shared \<br>  --prefix=/usr \<br>  --libdir=/usr/lib/x86_64-linux-gnu \<br>  --enable-ipv6 \<br>  --enable-loadable-sqlite-extensions \<br>  --with-dbmliborder=bdb:gdbm \<br>  --with-computed-gotos \<br>  --without-ensurepip \<br>  --with-system-expat \<br>  --with-dtrace \<br>  --with-ssl-default-suites=openssl \<br>  --with-system-libmpdec=no \<br>  --with-wheel-pkg-dir=/usr/share/python-wheels/ \<br></code></pre></td></tr></table></figure>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-0c5af8e3" role="button" aria-expanded="false" aria-controls="collapse-0c5af8e3">        <div class="fold-arrow">▶</div>Ubuntu22.04(Jammy)-3.10.12，源文件巨长，由GPT-5解析      </div>      <div class="fold-collapse collapse" id="collapse-0c5af8e3">        <div class="fold-content">          <p><a href="/img/python-envs/jammy-rules">参考源码</a></p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs bash">./configure \<br>  --prefix=/usr \<br>  --libdir=/usr/lib/x86_64-linux-gnu \<br>  --enable-ipv6 \<br>  --enable-loadable-sqlite-extensions \<br>  --with-dbmliborder=bdb:gdbm \<br>  --with-computed-gotos \<br>  --without-ensurepip \<br>  --with-system-expat \<br>  --with-dtrace \<br>  --with-system-libmpdec \<br>  --with-system-ffi \<br>  --with-wheel-pkg-dir=/usr/share/python-wheels/ \<br>  --enable-shared<br></code></pre></td></tr></table></figure>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-e3197aa5" role="button" aria-expanded="false" aria-controls="collapse-e3197aa5">        <div class="fold-arrow">▶</div>Ubuntu24.04(Noble)-3.12.3，源文件巨长，由GPT-5解析      </div>      <div class="fold-collapse collapse" id="collapse-e3197aa5">        <div class="fold-content">          <p><a href="/img/python-envs/noble-rules">参考源码</a></p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs bash">./configure \<br>  --prefix=/usr \<br>  --libdir=/usr/lib/x86_64-linux-gnu \<br>  --enable-ipv6 \<br>  --enable-loadable-sqlite-extensions \<br>  --with-dbmliborder=bdb:gdbm \<br>  --with-computed-gotos \<br>  --without-ensurepip \<br>  --with-system-expat \<br>  --with-dtrace \<br>  --with-ssl-default-suites=openssl \<br>  --with-wheel-pkg-dir=/usr/share/python-wheels/ \<br>  --enable-shared<br></code></pre></td></tr></table></figure>        </div>      </div>    </div><p>configure配置完后，执行编译和安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">make <span class="hljs-comment">#使用-jx多线程，例如-j9</span><br>make <span class="hljs-built_in">test</span> <span class="hljs-comment">#测试，可选</span><br>make altinstall <span class="hljs-comment">#使用altinstall安装不会创建python3和pip3软链接</span><br></code></pre></td></tr></table></figure><p><code>make test</code>出现一些奇怪的失败非常正常，成因各不相同，可以手动运行<code>./python -m test &lt;test_*&gt;</code>的方式重新测试检查原因，配合llm解读；<br>手动编译非常不建议开启<code>--enable-shared</code>，如果是下文打包的方式开启了<code>--enable-shared</code>，则测试前需要先把当前所在的目录加入<code>LD_LIBRARY_PATH</code>。</p><h3 id="打包"><a href="#打包" class="headerlink" title="打包"></a>打包</h3><div class="note note-info">            <p>对于打包方法的编译参数，考虑使用<code>--without-ensurepip</code>；咱自己是觉得打包一个pip进去更好，但是再次打包的时候，<code>make altinstall</code>执行<code>ensurepip</code>的时候，会检测到系统里面已经安装了相应的pip，从而认为<code>Requirement already satisfied</code>，不再打包一个新的pip进去，因此需要在打包之前先卸载掉上一个版本，或者把<code>/usr/lib/python3.X/site-packages</code>目录用一些特殊的方法（例如<code>mount</code>）屏蔽掉。</p>           </div><h4 id="PKGBUILD"><a href="#PKGBUILD" class="headerlink" title="PKGBUILD"></a>PKGBUILD</h4><div class="note note-info">            <p>更新：</p> <ol> <li>统一了<code>${srcdir}</code>和<code>${pkgdir}</code>的规范格式，不再使用<code>$srcdir</code>和<code>$pkgdir</code></li> <li>由于<code>ensurepip</code>在系统内已存在pip模块的时候会误判，导致打包漏掉pip，补充了手动补装bundled pip的逻辑；<del>与此同时，打包结束后移除不必要的bundled wheel节省空间</del>更新：删除bundled wheel会导致venv失败，撤销这项改动</li> <li>优化compile pyc逻辑，现在会阻止<code>ensurepip</code>和<code>pip</code>自动生成<code>.pyc</code>，改为手动调用<code>compileall</code>以调整记录的路径，从而消除对<code>${pkgdir}</code>引用的警告</li> <li>优化依赖列表，列出可选依赖，把<code>ccache</code>移出构建依赖，需要<code>ccache</code>自装即可</li> </ol>           </div><p>直接放三份PKGBUILD在这里，都是最简单粗暴的写法：</p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-0e514b77" role="button" aria-expanded="false" aria-controls="collapse-0e514b77">        <div class="fold-arrow">▶</div>python310-git      </div>      <div class="fold-collapse collapse" id="collapse-0e514b77">        <div class="fold-content">          <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># Maintainer: Silver Tuanzi</span><br>pkgname=python310-git<br>pkgver=v3.10.18.r13.f610f9e<br>pkgrel=1<br>pkgdesc=<span class="hljs-string">&quot;The Python programming language (version 3.10)&quot;</span><br><span class="hljs-built_in">arch</span>=(<span class="hljs-string">&#x27;x86_64&#x27;</span> <span class="hljs-string">&#x27;aarch64&#x27;</span>)<br>license=(<span class="hljs-string">&quot;PSF-2.0&quot;</span>)<br>url=<span class="hljs-string">&quot;www.python.org&quot;</span><br>depends=(<span class="hljs-string">&#x27;bzip2&#x27;</span> <span class="hljs-string">&#x27;expat&#x27;</span> <span class="hljs-string">&#x27;gdbm&#x27;</span> <span class="hljs-string">&#x27;libffi&#x27;</span> <span class="hljs-string">&#x27;libnsl&#x27;</span> <span class="hljs-string">&#x27;libxcrypt&#x27;</span> <span class="hljs-string">&#x27;openssl&#x27;</span> <span class="hljs-string">&#x27;zlib&#x27;</span> <span class="hljs-string">&#x27;mpdecimal&#x27;</span>)<br>makedepends=(<span class="hljs-string">&#x27;git&#x27;</span> <span class="hljs-string">&#x27;gdb&#x27;</span> <span class="hljs-string">&#x27;xz&#x27;</span> <span class="hljs-string">&#x27;sqlite&#x27;</span> <span class="hljs-string">&#x27;tk&#x27;</span>)<br>optdepends=(<span class="hljs-string">&#x27;sqlite: for a default database integration&#x27;</span><br>            <span class="hljs-string">&#x27;xz: for lzma&#x27;</span><br>            <span class="hljs-string">&#x27;tk: for tkinter&#x27;</span>)<br>provides=(<span class="hljs-string">&#x27;python310&#x27;</span>)<br>conflicts=(<span class="hljs-string">&#x27;python310&#x27;</span>)<br>options=(strip !debug)<br><span class="hljs-built_in">source</span>=(<span class="hljs-string">&#x27;python310::git+https://github.com/python/cpython.git#branch=3.10&#x27;</span>)<br>sha256sums=(<span class="hljs-string">&#x27;SKIP&#x27;</span>)<br><br><span class="hljs-function"><span class="hljs-title">pkgver</span></span>() &#123;<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310&quot;</span><br><span class="hljs-built_in">printf</span> <span class="hljs-string">&quot;%s&quot;</span> <span class="hljs-string">&quot;<span class="hljs-subst">$(git describe --long | sed &#x27;s/\([^-]*-\)g/r\1/;s/-/./g&#x27;)</span>&quot;</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-title">build</span></span>() &#123;<br>  <span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310&quot;</span><br>  ./configure --prefix=/usr \<br>              --with-computed-gotos \<br>              --enable-optimizations \<br>              --with-lto \<br>              --enable-ipv6 \<br>              --with-system-expat \<br>              --with-system-ffi \<br>              --with-dbmliborder=gdbm:ndbm \<br>              --with-system-libmpdec \<br>              --enable-loadable-sqlite-extensions \<br>              --enable-shared \<br>              ax_cv_c_float_words_bigendian=no<br>make<br>&#125;<br><br><span class="hljs-function"><span class="hljs-title">package</span></span>() &#123;<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310&quot;</span><br>PYTHONDONTWRITEBYTECODE=1 make DESTDIR=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/&quot;</span> altinstall<br><span class="hljs-built_in">export</span> PIP_FIND_LINKS=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>&quot;</span>/python310/Lib/ensurepip/_bundled/<br><span class="hljs-keyword">if</span> [[ ! -d <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/python3.10/site-packages/pip&quot;</span> ]]; <span class="hljs-keyword">then</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Fix missing pip...&quot;</span><br>PYTHONDONTWRITEBYTECODE=1 LD_LIBRARY_PATH=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310/python&quot;</span> -m pip install --no-index --root=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span> --ignore-installed pip<br><span class="hljs-built_in">rm</span> -f <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/bin/pip&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/bin/pip3&quot;</span><br><span class="hljs-keyword">else</span><br>sed -i <span class="hljs-string">&#x27;1s|^#!/usr/bin/python$|#!/usr/bin/python3.10|&#x27;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/bin/pip3.10&quot;</span><br><span class="hljs-keyword">fi</span><br><span class="hljs-keyword">if</span> [[ ! -d <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/python3.10/site-packages/setuptools&quot;</span> ]]; <span class="hljs-keyword">then</span><br>PYTHONDONTWRITEBYTECODE=1 LD_LIBRARY_PATH=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310/python&quot;</span> -m pip install --no-index --root=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span> --ignore-installed setuptools<br><span class="hljs-keyword">fi</span><br><span class="hljs-built_in">rm</span> -f <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/libpython3.so&quot;</span><br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span>/usr/lib/python*/<br><span class="hljs-built_in">rm</span> -r <span class="hljs-built_in">test</span> &amp;&amp; <span class="hljs-built_in">rm</span> -r */*<span class="hljs-built_in">test</span>*/<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span><br>LD_LIBRARY_PATH=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python310/python&quot;</span> -m compileall -s <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span> -p / usr/lib/python3.10/site-packages/<br>&#125;<br></code></pre></td></tr></table></figure>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-ad9eb357" role="button" aria-expanded="false" aria-controls="collapse-ad9eb357">        <div class="fold-arrow">▶</div>python311-git      </div>      <div class="fold-collapse collapse" id="collapse-ad9eb357">        <div class="fold-content">          <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># Maintainer: Silver Tuanzi</span><br>pkgname=python311-git<br>pkgver=v3.11.13.r15.8adac49<br>pkgrel=1<br>pkgdesc=<span class="hljs-string">&quot;The Python programming language (version 3.11)&quot;</span><br><span class="hljs-built_in">arch</span>=(<span class="hljs-string">&#x27;x86_64&#x27;</span> <span class="hljs-string">&#x27;aarch64&#x27;</span>)<br>license=(<span class="hljs-string">&quot;PSF-2.0&quot;</span>)<br>url=<span class="hljs-string">&quot;www.python.org&quot;</span><br>depends=(<span class="hljs-string">&#x27;bzip2&#x27;</span> <span class="hljs-string">&#x27;expat&#x27;</span> <span class="hljs-string">&#x27;gdbm&#x27;</span> <span class="hljs-string">&#x27;libffi&#x27;</span> <span class="hljs-string">&#x27;libnsl&#x27;</span> <span class="hljs-string">&#x27;libxcrypt&#x27;</span> <span class="hljs-string">&#x27;openssl&#x27;</span> <span class="hljs-string">&#x27;zlib&#x27;</span> <span class="hljs-string">&#x27;mpdecimal&#x27;</span>)<br>makedepends=(<span class="hljs-string">&#x27;git&#x27;</span> <span class="hljs-string">&#x27;gdb&#x27;</span> <span class="hljs-string">&#x27;xz&#x27;</span> <span class="hljs-string">&#x27;sqlite&#x27;</span> <span class="hljs-string">&#x27;tk&#x27;</span>)<br>optdepends=(<span class="hljs-string">&#x27;sqlite: for a default database integration&#x27;</span><br>            <span class="hljs-string">&#x27;xz: for lzma&#x27;</span><br>            <span class="hljs-string">&#x27;tk: for tkinter&#x27;</span>)<br>provides=(<span class="hljs-string">&#x27;python311&#x27;</span>)<br>conflicts=(<span class="hljs-string">&#x27;python311&#x27;</span>)<br>options=(strip !debug)<br><span class="hljs-built_in">source</span>=(<span class="hljs-string">&#x27;python311::git+https://github.com/python/cpython.git#branch=3.11&#x27;</span>)<br>sha256sums=(<span class="hljs-string">&#x27;SKIP&#x27;</span>)<br><br><span class="hljs-function"><span class="hljs-title">pkgver</span></span>() &#123;<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311&quot;</span><br><span class="hljs-built_in">printf</span> <span class="hljs-string">&quot;%s&quot;</span> <span class="hljs-string">&quot;<span class="hljs-subst">$(git describe --long | sed &#x27;s/\([^-]*-\)g/r\1/;s/-/./g&#x27;)</span>&quot;</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-title">build</span></span>() &#123;<br>  <span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311&quot;</span><br>  ./configure --prefix=/usr \<br>              --with-computed-gotos \<br>              --enable-optimizations \<br>              --with-lto \<br>              --enable-ipv6 \<br>              --with-system-expat \<br>              --with-system-ffi \<br>              --with-dbmliborder=gdbm:ndbm \<br>              --with-system-libmpdec \<br>              --enable-loadable-sqlite-extensions \<br>              --enable-shared \<br>              ax_cv_c_float_words_bigendian=no<br>make<br>&#125;<br><br><span class="hljs-function"><span class="hljs-title">package</span></span>() &#123;<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311&quot;</span><br>PYTHONDONTWRITEBYTECODE=1 make DESTDIR=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/&quot;</span> altinstall<br><span class="hljs-built_in">export</span> PIP_FIND_LINKS=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>&quot;</span>/python311/Lib/ensurepip/_bundled/<br><span class="hljs-keyword">if</span> [[ ! -d <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/python3.11/site-packages/pip&quot;</span> ]]; <span class="hljs-keyword">then</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Fix missing pip...&quot;</span><br>PYTHONDONTWRITEBYTECODE=1 LD_LIBRARY_PATH=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311/python&quot;</span> -m pip install --no-index --root=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span> --ignore-installed pip<br><span class="hljs-built_in">rm</span> -f <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/bin/pip&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/bin/pip3&quot;</span><br><span class="hljs-keyword">fi</span><br><span class="hljs-keyword">if</span> [[ ! -d <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/python3.11/site-packages/setuptools&quot;</span> ]]; <span class="hljs-keyword">then</span><br>PYTHONDONTWRITEBYTECODE=1 LD_LIBRARY_PATH=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311/python&quot;</span> -m pip install --no-index --root=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span> --ignore-installed setuptools<br><span class="hljs-keyword">fi</span><br><span class="hljs-built_in">rm</span> -f <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/libpython3.so&quot;</span><br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span>/usr/lib/python*/<br><span class="hljs-built_in">rm</span> -r <span class="hljs-built_in">test</span> &amp;&amp; <span class="hljs-built_in">rm</span> -r */*<span class="hljs-built_in">test</span>*/<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span><br>LD_LIBRARY_PATH=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python311/python&quot;</span> -m compileall -s <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span> -p / usr/lib/python3.11/site-packages/<br>&#125;<br></code></pre></td></tr></table></figure>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-ffbebcdb" role="button" aria-expanded="false" aria-controls="collapse-ffbebcdb">        <div class="fold-arrow">▶</div>python312-git      </div>      <div class="fold-collapse collapse" id="collapse-ffbebcdb">        <div class="fold-content">          <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># Maintainer: Silver Tuanzi</span><br>pkgname=python312-git<br>pkgver=v3.12.13.r21.7c999be49<br>pkgrel=1<br>pkgdesc=<span class="hljs-string">&quot;The Python programming language (version 3.12)&quot;</span><br><span class="hljs-built_in">arch</span>=(<span class="hljs-string">&#x27;x86_64&#x27;</span> <span class="hljs-string">&#x27;aarch64&#x27;</span>)<br>license=(<span class="hljs-string">&quot;PSF-2.0&quot;</span>)<br>url=<span class="hljs-string">&quot;www.python.org&quot;</span><br>depends=(<span class="hljs-string">&#x27;bzip2&#x27;</span> <span class="hljs-string">&#x27;expat&#x27;</span> <span class="hljs-string">&#x27;gdbm&#x27;</span> <span class="hljs-string">&#x27;libffi&#x27;</span> <span class="hljs-string">&#x27;libnsl&#x27;</span> <span class="hljs-string">&#x27;libxcrypt&#x27;</span> <span class="hljs-string">&#x27;openssl&#x27;</span> <span class="hljs-string">&#x27;zlib&#x27;</span> <span class="hljs-string">&#x27;mpdecimal&#x27;</span>)<br>makedepends=(<span class="hljs-string">&#x27;git&#x27;</span> <span class="hljs-string">&#x27;gdb&#x27;</span> <span class="hljs-string">&#x27;tk&#x27;</span> <span class="hljs-string">&#x27;xz&#x27;</span> <span class="hljs-string">&#x27;sqlite&#x27;</span>)<br>optdepends=(<span class="hljs-string">&#x27;sqlite: for a default database integration&#x27;</span><br>            <span class="hljs-string">&#x27;xz: for lzma&#x27;</span><br>            <span class="hljs-string">&#x27;tk: for tkinter&#x27;</span>)<br>provides=(<span class="hljs-string">&#x27;python312&#x27;</span>)<br>conflicts=(<span class="hljs-string">&#x27;python312&#x27;</span>)<br>options=(strip !debug)<br><span class="hljs-built_in">source</span>=(<span class="hljs-string">&#x27;python312::git+https://github.com/python/cpython.git#branch=3.12&#x27;</span>)<br>sha256sums=(<span class="hljs-string">&#x27;SKIP&#x27;</span>)<br><br><span class="hljs-function"><span class="hljs-title">pkgver</span></span>() &#123;<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python312&quot;</span><br><span class="hljs-built_in">printf</span> <span class="hljs-string">&quot;%s&quot;</span> <span class="hljs-string">&quot;<span class="hljs-subst">$(git describe --long | sed &#x27;s/\([^-]*-\)g/r\1/;s/-/./g&#x27;)</span>&quot;</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-title">build</span></span>() &#123;<br>  <span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python312&quot;</span><br>  ./configure --prefix=/usr \<br>              --with-computed-gotos \<br>              --enable-optimizations \<br>              --with-lto \<br>              --enable-ipv6 \<br>              --with-system-expat \<br>              --with-dbmliborder=gdbm:ndbm \<br>              --with-system-libmpdec \<br>              --enable-loadable-sqlite-extensions \<br>              --enable-shared \<br>              ax_cv_c_float_words_bigendian=no<br>make<br>&#125;<br><br><span class="hljs-function"><span class="hljs-title">package</span></span>() &#123;<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python312&quot;</span><br>PYTHONDONTWRITEBYTECODE=1 make DESTDIR=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/&quot;</span> altinstall<br><span class="hljs-built_in">export</span> PIP_FIND_LINKS=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>&quot;</span>/python312/Lib/ensurepip/_bundled/<br><span class="hljs-keyword">if</span> [[ ! -d <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/python3.12/site-packages/pip&quot;</span> ]]; <span class="hljs-keyword">then</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Fix missing pip...&quot;</span><br>PYTHONDONTWRITEBYTECODE=1 LD_LIBRARY_PATH=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python312&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python312/python&quot;</span> -m pip install --no-index --root=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span> --ignore-installed pip<br><span class="hljs-built_in">rm</span> -f <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/bin/pip&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/bin/pip3&quot;</span><br><span class="hljs-keyword">fi</span><br><span class="hljs-built_in">rm</span> -f <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/libpython3.so&quot;</span><br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span>/usr/lib/python*/<br><span class="hljs-built_in">rm</span> -r <span class="hljs-built_in">test</span> &amp;&amp; <span class="hljs-built_in">rm</span> -r */*<span class="hljs-built_in">test</span>*/<br><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span><br>LD_LIBRARY_PATH=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python312&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>/python312/python&quot;</span> -m compileall -s <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>&quot;</span> -p / usr/lib/python3.12/site-packages/<br>&#125;<br></code></pre></td></tr></table></figure>        </div>      </div>    </div><div class="note note-info">            <ol> <li>只按照最简单的<code>configure</code> <code>make</code> <code>make altinstall</code>三步走的流程。</li> <li>依赖精挑细选了一些，可以调整；尤其注意，如果想使用tkinter相关功能，tk包需要保留；这种情况下可以考虑把tk直接移入depends中。直接不保留任何依赖也行，反正<a href="#%E4%BE%9D%E8%B5%96%E5%87%86%E5%A4%87">准备依赖</a>的时候已经把需要的包显式安装了。</li> <li>让<code>makepkg</code>自己拉代码会拉下来很多多余的内容；似乎没有包含<code>--single-branch</code>参数。拉下来的仓库会达到吓人的2.6GB甚至更多。因此可以在执行<code>makepkg</code>之前，手动把仓库浅克隆下来： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> https://github.com/python/cpython.git -b 3.10 --shallow-since 2025-06-03 --bare python310 <span class="hljs-comment">#python3.10</span><br>git <span class="hljs-built_in">clone</span> https://github.com/python/cpython.git -b 3.11 --shallow-since 2025-06-03 --bare python311 <span class="hljs-comment">#python3.11</span><br>git <span class="hljs-built_in">clone</span> https://github.com/python/cpython.git -b 3.12 --shallow-since 2025-06-02 --bare python312 <span class="hljs-comment">#python3.12</span><br></code></pre></td></tr></table></figure>  这里使用<code>--shallow-since</code>而非<code>--depth 1</code>，是因为脚本需要<code>git describe</code>来获取版本号，因此需要保留最近的一个git tag；使用<code>--shallow-exclude</code>的话也可以，但是要回溯到倒数第二个tag，因为<code>shallow-exclude</code>指定的提交不会被保留。例如，如果当前版本是3.12.10，就需要<code>--shallow-exclude v3.12.9</code></li> <li>没有使用<code>--with-dtrace</code>；dtrace本就不是为Linux准备的，如果强行使用的话，需要安装依赖systemtab，但是在咱这里会编译失败。</li> <li>没有使用<code>make test</code>；如果想要使用的话，在<code>build()</code>函数和<code>package()</code>函数中间增加一个<code>check()</code>函数，在这里面执行；不使用是因为<code>test</code>失败不便排查且只能重新编译，而大部分时候测试的问题都是干扰项问题。</li> <li>如果不开启<code>--enable-shared</code>，则编译成静态库，可以因此去掉<code>package()</code>中的 <figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell"><span class="hljs-built_in">rm</span> <span class="hljs-operator">-f</span> <span class="hljs-string">&quot;<span class="hljs-variable">$</span>&#123;pkgdir&#125;/usr/lib/libpython3.so&quot;</span><br></code></pre></td></tr></table></figure></li> <li>如果想要安装man手册，在<code>altinstall</code>后面再添加<code>maninstall</code>，然后打包的时候为了避免冲突执行 <figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell"><span class="hljs-built_in">rm</span> <span class="hljs-operator">-f</span> <span class="hljs-string">&quot;<span class="hljs-variable">$</span>&#123;pkgdir&#125;/usr/share/man/man1/python3.1&quot;</span><br></code></pre></td></tr></table></figure></li> <li>按照PEP668标准，可以准备一个文件，文件名为<code>EXTERNALLY-MANAGED</code>，内容为： <figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs angelscript"><span class="hljs-string">[externally-managed]</span><br></code></pre></td></tr></table></figure>  然后<code>package()</code>阶段，把它安装进去： <figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">install</span> -Dm644 <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;srcdir&#125;</span>&quot;</span>/EXTERNALLY-MANAGED -t <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;pkgdir&#125;</span>/usr/lib/python3.X/&quot;</span> <span class="hljs-comment"># X换为对应版本</span><br></code></pre></td></tr></table></figure>  但是不建议在手动编译安装的环境里面加这个，它会阻止你用pip往系统环境里面塞包。</li> </ol>           </div><h4 id="deb"><a href="#deb" class="headerlink" title=".deb"></a>.deb</h4><p>.deb包<code>dpkg-buildpackage</code>自动化编译写起来不太方便，这里选择手动编译再打包的方法：<br>首先按照上一节的手动编译方法，配置好编译参数，其中给到<code>--prefix=/usr</code>，如果想要更贴近debian的打包规范就再给到<code>--libdir=/usr/lib/x86_64-linux-gnu</code>；<br>执行完<code>./configure</code>和<code>make</code>之后不执行<code>make altinstall</code>，而是改为先找一个临时存放的目录，例如<code>/tmp/py</code>，然后执行<code>make DESTDIR=&quot;/path/to/dir&quot; altinstall</code><br>接着进入前面的<code>DESTDIR</code>目录，里面应该是只有一个<code>/usr</code>目录。在这个文件夹下再新建一个<code>DEBIAN</code>目录。<br>DEBIAN目录下新建一个control文件，填入以下内容：（以3.10为例，包名命名为python3.10-git）</p><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs nix"><span class="hljs-params">Package:</span> python3.<span class="hljs-number">1</span>0-git<br><span class="hljs-params">Version:</span> <span class="hljs-number">3.10</span>.<span class="hljs-number">18</span><span class="hljs-operator">+</span>git12.g5c19c5b <span class="hljs-comment">#来自git describe</span><br><span class="hljs-params">Section:</span> python<br><span class="hljs-params">Priority:</span> optional<br><span class="hljs-params">Architecture:</span> amd64<br><span class="hljs-params">Installed-Size:</span> <span class="hljs-number">247424</span> <span class="hljs-comment">#自行查看大小之后转写为KiB单位</span><br><span class="hljs-params">Conflicts:</span> python3.<span class="hljs-number">1</span>0-minimal,python3.<span class="hljs-number">10</span> <span class="hljs-comment">#为了避免和deadsnake ppa源的python包冲突；可以省略，反正要自己管理的</span><br><span class="hljs-params">Maintainer:</span> Silver Tuanzi<br><span class="hljs-params">Homepage:</span> https:<span class="hljs-operator">//</span>www.python.org<span class="hljs-symbol">/</span><br><span class="hljs-params">Description:</span> Git build of Python <span class="hljs-number">3.10</span><br> This is a custom git build of Python <span class="hljs-number">3.10</span> compiled from source.<br> .<br> The package provides a complete Python <span class="hljs-number">3.10</span> installation including<br> the standard library, development headers, and virtual environment<br> support.<br> .<br> Git <span class="hljs-params">describe:</span> v3.<span class="hljs-number">10.1</span>8-<span class="hljs-number">9</span><span class="hljs-operator">-</span>g5c19c5b<br> Built from git <span class="hljs-params">commit:</span> <span class="hljs-number">5</span>c19c5b (<span class="hljs-number">9</span> commits after v3.<span class="hljs-number">10.18</span>)<br></code></pre></td></tr></table></figure><p>这个control文件没有设置任何的<code>Depends</code>,<code>Provides</code>和<code>Replace</code>字段，因为必需的依赖已经显式安装；自行管理即可。<br>python包不需要<code>postinst</code>和<code>postrm</code>脚本执行操作，因此不作设置。<br><br>如果编译的时候使用了<code>--enable-shared</code>，进入<code>/usr/lib</code>目录，删除<code>libpython3.so</code>文件，避免冲突。<br><br>对于python3.10版本，进入<code>/usr/bin</code>目录，检查<code>pip3.10</code>文件的第一行是否错误地标注成了<code>#!/usr/bin/python</code>；如果是，将其手动修复为<code>#!/usr/bin/python3.10</code>。<br>当然如果编译的时候本来就带了<code>--without-ensurepip</code>就不用考虑这个问题了。<br><br>最后，回到<code>/usr</code>和<code>/DEBIAN</code>目录所在的文件夹，执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">dpkg -b . python3.10-git.deb<br></code></pre></td></tr></table></figure><p>deb文件名可以随意，稍等片刻之后打包成功，可以使用<code>dpkg -i</code>或者<code>apt install</code>安装。<br>建议使用<code>apt install ./python3.10-git.deb</code>的方式安装，如果打包不规范，可能会出现<code>apt</code>安装失败但是<code>dpkg</code>能安装成功的情况，使用<code>apt</code>有助于规避未来使用可能遇到的一些奇怪的依赖问题。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>修复Linux版本edge的侧边栏和copilot按钮</title>
    <link href="/2025/06/27/fix-edge-copilot/"/>
    <url>/2025/06/27/fix-edge-copilot/</url>
    
    <content type="html"><![CDATA[<p>Linux版本的edge浏览器右侧边栏不可用，copilot按钮可见但是点击无反应，还无法关闭。</p><hr><p>最简单的解决方法：如果有双系统，定位到windows的用户文件夹，路径为<code>C:\users\%USERNAME%\AppData\Local\Microsoft\Edge\User Data\Default</code><br>或者，<code>win+R</code>组合键，然后键入<code>%LOCALAPPDATA%</code>即可进入到上述路径的<code>Local</code>层。<br>将其中的<code>HubApps</code>文件复制出来。<br>接着，进入Linux的<code>$HOME</code>目录，<code>~/.config/microsoft-edge/Default</code>路径下，把刚才的<code>HubApps</code>文件复制进去。<br>如果edge是Flatpak版本的，那这个路径<em>应该</em>是<code>~/.var/app/com.microsoft.Edge/config/microsoft-edge/Default/</code><br>然后启动edge，此时copilot按钮已经可用了，然后设置中也可以关闭copilot按钮，且正常使用边栏了。</p><hr><p>以下方法来自论坛，未经测试，对于没有Windows双系统可以尝试的方案：<br>将edge回退到旧版本（127及更早），这里提供下载链接：<a href="https://packages.microsoft.com/repos/edge/pool/main/m/microsoft-edge-stable/">deb</a>&#x2F;<a href="https://packages.microsoft.com/yumrepos/edge/Packages/m/">rpm</a><br>启动edge，测试问题是否解决，如果解决了，就可以正常升级回最新版。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>修复DNS污染导致无法通过ssh连接github的问题</title>
    <link href="/2025/04/20/fix-ssh-github/"/>
    <url>/2025/04/20/fix-ssh-github/</url>
    
    <content type="html"><![CDATA[<p>某天因为在手机上改过了blog的内容，然后就想在电脑上pull过来：<br><img src="/img/fix-ssh-github/pull-refused.png"><br>看到refused的时候咱整个人是懵的，于是测试了一下连接：<br><img src="/img/fix-ssh-github/test-refused.png"><br>起初咱还以为是ssh key炸了，但是连接gitee和huggingface都是没有问题的：<br><img src="/img/fix-ssh-github/gitee-hf-fine.png"><br>删掉github上面的公钥重新添加当然也没任何效果。</p><p>看一下连接日志，发现问题了：<br><img src="/img/fix-ssh-github/ssh-v.png"><br>被DNS搞了，搞去127.0.0.1了；那解决方案就显而易见了：换个公共DNS，或者手动改hosts。</p><div class="note note-secondary">            <p>公共DNS网上搜一下随便挑一个就行，像什么114还有阿里什么的都可以考虑。<br>但是咱担心改全局DNS可能影响访问其它网站的速度和延迟，所以还是单独改hosts吧。</p>           </div><p>首先获取到目标网站的ip地址：这里推荐使用<a href="https://dnschecker.org/">https://dnschecker.org/</a>, 可以看到服务器的位置，便于挑选地理位置最近的服务器</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/fix-ssh-github/dns-github.png"></div><div class="group-image-wrap"><img src="/img/fix-ssh-github/dns-ssh-github.png"></div></div></div><p>然后接下来就是编辑hosts文件了，Linux直接用root权限编辑<code>/etc/hosts</code>就行，Windows如果是火绒用户，火绒提供了快捷入口，否则需要先用管理员权限打开任意编辑器，然后打开<code>C:\Windows\System32\drivers\etc\hosts</code>文件编辑；安卓手机没有root权限的话比较麻烦，下文再提。<br><img src="/img/fix-ssh-github/huorong.png"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs hosts">20.205.243.166github.com<br>20.200.245.248ssh.github.com<br></code></pre></td></tr></table></figure><p>编辑完成后再测试一下连接，已经可以正常通了：<br><img src="/img/fix-ssh-github/pass.png"></p><div class="note note-info">            <p>碎碎念：用ssh连接clone大项目的时候网速慢的出奇，所以还是挂代理然后用https吧，ssh主要是push的时候认证比较方便。</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-d3182882" role="button" aria-expanded="false" aria-controls="collapse-d3182882">        <div class="fold-arrow">▶</div>没root的Android手机修改hosts方案      </div>      <div class="fold-collapse collapse" id="collapse-d3182882">        <div class="fold-content">          <p>下载软件<a href="https://github.com/x-falcon/Virtual-Hosts">Virtual Hosts</a>, <a href="https://f-droid.org/packages/com.github.xfalcon.vhosts">F-droid</a> 上有更新的版本。软件开源但是作者在Google Play上收费340日元。<br>然后可以直接使用远端hosts和DNS(如果连接不上谷歌的8.8.8.8可以换成别的)，也可以在任意目录(建议Documents文件夹)创建一个文本文件填入hosts内容导入进去，之后启动即可。</p> <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/fix-ssh-github/virtual-host1.jpg"></div><div class="group-image-wrap"><img src="/img/fix-ssh-github/virtual-host2.jpg"></div></div></div> 然后在termux里面测试连接： <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/fix-ssh-github/termux-test.jpg"></div></div></div>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-13b8a4a1" role="button" aria-expanded="false" aria-controls="collapse-13b8a4a1">        <div class="fold-arrow">▶</div>受影响的地区      </div>      <div class="fold-collapse collapse" id="collapse-13b8a4a1">        <div class="fold-content">          <p><a href="https://www.itdog.cn/ping/">这里</a>可以查询实时的情况。<br>截至2025年4月20日，<br>github.com受影响的区域：广东移动，广东电信，云南移动；新疆传统墙中墙解析出来的ip莫名其妙的不知道能不能一样用（貌似是随机污染，有时候也能正常解析到microsoft服务器）：</p> <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/fix-ssh-github/ping-github.png"></div><div class="group-image-wrap"><img src="/img/fix-ssh-github/ping-github-xj.png"></div></div></div> ssh.github.com受影响的区域：广东移动，广东电信；新疆传统墙中墙解析出来的ip莫名其妙的不知道能不能一样用（貌似是随机污染，有时候也能正常解析到microsoft服务器）： <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/fix-ssh-github/ping-ssh-github.png"></div><div class="group-image-wrap"><img src="/img/fix-ssh-github/ping-ssh-github-xj.png"></div></div></div>        </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-8b4d5093" role="button" aria-expanded="false" aria-controls="collapse-8b4d5093">        <div class="fold-arrow">▶</div>顺便记录一下配置ssh的过程吧      </div>      <div class="fold-collapse collapse" id="collapse-8b4d5093">        <div class="fold-content">          <ol> <li>生成ssh密钥，Windows现在已经内置了openssh所以不需要专门配置ssh客户端： <figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs excel">ssh-keygen -<span class="hljs-built_in">t</span> <span class="hljs-symbol">ed25519</span> -C <span class="hljs-string">&quot;youremail@example.com&quot;</span><br></code></pre></td></tr></table></figure>  参数解释：<br> <code>-t ed25519</code>: 指定密钥使用的加密类型。如果不加指定，默认使用ed25519，该算法性能最强的同时安全性也最好，同时占用空间也较小。虽然理论上可能对旧系统兼容性不好，但是即使是Ubuntu20.04预装的openssh8.2中，ed25519也可以正常使用（虽然该版本默认使用的是rsa）。有其它的算法可选：<code>dsa | ecdsa | ecdsa-sk | ed25519 | ed25519-sk | rsa</code><br> <code>-C &quot;youremail@example.com&quot;</code>：仅作为注释，但是换成自己的邮箱会有标志作用。</li> <li>接下来会询问两个选项，第一个是路径，直接回车保持默认就好，但是把路径记录下来后面会用到；<br> 第二个是询问密码，直接回车不设置密码，否则之后使用ssh操作时会额外询问这个密码（应该不会有人需要这么变态的安全性吧）</li> <li>去到上一步记录下来的路径，通常会是用户文件夹下面的<code>.ssh</code>文件夹，里面会有一对密钥，<code>id_ed25519</code>是私钥，<code>id_ed25519.pub</code>是公钥。<br> 妥善保存好私钥不要泄漏；<br> 打开公钥把整个文件的内容复制下来。</li> <li>前往github的个人设置，在左侧找到<code>SSH and GPG keys</code>，点击<code>New SSH key</code>，在<code>Title</code>选项随意填，<code>Key</code>选项填上刚刚复制的 SSH 公钥，再点<code>Add SSH key</code>保存。</li> <li>在本地测试连接： <figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">ssh</span> -T git<span class="hljs-variable">@github</span>.com<br>ssh -T git<span class="hljs-variable">@ssh</span>.github.com<br></code></pre></td></tr></table></figure></li> </ol>         </div>      </div>    </div>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Arch Linux配置休眠</title>
    <link href="/2025/04/06/arch-hibernate/"/>
    <url>/2025/04/06/arch-hibernate/</url>
    
    <content type="html"><![CDATA[<h1 id="写在前面：名词解释"><a href="#写在前面：名词解释" class="headerlink" title="写在前面：名词解释"></a>写在前面：名词解释</h1><h2 id="休眠与睡眠"><a href="#休眠与睡眠" class="headerlink" title="休眠与睡眠"></a>休眠与睡眠</h2><p>睡眠（挂起到内存，suspend to RAM），将当前运行的程序状态保存到内存，然后保留内存供电；<br>休眠（挂起到硬盘，suspend to disk），将当前运行的程序状态保存到硬盘，然后完全断电，这意味着可以拔掉台式机的插座，搬走之后插电再恢复。<br>在Linux中，休眠会使用swap，无论是swap分区还是swap文件都可以。</p><div class="note note-success">            <p>由于是对系统层面的修改，下文的所有操作如无特殊说明，均需要在root权限下执行。</p>           </div><h2 id="重新生成initramfs"><a href="#重新生成initramfs" class="headerlink" title="重新生成initramfs"></a>重新生成initramfs</h2><p>是下文将会使用的操作。当提到“重新生成initramfs”时，需要执行下面两种操作的一种：</p><ol><li>对所有安装的内核重新生成initramfs： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">mkinitcpio -P<br></code></pre></td></tr></table></figure></li><li>对指定的一个内核单独重新生成initramfs，以<code>linux</code>为例 <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">mkinitcpio -p linux<br></code></pre></td></tr></table></figure> 可以只对当前正在使用的内核执行，因为内核在更新的时候也会自动重新生成initramfs，不使用的内核等到滚新版本的时候让它自动来就行了，节省时间。</li></ol><h2 id="更新grub配置"><a href="#更新grub配置" class="headerlink" title="更新grub配置"></a>更新grub配置</h2><p>如果配置过 <a href="https://aur.archlinux.org/packages/update-grub">update-grub</a> 脚本，执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">update-grub<br></code></pre></td></tr></table></figure><p>否则执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">grub-mkconfig -o /boot/grub/grub.cfg<br></code></pre></td></tr></table></figure><h1 id="Windows-Arch双系统休眠需要注意的地方"><a href="#Windows-Arch双系统休眠需要注意的地方" class="headerlink" title="Windows+Arch双系统休眠需要注意的地方"></a>Windows+Arch双系统休眠需要注意的地方</h1><div class="note note-info">            <p>本节内容默认关闭了Windows快速启动。</p>           </div><ol><li>EFI分区：如果双系统共用同一个EFI分区，当休眠其中一个系统并启动另一个系统时，根据 <a href="https://wiki.archlinux.org/title/Dual_boot_with_Windows#Fast_Startup_and_hibernation">ArchWiki</a> ，EFI分区<em>可能</em>遭到破坏。通常来说并不会出现问题（咱这么干过并且没事），但是这么做的时候务必确保手头有现成的WindowsPE盘以及Linux的liveCD（可以通过 <a href="https://www.ventoy.net/cn/">ventoy</a> 把两个功能整合在同一个U盘里），以在EFI分区万一真的被破坏的时候，可以从容地修复引导。</li><li>文件系统：当休眠其中一个系统并启动另一个系统时，如果挂载了在另一个系统中处于挂载状态的分区，那么<em>可能</em>发生数据损失；并且如果对这个分区进行了写入操作，文件系统<strong>几乎一定</strong>会损坏。由于Windows通常并不能读取Linux的分区，这种情况最可能出现在“休眠了Windows之后启动Linux，并挂载了Windows分区”的时候，这时尽快结束当前操作，重启回到Windows，并对这个分区执行<code>chkdsk</code>检查与修复。</li></ol><h1 id="如果不需要休眠"><a href="#如果不需要休眠" class="headerlink" title="如果不需要休眠"></a>如果不需要休眠</h1><p>在以下两种方案中选择一种，以禁用休眠功能并在桌面环境中隐藏休眠按钮，避免误触引发意料之外的后果。参考 <a href="https://wiki.archlinux.org/title/KDE#Disable_hibernate">ArchWiki</a> ，虽然是KDE的wiki页但是对其它桌面环境同样有效。</p><ol><li>创建文件<code>/etc/polkit-1/rules.d/99-disable-hibernate.rules</code>，填入以下内容： <figure class="highlight wren"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs wren"><span class="hljs-comment">// Disable hibernate for all users</span><br><span class="hljs-variable">polkit</span>.<span class="hljs-property">addRule</span>(<span class="hljs-title function_">function</span>(<span class="hljs-params">action</span>, <span class="hljs-params">subject</span>) &#123;<br><span class="hljs-keyword">if</span> ((<span class="hljs-variable">action</span>.<span class="hljs-property">id</span> <span class="hljs-operator">==</span> <span class="hljs-string">&quot;org.freedesktop.login1.hibernate&quot;</span>)) &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-variable">polkit</span>.<span class="hljs-property">Result</span>.<span class="hljs-property">NO</span>;<br>&#125;<br>&#125;);<br><span class="hljs-variable">polkit</span>.<span class="hljs-property">addRule</span>(<span class="hljs-title function_">function</span>(<span class="hljs-params">action</span>, <span class="hljs-params">subject</span>) &#123;<br><span class="hljs-keyword">if</span> ((<span class="hljs-variable">action</span>.<span class="hljs-property">id</span> <span class="hljs-operator">==</span> <span class="hljs-string">&quot;org.freedesktop.login1.hibernate-multiple-sessions&quot;</span>)) &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-variable">polkit</span>.<span class="hljs-property">Result</span>.<span class="hljs-property">NO</span>;<br>&#125;<br>&#125;);<br></code></pre></td></tr></table></figure> <img src="/img/arch-hibernate/disable1.png"></li><li>创建文件<code>/etc/systemd/sleep.conf.d/00-disable-hibernation.conf</code>，填入以下内容： <div class="note note-info">            <p>只需要保证该文件位于<code>/etc/systemd/sleep.conf.d/</code>路径下即可，文件名并不重要。</p>           </div> <figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-section">[Sleep]</span><br><span class="hljs-attr">AllowHibernation</span>=<span class="hljs-literal">no</span><br><span class="hljs-attr">AllowSuspendThenHibernate</span>=<span class="hljs-literal">no</span><br><span class="hljs-attr">AllowHybridSleep</span>=<span class="hljs-literal">no</span><br></code></pre></td></tr></table></figure> <img src="/img/arch-hibernate/disable2.png"></li></ol><h1 id="配置休眠"><a href="#配置休眠" class="headerlink" title="配置休眠"></a>配置休眠</h1><p>详细资料参见 <a href="https://wiki.archlinux.org/title/Power_management/Suspend_and_hibernate#Hibernation">ArchWiki</a> 。<br>需要确保至少有一个足够大的swap分区或者swap文件。Linux不支持跨swap休眠，休眠数据必须存储在单一的swap分区或者swap文件上。具体的大小根据内存的大小判断。</p><div class="note note-info">            <p>测试平台：Oracle Virtualbox虚拟机，VMware虚拟机不支持休眠。测试涵盖了swap分区，ext4的swap文件和btrfs的swap文件三种形式，均可以休眠并成功恢复。<br>2025年8月在物理机上测试正常工作。</p>           </div><h2 id="配置initramfs"><a href="#配置initramfs" class="headerlink" title="配置initramfs"></a>配置initramfs</h2><p>Arch Linux使用<code>mkinitcpio</code>来管理initramfs。<code>mkinitcpio</code>支持两种initramfs，一种基于busybox（这是默认值），另一种基于systemd。</p><div class="note note-info">            <p>2025年11月5日，mkinitcpio发布v40版本。在该版本中，基于systemd的initramfs变成了默认值。<br>因此，对于此后的版本，以下内容完全不需要做任何配置，只要具有足够大小的单块swap分区或者单个swap分区，休眠应该是开箱即用的。</p>           </div><div class="note note-info">            <p>基于systemd的似乎更现代，支持并行启动，开机更快。但是有一些功能在基于systemd的initramfs上面似乎并没有平替。<br>例如systemd似乎无法使用救急shell，并且似乎无法在initramfs阶段执行fsck检查文件系统（都是根据wiki的推测，wiki这一块讲的感觉很含糊）<br>相关的说明可以参考<a href="https://wiki.archlinux.org/title/Mkinitcpio#Common_hooks">ArchWiki-mkinitcpio</a> ，属于进阶阅读内容。</p>           </div><p>需要编辑<code>/etc/mkinitcpio.conf</code>，找到这一行：</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs abnf"><span class="hljs-attribute">HOOKS</span><span class="hljs-operator">=</span>(base udev autodetect microcode modconf kms keyboard keymap consolefont block filesystems fsck)<br></code></pre></td></tr></table></figure><ol><li>继续使用基于busybox的initramfs，则需要加入<code>resume</code>钩子，并且确保<code>resume</code>在<code>udev</code>之后： <figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs abnf"><span class="hljs-attribute">HOOKS</span><span class="hljs-operator">=</span>(base udev autodetect microcode modconf kms keyboard keymap consolefont block filesystems resume fsck)<br></code></pre></td></tr></table></figure> <img src="/img/arch-hibernate/busybox.png"></li><li>改为使用基于systemd的initramfs，则systemd自带休眠支持： <figure class="highlight nsis"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nsis">HOOKS=(base <span class="hljs-params">system</span>d autodetect microcode modconf kms keyboard sd-vconsole block <span class="hljs-keyword">file</span><span class="hljs-params">system</span>s fsck)<br></code></pre></td></tr></table></figure> <img src="/img/arch-hibernate/systemd1.png"><br> 或者，考虑去掉可能无用的<code>base</code>和<code>fsck</code>(例如，btrfs不需要；并且systemd另外有一套检查体系) <figure class="highlight nsis"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nsis">HOOKS=(<span class="hljs-params">system</span>d autodetect microcode modconf kms keyboard sd-vconsole block <span class="hljs-keyword">file</span><span class="hljs-params">system</span>s)<br></code></pre></td></tr></table></figure> <img src="/img/arch-hibernate/systemd2.png"></li></ol><p>完成编辑后，<a href="#%E9%87%8D%E6%96%B0%E7%94%9F%E6%88%90initramfs">重新生成initramfs</a>。</p><h2 id="指定休眠的位置"><a href="#指定休眠的位置" class="headerlink" title="指定休眠的位置"></a>指定休眠的位置</h2><blockquote><p>从systemd v255和mkinitcpio v38开始，当系统运行在UEFI上时，<code>systemd-sleep</code>将自动选择合适的交换空间进行休眠，并且所用交换空间的信息存储在<code>HibernateLocation</code>EFI变量中。在下次启动时，<code>systemd-hibernate-resume</code>从EFI变量读取位置，系统恢复。这意味着除非系统使用传统BIOS或您想选择与自动选择的不同的交换空间，否则以下步骤不是必需的。</p></blockquote><h3 id="休眠到swap分区"><a href="#休眠到swap分区" class="headerlink" title="休眠到swap分区"></a>休眠到swap分区</h3><p>休眠到swap分区的操作相对比较简单，首先获取swap分区的UUID：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">lsblk -o name,mountpoints,size,uuid<br></code></pre></td></tr></table></figure><p><img src="/img/arch-hibernate/swap-part-uuid.png"><br>图中UUID是<code>851b93b3-ab5a-401c-88ad-5487cd4ad270</code><br>接下来需要添加内核参数，对于使用grub引导的系统，编辑<code>/etc/default/grub</code>文件，找到<code>GRUB_CMDLINE_LINUX_DEFAULT</code>一行，在其中添加<code>resume=UUID= </code><br><img src="/img/arch-hibernate/swap-part-para.png"><br>最后<a href="#%E6%9B%B4%E6%96%B0grub%E9%85%8D%E7%BD%AE">更新grub配置</a>；完成后需要重启才能使更改生效。</p><h3 id="休眠到swap文件"><a href="#休眠到swap文件" class="headerlink" title="休眠到swap文件"></a>休眠到swap文件</h3><h4 id="1-swap文件位于btrfs分区"><a href="#1-swap文件位于btrfs分区" class="headerlink" title="1.swap文件位于btrfs分区"></a>1.swap文件位于btrfs分区</h4><p>首先获取swap文件所在分区的UUID：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">lsblk -o name,mountpoints,size,uuid<br></code></pre></td></tr></table></figure><p><img src="/img/arch-hibernate/swap-btrfs-uuid.png"><br>swap文件是<code>/swap/swapfile</code>，位于<code>/dev/sda2</code>分区，UUID是<code>5df5e112-f1ec-4639-9871-c3bdd2da41ac</code><br>然后需要获取swap文件相对于分区的偏移量。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">btrfs inspect-internal map-swapfile -r path/to/swapfile<br></code></pre></td></tr></table></figure><p><img src="/img/arch-hibernate/swap-btrfs-offset.png"><br>图中偏移量是<code>1451264</code></p><p>接下来需要添加内核参数，对于使用grub引导的系统，编辑<code>/etc/default/grub</code>文件，找到<code>GRUB_CMDLINE_LINUX_DEFAULT</code>一行，在其中添加<code>resume=UUID= resume_offset= </code><br><img src="/img/arch-hibernate/swap-btrfs-para.png"><br>最后<a href="#%E6%9B%B4%E6%96%B0grub%E9%85%8D%E7%BD%AE">更新grub配置</a>；完成后需要重启才能使更改生效。</p><h4 id="2-swap文件位于其它文件系统分区"><a href="#2-swap文件位于其它文件系统分区" class="headerlink" title="2.swap文件位于其它文件系统分区"></a>2.swap文件位于其它文件系统分区</h4><div class="note note-info">            <p>其它文件系统(ext4,xfs等等)均适用此方法。</p>           </div><p>首先获取swap文件所在分区的UUID：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">lsblk -o name,mountpoints,size,uuid<br></code></pre></td></tr></table></figure><p><img src="/img/arch-hibernate/swap-ext-uuid.png"><br>swap文件是<code>/swapfile</code>，位于<code>/dev/sda2</code>分区，UUID是<code>ccbc7703-d83e-4846-b70b-61eec5b3f9ec</code><br>然后需要获取swap文件相对于分区的偏移量。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">filefrag -v path/to/swapfile<br></code></pre></td></tr></table></figure><p><img src="/img/arch-hibernate/swap-ext-offset1.png"><br>图中需要的数值为第一行<code>physical_offset</code>列，偏移量是<code>352256</code></p><p>或者可以这样直接获取偏移值：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">filefrag -v path/to/swapfile | awk <span class="hljs-string">&#x27;$1==&quot;0:&quot; &#123;print substr($4, 1, length($4)-2)&#125;&#x27;</span><br></code></pre></td></tr></table></figure><p><img src="/img/arch-hibernate/swap-ext-offset2.png"><br>图中偏移量是<code>352256</code><br><br>接下来需要添加内核参数，对于使用grub引导的系统，编辑<code>/etc/default/grub</code>文件，找到<code>GRUB_CMDLINE_LINUX_DEFAULT</code>一行，在其中添加<code>resume=UUID= resume_offset= </code><br><img src="/img/arch-hibernate/swap-ext-para.png"><br>最后<a href="#%E6%9B%B4%E6%96%B0grub%E9%85%8D%E7%BD%AE">更新grub配置</a>；完成后需要重启才能使更改生效。</p><h2 id="测试休眠"><a href="#测试休眠" class="headerlink" title="测试休眠"></a>测试休眠</h2><p>任意启动几个软件，然后使用桌面环境自带的休眠，或者使用</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl hibernate<br></code></pre></td></tr></table></figure><p>电源完全关闭后，重新开机并进入Arch Linux，确认应用是否成功恢复。<br>成功断电并且成功恢复说明配置成功。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Arch Linux配置cinnamon桌面与常用应用</title>
    <link href="/2025/02/11/arch-cinnamon/"/>
    <url>/2025/02/11/arch-cinnamon/</url>
    
    <content type="html"><![CDATA[<p>注：官方源的软件包用角标<sup>pkg</sup>标注；archlinuxcn社区源的软件包用角标<sup>cn</sup>标注；arch4edu源的用角标<sup>edu</sup>标注；来自AUR的软件包用角标<sup>aur</sup>标注，并且附上AUR地址的链接，便于查看源码。<br>涉及到ArchWiki的只贴英文链接啦，反正英文界面可以很简单地跳转到中文版的<br><img src="/img/arch-cinnamon/cnwiki.png"></p><p>配置的最终效果应该类似这样：</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/archcinnamon1.png"></div></div><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/archcinnamon2.png"></div></div></div><p>配置的起点是一个只执行了最基本安装的，只有root用户的最基本Arch Linux系统，主要的配置类似<a href="/2025/02/10/windows-with-arch/" title="Windows+Arch Linux双系统基本安装">上一篇文章</a>中的安装过程。<br><br>下面正文开始。</p><h1 id="安装桌面环境之前"><a href="#安装桌面环境之前" class="headerlink" title="安装桌面环境之前"></a>安装桌面环境之前</h1><p>这部分配置应该是无论安装什么桌面环境都需要做的。</p><h2 id="0-确保系统为最新"><a href="#0-确保系统为最新" class="headerlink" title="0.确保系统为最新"></a>0.确保系统为最新</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -Syu<br></code></pre></td></tr></table></figure><p>滚动更新的系统最新的才是最稳定的。</p><h2 id="1-配置普通用户"><a href="#1-配置普通用户" class="headerlink" title="1.配置普通用户"></a>1.配置普通用户</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">useradd -m -G wheel -s /bin/bash myusername <span class="hljs-comment">#添加一个名为myusername的用户，指定登录shell为bash，并添加到wheel用户组</span><br>passwd myusername <span class="hljs-comment">#为新用户设置密码</span><br></code></pre></td></tr></table></figure><div class="note note-info">            <p>Arch Linux似乎约定俗成的把<code>wheel</code>用户组作为特权组给予<code>sudo</code>权限；如果想像其它发行版一样使用<code>sudo</code>用户组当然也是可以的。</p>           </div><p>然后还需要配置<code>sudo</code>权限：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">EDITOR=vim visudo<br></code></pre></td></tr></table></figure><div class="note note-warning">            <p>使用<code>EDITOR</code>环境变量指定编辑器是不可省略的；<code>visudo</code>命令默认使用<code>vi</code>编辑器，但是咱没安装，因此如果不指定编辑器就会执行失败。编辑器可以换为<code>nano</code>等等，前提是安装了它。</p>           </div><p>定位到图示的行（在文件的最后），然后取消注释。</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/visudo1.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/visudo2.png"></div></div></div><p>这三行分别表示：</p><ol><li>给<code>wheel</code>用户组赋予<code>sudo</code>权限</li><li>给<code>wheel</code>用户组赋予<code>sudo</code>权限，并且无需输入密码</li><li>给<code>sudo</code>用户组赋予<code>sudo</code>权限</li></ol><p>挑选想要的其中一行取消注释即可。<br>另外，还可以在文件中添加这样一行：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">Defaults pwfeedback</span><br></code></pre></td></tr></table></figure><p>添加后<code>sudo</code>输入密码时会以星号(<code>*</code>)回显，而非像通常那样什么都不显示。</p><p>之后可以<code>exit</code>退出root用户，然后重新登录到非特权用户；也可以使用<code>su -l myusername</code>直接切换用户。</p><h2 id="2-安装AUR助手"><a href="#2-安装AUR助手" class="headerlink" title="2.安装AUR助手"></a>2.安装AUR助手</h2><p>通常最常见的选择是<a href="https://aur.archlinux.org/packages/yay">yay</a><sup>cn&#x2F;aur</sup>或者<a href="https://aur.archlinux.org/packages/paru">paru</a><sup>cn&#x2F;aur</sup>，archlinuxcn社区源已经打好了包，直接安装即可：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S paru<br></code></pre></td></tr></table></figure><div class="note note-info">            <ol> <li>个人更喜欢<code>paru</code>；但是从交互体验来看<code>yay</code>的反馈可能会更好些；</li> <li>Arch Linux更希望用户能手动从AUR安装一遍，熟悉手动打包的过程： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> https://aur.archlinux.org/paru.git --depth 1<br><span class="hljs-built_in">cd</span> paru<br>makepkg -si<br></code></pre></td></tr></table></figure></li> <li>出于安全原因，<code>makepkg</code>命令和AUR助手均会拒绝直接在root用户下运行，所以记得切换用户。</li> </ol>           </div>    <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-b2662aae" role="button" aria-expanded="false" aria-controls="collapse-b2662aae">        <div class="fold-arrow">▶</div>留意不同AUR包后缀的区别      </div>      <div class="fold-collapse collapse" id="collapse-b2662aae">        <div class="fold-content">          <p>通常来说，一个开源软件包<code>foo</code>会在AUR上存在至少三种分支：</p> <ul> <li><code>foo</code>:使用发布版本的源码包编译安装（源码包从release里面下载）</li> <li><code>foo-bin</code>:直接打包上游编译好的二进制包</li> <li><code>foo-git</code>:从git拉取最新的源码编译安装</li> </ul> <p>其它后缀的阅读包描述可以看出。通常情况下，<code>-bin</code>因为是现成的二进制文件，只需要解压然后重打包，安装速度最快；源码编译的则会兼容性更好，并且如果动态链接了系统依赖，可能一定程度节省硬盘空间（比如相比于一堆包一人打包一个electron，全部共用系统的electron库明显更节省空间）。<br>另外，<code>yay</code>是go语言编写的，国内网络环境可能会拉取依赖困难。<code>yay-bin</code>就能避免这一问题。</p>         </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-a9b2ede2" role="button" aria-expanded="false" aria-controls="collapse-a9b2ede2">        <div class="fold-arrow">▶</div>另一个可能有点邪门和灵车的选择:pamac      </div>      <div class="fold-collapse collapse" id="collapse-a9b2ede2">        <div class="fold-content">          <p>pamac是Manjaro团队开发的另一套辅助性的包管理软件，在Manjaro系统中默认预装，ArchLinux也可以从AUR中获取到；<br>pamac提供了另一套不同于pacman的命令，命令的整体特征更类似于<code>apt</code>等常见的包管理器（例如<code>pamac install foo</code>等等），命令的用法参照<a href="https://wiki.manjaro.org/index.php/Pamac">Manjaro Wiki - pamac</a> （此wiki的汉化约等于没有）<br>此外pamac也提供了图形化操作界面，预览同样可以在前述的wiki界面找到。<br>下面列出在AUR中pamac不同包的区别：<br><a href="https://aur.archlinux.org/packages/pamac-cli">pamac-cli</a> –只提供命令行；<br><a href="https://aur.archlinux.org/packages/pamac-aur">pamac-aur</a> –提供图形界面，包含基本的软件包和AUR包支持；<br><a href="https://aur.archlinux.org/packages/pamac-flatpak">pamac-flatpak</a> –提供图形界面，在上一个的基础上增加了flatpak支持（需要自己配置安装flatpak<sup>pkg</sup>）；<br><a href="https://aur.archlinux.org/packages/pamac-all">pamac-all</a> –提供图形界面，在上一个的基础上增加了snap支持（需要自己配置安装<a href="https://aur.archlinux.org/packages/snapd">snapd</a><sup>aur</sup>）；</p>         </div>      </div>    </div><h2 id="3-关于显卡驱动"><a href="#3-关于显卡驱动" class="headerlink" title="3.关于显卡驱动"></a>3.关于显卡驱动</h2><p>Linux内核已经集成了至少能亮机的modesetting驱动，所以驱动可以先不装；配置好能正常点亮的桌面，然后把快照等措施准备好了，再来大战驱动也不迟。<br>另外由于咱目前还只是在虚拟机做的测试，就不写驱动的配置啦，三家显卡分别参考各自的wiki就好了（包括核显）：<br><a href="https://wiki.archlinux.org/title/Intel_graphics">Intel</a> 和 <a href="https://wiki.archlinux.org/title/NVIDIA">NVIDIA</a><br>AMD: 先参考 <a href="https://wiki.archlinux.org/title/Xorg#AMD">这里</a> 根据显卡架构确定安装的驱动类型，然后参考 <a href="https://wiki.archlinux.org/title/AMDGPU">AMDGPU</a> 和 <a href="https://wiki.archlinux.org/title/ATI">ATI</a> ，<span class="label label-info">由于RX400系已经属于GCN4架构了，现在能见到的常见A卡只要比RX400系新，就装AMDGPU就行</span></p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-c305dbf4" role="button" aria-expanded="false" aria-controls="collapse-c305dbf4">        <div class="fold-arrow">▶</div>VMware虚拟机需要做的事      </div>      <div class="fold-collapse collapse" id="collapse-c305dbf4">        <div class="fold-content">          <p>安装开源的open-vm-tools以及必要的驱动，然后启动相应的服务：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S open-vm-tools xf86-input-vmmouse xf86-video-vmware mesa<br>systemctl <span class="hljs-built_in">enable</span> --now vmtoolsd.service<br>systemctl <span class="hljs-built_in">enable</span> --now vmware-vmblock-fuse.service<br></code></pre></td></tr></table></figure> <blockquote> <p>如果主机和客户机间复制粘贴功能无法正常工作，尝试安装gtkmm3<sup>pkg</sup>。</p> </blockquote>         </div>      </div>    </div><h2 id="3-5-大战NVIDIA驱动"><a href="#3-5-大战NVIDIA驱动" class="headerlink" title="3.5 大战NVIDIA驱动"></a>3.5 大战NVIDIA驱动</h2><p>在N卡物理机上安装时（配置为RTX2080Ti），由于当前版本的mesa&#x2F;nouveau驱动链有一点问题（无论安装<code>xf86-video-nouveau</code>还是使用默认的<code>modesetting</code>驱动都一样），xorg会启动失败，需要稍微经过一点配置才能亮机；但是反正N卡最后要换到nvidia驱动的，因此这里并不值得单独为了nouveau折腾一通，直接上得了。</p><ol><li><p>先确认自己的显卡架构；咱整理了一份简单的表：</p><table><thead><tr><th>架构名</th><th>代表型号(只收录常见的)</th></tr></thead><tbody><tr><td>Maxwell</td><td>GTX745, 750, 750 Ti; 8系9系全系; GTX Titan X</td></tr><tr><td>Pascal</td><td>10系; Titan X和Titan Xp</td></tr><tr><td>Volta</td><td>Titan V和V100</td></tr><tr><td>Turing</td><td>16系和20系; T4</td></tr><tr><td>Ampere</td><td>30系; A100</td></tr><tr><td>Ada Lovelace</td><td>40系; L40</td></tr><tr><td>Hopper</td><td>H100</td></tr><tr><td>Grace Hopper</td><td>GH200</td></tr><tr><td>Blackwell</td><td>50系; B200</td></tr></tbody></table><p> 比Maxwell更老的架构已经不受NVIDIA支持；建议根据ArchWiki自行选择，是安装不受nv支持但是可能图形性能更好的旧驱动，还是使用xorg社区支持的nouveau。</p><blockquote><p>Nvidia不再积极为这些显卡提供驱动。这意味着这些驱动不正式支持现在的Xorg版本。因此使用 Nouveau 驱动可能会更方便，因为它依然为这些老显卡提供对当前Xorg的支持。但是，Nvidia 提供的 legacy 驱动依旧可用，并且或许拥有更好的 3D 性能（稳定性）。</p></blockquote></li><li><p>根据显卡架构和系统内核，选择合适的驱动分支：</p><ul><li>从Maxwell到Volta之间的GPU受nvidia旧版闭源驱动分支支持：从AUR安装<a href="https://aur.archlinux.org/packages/nvidia-580xx-dkms/">nvidia-580xx-dkms</a></li><li>从Turing到最新的所有GPU受nvidia-open开源驱动分支支持；在nvidia闭源驱动分支放弃支持Volta以下旧显卡之后，arch社区就不再官方维护nvidia闭源分支，完全迁移到nvidia-open<br>  linux<sup>pkg</sup>内核：使用nvidia-open<sup>pkg</sup><br>  linux-lts<sup>pkg</sup>内核：使用nvidia-open-lts<sup>pkg</sup><br>  其它内核：使用nvidia-open-dkms<sup>pkg</sup></li></ul> <div class="note note-info">            <p>dkms驱动会在每次更新内核或者驱动的时候，动态地把驱动编译成相应的内核模块；务必确保安装了内核相应的<code>headers</code>头文件。<br>在咱的e3 1230v2物理机上，对每个内核的dkms编译，需要cpu满载大约4~5分钟；如果不用dkms可以省出一些时间<del>和电费</del>。cpu性能好的话，cpu火力全开应该会很快，所以根据实际需求了。</p>           </div></li><li><p>根据选择的驱动分支安装驱动包以及必须的其它支持：nvidia-utils<sup>pkg</sup>提供了大部分图形功能的必要组件，包括OpenGL和Vulkan的支持；lib32-nvidia-utils<sup>pkg</sup>用于支持32位应用（例如steam<sup>pkg</sup>）</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S nvidia-open-dkms nvidia-utils lib32-nvidia-utils<br></code></pre></td></tr></table></figure><blockquote><p>nvidia-utils软件包包含一个文件，其将会在重启后屏蔽 nouveau 内核模块。</p></blockquote>     <div class="fold">      <div class="fold-title fold-warning collapsed" data-toggle="collapse" href="#collapse-71d6cbeb" role="button" aria-expanded="false" aria-controls="collapse-71d6cbeb">        <div class="fold-arrow">▶</div>使用预编译的非dkms驱动的注意事项      </div>      <div class="fold-collapse collapse" id="collapse-71d6cbeb">        <div class="fold-content">          <p>为了避免冲突，不要与dkms驱动同时安装；<br>滚动更新系统之前留意下更新的软件列表，如果内核更了驱动没更，就先别y，等驱动推新版本了再一块更。<br>预编译驱动安装完成后不会自动执行<code>mkinitcpio</code>，因此记得手动执行一下，<del>或者，参照<a href="https://wiki.archlinux.org/title/NVIDIA#pacman_hook">wiki</a>添加pacman钩子，以在升级驱动的时候自动运行mkinitcpio：</del>更新：虽然驱动本身并不会执行钩子，但是驱动本体更新的时候，通常伴随着nvidia-utils包的更新，而这个包是带有<code>mkinitcpio -P</code>钩子的，因此或许这两个pacman钩子完全没有必要？<br><code>/etc/pacman.d/hooks/nvidia-lts.hook</code></p> <figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs routeros">[Trigger]<br><span class="hljs-attribute">Operation</span>=Install<br><span class="hljs-attribute">Operation</span>=Upgrade<br><span class="hljs-attribute">Operation</span>=Remove<br><span class="hljs-attribute">Type</span>=Package<br><span class="hljs-attribute">Target</span>=nvidia-open-lts<br><span class="hljs-attribute">Target</span>=linux-lts<br><br>[Action]<br><span class="hljs-attribute">Description</span>=Updating Nvidia-lts module <span class="hljs-keyword">in</span> initcpio<br><span class="hljs-attribute">Depends</span>=mkinitcpio<br><span class="hljs-attribute">When</span>=PostTransaction<br>NeedsTargets<br><span class="hljs-attribute">Exec</span>=/bin/sh -c <span class="hljs-string">&#x27;while read -r trg; do case $trg in linux-lts) echo &quot;Already updated.&quot;; exit 0; esac; done; /usr/bin/mkinitcpio -p linux-lts&#x27;</span><br></code></pre></td></tr></table></figure>        </div>      </div>    </div></li><li><p>安装完毕后，重启，然后使用<code>nvidia-smi</code>命令确定驱动是否正常运行。</p></li><li><p>此时xorg应该已经可以正常运行了。对于x11(cinnamon桌面目前仍然默认使用x11，wayland模式仍在测试中)，为了便于配置，进行下面步骤：</p><ul><li>执行：  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nvidia-xconfig<br></code></pre></td></tr></table></figure><blockquote><p>该命令会自动检测并根据现有硬件的情况创建（或修改）&#x2F;etc&#x2F;X11&#x2F;xorg.conf</p></blockquote></li><li>nvidia-settings<sup>pkg</sup>提供了一个图形界面。  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S nvidia-settings<br></code></pre></td></tr></table></figure>  <div class="note note-info">            <p>似乎wayland下还不支持使用这个工具。</p>           </div> 更多x11配置以及wayland配置参见<a href="https://wiki.archlinux.org/title/NVIDIA">ArchWiki</a></li></ul></li></ol><h2 id="4-优化AUR编译参数"><a href="#4-优化AUR编译参数" class="headerlink" title="4.优化AUR编译参数"></a>4.优化AUR编译参数</h2><p>编辑<code>/etc/makepkg.conf</code>，主要做三个方面的修改：</p><ol><li><p>启用多核编译：<br> 定位到<code>MAKEFLAGS</code>处，取消注释，并把-j2改成需要的线程数（这里咱给虚拟机分配了6核）。有一种说法是，如果希望CPU能彻底吃满，应当设定成比CPU线程数多1的超负荷。所以咱最终给了<code>-j7</code></p> <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/makepkg/makeflags1.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/makepkg/makeflags2.png"></div></div></div></li><li><p>更改软件包的压缩格式：<br> 默认情况下打好的软件包使用兼顾速度和压缩率的zstd压缩格式(Arch官方的pacman源也使用这种格式)，这里只列举两种极端需求。两种需求均是定位到<code>PKGEXT</code>处。<br> <img src="/img/arch-cinnamon/makepkg/zstd.png"></p><ul><li>对速度有极端需求，不介意占用空间的：直接不压缩<br>  <img src="/img/arch-cinnamon/makepkg/nocom.png"></li><li>对硬盘空间有极端需求，不介意压缩和解压速度的：改用xz压缩格式<br>  <img src="/img/arch-cinnamon/makepkg/xz.png"></li></ul></li><li><p>阻止生成debug包：<br> 正常情况下，makepkg会生成一个正常包和一个debug软件包；对于普通用户而言，debug包没有任何意义，因此取消掉以节省时间：<br> 定位到<code>GLOBAL PACKAGE OPTION</code>处，然后把最下面的括号中的<code>debug</code>前面加上<code>!</code>。</p> <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/makepkg/nodebug1.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/makepkg/nodebug2.png"></div></div></div></li></ol><h1 id="cinnamon桌面环境"><a href="#cinnamon桌面环境" class="headerlink" title="cinnamon桌面环境"></a>cinnamon桌面环境</h1><h2 id="1-安装桌面环境和中文字体等必要组件"><a href="#1-安装桌面环境和中文字体等必要组件" class="headerlink" title="1.安装桌面环境和中文字体等必要组件"></a>1.安装桌面环境和中文字体等必要组件</h2><p>安装cinnamon<sup>pkg</sup>桌面本体以及一些属于cinnamon的软件：</p><table><thead><tr><th>功能</th><th>应用</th></tr></thead><tbody><tr><td>文字编辑器</td><td>xed<sup>pkg</sup></td></tr><tr><td>图片查看器</td><td><a href="https://aur.archlinux.org/packages/xviewer/">xviewer</a><sup>aur</sup></td></tr><tr><td>文档阅读器</td><td>xreader<sup>pkg</sup></td></tr><tr><td>媒体播放器</td><td><a href="https://aur.archlinux.org/packages/xplayer/">xplayer</a><sup>aur</sup>(<a href="https://github.com/linuxmint/xplayer">上游</a>已停止维护，现版本安装失败，视频播放器直接移步vlc吧)</td></tr><tr><td>图像管理器</td><td><a href="https://aur.archlinux.org/packages/pix/">pix</a><sup>aur</sup></td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S cinnamon xed xreader gnome-terminal<br></code></pre></td></tr></table></figure><div class="note note-secondary">            <p>这里AUR包先不装，由于AUR包的发布特点无法做镜像站，因此总会遇到点网络问题，所以先去基本的桌面解决了网络问题再回来整这些。</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-fbdc9892" role="button" aria-expanded="false" aria-controls="collapse-fbdc9892">        <div class="fold-arrow">▶</div>cinnamon后备模式      </div>      <div class="fold-collapse collapse" id="collapse-fbdc9892">        <div class="fold-content">          <blockquote> <p>当Cinnamon发生崩溃事件时, 会激活其后备模式。要控制此模式下打开的窗口，需要安装metacity软件包以及gnome-shell以显示任务栏。</p> </blockquote> <p>但是存在两个问题：</p> <ol> <li>信息矛盾：参见<a href="https://archlinux.org/packages/extra/x86_64/cinnamon/">cinnamon包页面</a>, 被标记为可选依赖，并提供”fallback mode”的包是metacity和gnome-panel，而不是gnome-shell；</li> <li>引入大量无关依赖：无论是gnome-panel还是gnome-shell，均会引入大量gnome桌面相关的依赖（甚至包含gnome-desktop<sup>pkg</sup>，然后再间接引入nautilus<sup>pkg</sup>等等一大堆gnome应用）。</li> </ol> <p>综合考虑这些信息，并去Linux Mint预装的软件包中一顿搜索后，咱决定只安装metacity<sup>pkg</sup>，等触发了后备模式再看看能不能正常运作。</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S metacity<br></code></pre></td></tr></table></figure>        </div>      </div>    </div><p>然后安装一些常见的开源字体：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S adobe-source-han-serif-cn-fonts wqy-zenhei<br>pacman -S noto-fonts noto-fonts-cjk noto-fonts-emoji noto-fonts-extra<br></code></pre></td></tr></table></figure><div class="note note-info">            <p>中文字体的设置只能通过配置文件；如果不想在这上面花时间，就只安装一套字体（下文的微软字体也不配置）。例如，如果想开箱就有类似mint的字体，就只安装第二行的noto一系。</p>           </div><p>当然还有cinnamon的语言包：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S cinnamon-translations<br></code></pre></td></tr></table></figure><p>在这里直接把ntfs-3g<sup>pkg</sup>安了，提供ntfs文件系统的完整支持：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S ntfs-3g<br></code></pre></td></tr></table></figure><h2 id="1-5-微软字体"><a href="#1-5-微软字体" class="headerlink" title="1.5.微软字体"></a>1.5.微软字体</h2><p>参考<a href="https://wiki.archlinux.org/title/Microsoft_fonts">ArchWiki</a><br>只建议把Windows的字体文件复制到Linux系统下，不建议使用软链接；因为按照咱的使用习惯，咱并不喜欢开机自动挂载Windows分区（甚至希望尽量完全避免挂载Windows系统盘分区）。<br>首先挂载Windows的系统盘（这里考虑直接挂载到<code>/mnt</code>目录，并假设Windows的系统盘分区是<code>/dev/nvme0n1p3</code>）。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#以下两行二选一</span><br>mount -t ntfs3 /dev/nvme0n1p3 /mnt <span class="hljs-comment">#使用内核自带的ntfs3驱动挂载必须显式指定</span><br>mount /dev/nvme0n1p3 /mnt <span class="hljs-comment">#在安装ntfs-3g后会默认使用ntfs-3g驱动</span><br></code></pre></td></tr></table></figure><p>然后复制相应的字体文件到对应目录，并设定好权限：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mkdir</span> /usr/local/share/fonts<br><span class="hljs-built_in">mkdir</span> /usr/local/share/fonts/WindowsFonts<br><span class="hljs-built_in">cp</span> /mnt/Windows/Fonts/* /usr/local/share/fonts/WindowsFonts/<br><span class="hljs-built_in">chmod</span> 644 /usr/local/share/fonts/WindowsFonts/*<br></code></pre></td></tr></table></figure><p>最后重新生成字体缓存：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">fc-cache --force<br></code></pre></td></tr></table></figure><p>以及卸下挂载的Windows分区：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">umount /mnt<br></code></pre></td></tr></table></figure><h2 id="2-startx启动桌面环境"><a href="#2-startx启动桌面环境" class="headerlink" title="2.startx启动桌面环境"></a>2.startx启动桌面环境</h2><p>安装启动图形环境的脚本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S xorg-xinit<br></code></pre></td></tr></table></figure><p>然后使用脚本启动桌面环境（此处应切换到非root用户）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">startx /usr/bin/cinnamon-session <span class="hljs-comment">#启动常规的cinnamon桌面</span><br>startx /usr/bin/cinnamon-session-cinnamon2d <span class="hljs-comment">#或者，启动遇到问题时，启动禁用图形加速的cinnamon桌面</span><br></code></pre></td></tr></table></figure><p>初始状态下的cinnamon真的很丑：<br><img src="/img/arch-cinnamon/cinnamon-start.png"></p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-482f8445" role="button" aria-expanded="false" aria-controls="collapse-482f8445">        <div class="fold-arrow">▶</div>关于xinit      </div>      <div class="fold-collapse collapse" id="collapse-482f8445">        <div class="fold-content">          <p>xinit的更多用法参考<a href="https://wiki.archlinux.org/title/Xinit">ArchWiki</a><br>使用这个脚本启动桌面可以绕开桌面管理器（以下简称dm），在桌面崩溃的时候，可以使用这个脚本排除问题（xorg&#x2F;dm&#x2F;桌面三者均可能出问题，用这个可以排除dm的问题，<del>不过大部分时候都是xorg在抽风</del>）<br>这里不装dm还有一个比较难绷的理由，曾经有一段很长的时间，<code>lightdm-slick-greeter</code><sup>pkg</sup>这个包是坏的<del>或者只是单纯是咱那会wiki没看全没配置对</del>，启动lightdm会无法显示登录界面；而这个greeter是mint主题的默认登录界面，而且有些mint相关的应用程序直接硬依赖这个greeter，因此只能去AUR里面取两个包曲线救国: <a href="https://aur.archlinux.org/packages/lightdm-slick-greeter-mint-theme">lightdm-slick-greeter-mint-theme</a><sup>aur</sup>和<a href="https://aur.archlinux.org/packages/lightdm-guest">lightdm-guest</a><sup>aur</sup>。不过现在这个包已经好了，所以跳过<code>xinit</code>环节也行，但是咱还是更喜欢留着xinit脚本在排查故障的时候用。</p>         </div>      </div>    </div><h2 id="2-5-一点必要的小手段"><a href="#2-5-一点必要的小手段" class="headerlink" title="2.5.一点必要的小手段"></a>2.5.一点必要的小手段</h2><p>AUR是源码发布机制，从源码可以发现很多开源包的source是直接从github上面拉的，所以难免遇到网络问题，需要一些必要的小手段。<br>pacman上面有一些已经打包好的客户端可以直接用（善用<code>pacman -Ss</code>多做搜索）。<br>总之就是现在有了一只可爱的小猫来解决网络问题：<br><img src="/img/arch-cinnamon/clash-verge-rev.png"></p><h2 id="3-安装桌面管理器"><a href="#3-安装桌面管理器" class="headerlink" title="3.安装桌面管理器"></a>3.安装桌面管理器</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S lightdm lightdm-slick-greeter<br></code></pre></td></tr></table></figure>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-3292b6c2" role="button" aria-expanded="false" aria-controls="collapse-3292b6c2">        <div class="fold-arrow">▶</div>其它可选项      </div>      <div class="fold-collapse collapse" id="collapse-3292b6c2">        <div class="fold-content">          <p><a href="https://aur.archlinux.org/packages/lightdm-guest">lightdm-guest</a><sup>aur</sup>: 除了提供lightdm本体外，额外支持访客用户登录。<br><a href="https://aur.archlinux.org/packages/lightdm-slick-greeter-mint-theme">lightdm-slick-greeter-mint-theme</a><sup>aur</sup>: 额外提前设置好了一些选项，具体参考AUR页面的置顶评论。这个包强制依赖<a href="https://aur.archlinux.org/packages/lightdm-guest">lightdm-guest</a><sup>aur</sup>。由于这个包还会使用<code>wget</code>命令下载当日的微软壁纸并设置为锁屏壁纸，因此确保系统安装了<code>wget</code>否则会打包失败。<br>如果使用了这两个包，下面的配置greeter可以不做，这两个包会自动配置好。</p>         </div>      </div>    </div><p>然后还要配置greeter:编辑<code>/etc/lightdm/lightdm.conf</code>，找到<code>[Seat:*]</code>模块，然后修改<code>greeter-session</code>变量，就像这样：</p><figure class="highlight node-repl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs node-repl">[Seat:*]<br><span class="hljs-meta prompt_">...</span><br>greeter-session=lightdm-slick-greeter<br></code></pre></td></tr></table></figure><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/greeter1.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/greeter2.png"></div></div></div><div class="note note-warning">            <p>这个配置过程不是可选的，不设置的话会导致greeter无法启动，如果配置了自动登录会无法切换用户，如果没配置自动登录就会直接导致lightdm启动失败。<br>但是作为lightdm默认的<code>lightdm-gtk-greeter</code>例外。</p>           </div><p>最后把lightdm设置为开机自启，由于现在还开着桌面就不立即启动lightdm了：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl <span class="hljs-built_in">enable</span> lightdm.service<br></code></pre></td></tr></table></figure><h2 id="4-设置中文"><a href="#4-设置中文" class="headerlink" title="4.设置中文"></a>4.设置中文</h2><p>编辑<code>/etc/locale.conf</code>，把<code>LANG=en_US.UTF-8</code>改为<code>LANG=zh_CN.UTF-8</code>。<br>但是这样修改之后，tty终端就会试图显示中文（但是只会显示菱形方块），当桌面崩溃需要tty救急时这会导致tty输出完全不可读。<br>所以在<code>/etc/profile.d/</code>路径下新建一个空文件，取一个喜欢的名字（例如<code>tty-en.sh</code>），然后填入以下内容：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># Set tty English</span><br><br><span class="hljs-keyword">for</span> ttyi <span class="hljs-keyword">in</span> &#123;1..6&#125;; <span class="hljs-keyword">do</span><br>    <span class="hljs-keyword">if</span> [ <span class="hljs-string">&quot;<span class="hljs-subst">$(tty)</span>&quot;</span> = <span class="hljs-string">&quot;/dev/tty<span class="hljs-variable">$ttyi</span>&quot;</span> ];<br>    <span class="hljs-keyword">then</span><br>        <span class="hljs-built_in">export</span> LC_ALL=<span class="hljs-string">&quot;en_US.UTF-8&quot;</span><br>        <span class="hljs-built_in">export</span> LANGUAGE=<span class="hljs-string">&quot;en_US.UTF-8&quot;</span><br>        <span class="hljs-built_in">export</span> LANG=<span class="hljs-string">&quot;en_US.UTF-8&quot;</span><br>    <span class="hljs-keyword">fi</span><br><span class="hljs-keyword">done</span><br></code></pre></td></tr></table></figure><p>这会当检测到窗口处于tty时，把语言设为英文。添加在这里和把代码追加在<code>/etc/profile</code>文件中等效，并且对所有用户生效。</p><div class="note note-info">            <p><a href="https://aur.archlinux.org/packages/mintlocale">mintlocale</a><sup>aur</sup>会在cinnamon桌面的设置菜单中添加一个语言设置菜单，允许设置当前用户或者所有用户的语言，其中设置所有用户时会修改<code>/etc/locale.conf</code>。<br>这个包无论在什么语言环境下安装，最终菜单都会以英文显示，无法完美的像原生Mint系统下变为中文菜单。</p>           </div><p>完成设置语言之后重启计算机。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">reboot<br></code></pre></td></tr></table></figure><p>重启后就能看到登录界面了：<br><img src="/img/arch-cinnamon/login.png"></p><blockquote><p>为防止系统用户出现在登录界面，安装可选依赖accountsservice<sup>pkg</sup>, 或者把这些用户名添加到<code>/etc/lightdm/users.conf</code>下的<code>hidden-users</code>里。前者优势在于添加&#x2F;删除用户时不用更新列表。</p></blockquote><h2 id="4-5-配置自动登录"><a href="#4-5-配置自动登录" class="headerlink" title="4.5.配置自动登录"></a>4.5.配置自动登录</h2><p>编辑<code>/etc/lightdm/lightdm.conf</code>，同样找到<code>[Seat:*]</code>模块，然后修改<code>autologin-user</code>变量，改为想要自动登录的用户名；<br>然后还需要确保自动登录的用户在<code>autologin</code>用户组中：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">groupadd autologin <span class="hljs-comment">#创建autologin组</span><br>gpasswd -a username autologin <span class="hljs-comment">#把用户username添加进去</span><br></code></pre></td></tr></table></figure><p>或者可以使用cinnamon桌面的图形化设置操作用户组：</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/group1.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/group2.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/group3.png"></div></div></div><h2 id="5-cinnamon配置"><a href="#5-cinnamon配置" class="headerlink" title="5.cinnamon配置"></a>5.cinnamon配置</h2><h3 id="terminal"><a href="#terminal" class="headerlink" title="terminal"></a>terminal</h3><p><code>编辑</code>→<code>首选项</code>，把字体改为<code>source code pro</code>且大小为10号，颜色主题改为<code>Tango 暗色</code></p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/theme/terminal1.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/theme/terminal2.png"></div></div></div><h3 id="桌面与主题"><a href="#桌面与主题" class="headerlink" title="桌面与主题"></a>桌面与主题</h3><p>安装以下包：</p><table><thead><tr><th>包名</th><th>内容</th></tr></thead><tbody><tr><td><a href="https://aur.archlinux.org/packages/bibata-cursor-theme">bibata-cursor-theme</a><sup>aur</sup></td><td>鼠标指针</td></tr><tr><td><a href="https://aur.archlinux.org/packages/mint-y-icons">mint-y-icons</a><sup>aur</sup></td><td>图标主题</td></tr><tr><td><a href="https://aur.archlinux.org/packages/mint-artwork">mint-artwork</a><sup>aur</sup></td><td>壁纸和开始菜单图标，顺便绑定了一堆mint常用的主题、字体等等</td></tr></tbody></table><div class="note note-info">            <p>事实上，<a href="https://aur.archlinux.org/packages/mint-artwork">mint-artwork</a><sup>aur</sup>已经包含了<a href="https://aur.archlinux.org/packages/bibata-cursor-theme">bibata-cursor-theme</a><sup>aur</sup>和<a href="https://aur.archlinux.org/packages/mint-y-icons">mint-y-icons</a><sup>aur</sup>作为依赖，因此直接安装<a href="https://aur.archlinux.org/packages/mint-artwork">mint-artwork</a><sup>aur</sup>即可。</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">paru -S mint-artwork<br></code></pre></td></tr></table></figure>          </div><p>全部安装完成之后，进入系统设置，主题菜单已经发生了变化：<br><img src="/img/arch-cinnamon/theme/theme1.png"><br>点开高级设置展开可以看到以前的界面，然后做一些想要的调整就行。<br><img src="/img/arch-cinnamon/theme/theme2.png"><br>此外还有壁纸：<br><img src="/img/arch-cinnamon/theme/wallpaper.png"><br>调整一下字体（<code>pacman -S ttf-dejavu</code>）：<br><img src="/img/arch-cinnamon/theme/fonts.png"><br>到这一步后，基本的设置大体完成，但是中文字体似乎还没统一。针对中文字体的更详细配置参考<a href="https://wiki.archlinux.org/title/Font_configuration/Chinese">ArchWiki</a></p><p>此外，还有一份 <a href="https://github.com/licyk/cinnamon-theme-transparent">半透明主题</a> 可以安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> https://github.com/licyk/cinnamon-theme-transparent<br></code></pre></td></tr></table></figure><p>然后把三个主题文件夹放在<code>~/.themes/</code>（对单用户生效）或者<code>/usr/local/share/themes/</code>（对所有用户生效），之后就可以使用了。</p><div class="note note-info">            <p>不通过包管理器而是自行安装的东西都放在<code>/usr/local/</code>目录下。</p>           </div><div class="note note-info">            <p>虽然这主题截至2026年1月还能用，但是主线版本已经迭代n次，这个主题已经显得过旧了；并且改法也是老旧的cinnamon主题的改法。<br>还是尽量使用官方支持的主题，然后可以在cinnamon扩展里面安装transparent panels reloaded扩展，实现面板半透明。</p>           </div><h3 id="“开始”菜单"><a href="#“开始”菜单" class="headerlink" title="“开始”菜单"></a>“开始”菜单</h3><p>在左下处右键，然后选择配置：<br><img src="/img/arch-cinnamon/start1.png"><br>勾选“使用自定义图标和标签”，根据图示选图标，文本删空：<br><img src="/img/arch-cinnamon/start2.png"></p><h2 id="6-补齐系统应用"><a href="#6-补齐系统应用" class="headerlink" title="6.补齐系统应用"></a>6.补齐系统应用</h2><p>到这一步，cinnamon桌面的外观已经基本没有问题了，但是系统设置里面还缺少了很多项。<br>（左ArchLinux, 右LinuxMint22.1)</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/settings-arch1.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/settings-mint1.png"></div></div><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/settings-arch2.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/settings-mint2.png"></div></div></div><p>接下来逐一补充。</p><ol><li>输入法：与<code>apt</code>高度耦合，无法补充，自行配置。</li><li>语言：安装<a href="https://aur.archlinux.org/packages/mintlocale">mintlocale</a><sup>aur</sup></li><li>在线账户：安装<a href="https://aur.archlinux.org/packages/gnome-online-accounts-gtk">gnome-online-accounts-gtk</a><sup>aur</sup></li><li>磁盘：安装gnome-disk-utility<sup>pkg</sup></li><li>打印机：安装system-config-printer<sup>pkg</sup></li><li>蓝牙：安装blueman<sup>pkg</sup></li><li>驱动管理和软件源：与<code>apt</code>高度耦合，无法补充，自行配置。</li><li>防火墙：安装gufw<sup>pkg</sup></li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">paru -S mintlocale gnome-online-accounts-gtk gnome-disk-utility system-config-printer blueman gufw<br></code></pre></td></tr></table></figure><div class="note note-info">            <p>其中大部分应用安装即可使用，但也有一些只是前端窗口，使用需要额外配置后台服务（如打印机可能需要配置<code>cups</code><sup>pkg</sup>）</p>           </div><p>此外还有一些其它的系统软件：<br>重要的：<br>nemo-fileroller<sup>pkg</sup>: 压缩与解压文件，同时提供Nemo文件管理器的右键压缩与解压支持<br>其它的Linux Mint预装软件：</p><table><thead><tr><th>软件</th><th>在Arch Linux的包名</th></tr></thead><tbody><tr><td>归档管理器</td><td><a href="https://aur.archlinux.org/packages/file-roller-linuxmint">file-roller-linuxmint</a><sup>aur</sup>取代默认的gnome主题的fileroller；留意fileroller的可选依赖，其中包含对其它各种压缩格式的额外支持，自选安装</td></tr><tr><td>便笺</td><td><a href="https://aur.archlinux.org/packages/sticky">sticky</a><sup>aur</sup></td></tr><tr><td>磁盘使用情况分析器</td><td><a href="https://aur.archlinux.org/packages/baobab-gtk3">baobab-gtk3</a><sup>aur</sup></td></tr><tr><td>电源统计</td><td>gnome-power-manager<sup>pkg</sup></td></tr><tr><td>计算器</td><td><a href="https://aur.archlinux.org/packages/gnome-calculator-gtk3">gnome-calculator-gtk3</a><sup>aur</sup></td></tr><tr><td>截图</td><td>gnome-screenshot<sup>pkg</sup></td></tr><tr><td>密码和密钥</td><td>后端gnome-keyring<sup>pkg</sup>,前端seahorse<sup>pkg</sup></td></tr><tr><td>日历</td><td>gnome-calendar<sup>pkg</sup><span class="label label-info">只有gnome版，没有与cinnamon契合的旧版</span>咱自己移植了一下，目前能用，仓库地址 <a href="https://gitee.com/silvertuanzi/gnome-calendar-linuxmint">gitee</a> &#x2F; <a href="https://github.com/silvertuanzi/gnome-calendar-linuxmint">github</a></td></tr><tr><td>书库</td><td><a href="https://aur.archlinux.org/packages/thingy">thingy</a><sup>aur</sup></td></tr><tr><td>网络电视 Hypnotix</td><td><a href="https://aur.archlinux.org/packages/hypnotix">hypnotix</a><sup>aur</sup>或者<a href="https://aur.archlinux.org/packages/hypnotix-wayland">hypnotix-wayland</a><sup>aur</sup></td></tr><tr><td>文档扫描仪</td><td><a href="https://aur.archlinux.org/packages/simple-scan-linuxmint">simple-scan-linuxmint</a><sup>aur</sup></td></tr><tr><td>文件重命名工具</td><td><a href="https://aur.archlinux.org/packages/bulky">bulky</a><sup>aur</sup></td></tr><tr><td>系统监视器</td><td>cinnamon版本<a href="https://aur.archlinux.org/packages/gnome-system-monitor-linuxmint">gnome-system-monitor-linuxmint</a><sup>aur</sup>显示效果更好，但是gnome版本gnome-system-monitor<sup>pkg</sup>在系统参数页面多显示一项磁盘读写速度</td></tr><tr><td>字符映射表</td><td>gucharmap<sup>pkg</sup></td></tr><tr><td>Web应用</td><td><a href="https://aur.archlinux.org/packages/webapp-manager">webapp-manager</a><sup>aur</sup></td></tr></tbody></table><p>未列在此表中的内容：<br>libreoffice全家桶，以及下图:</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/arch-cinnamon/mintapp1.png"></div><div class="group-image-wrap"><img src="/img/arch-cinnamon/mintapp2.png"></div></div></div><p>电源模式（平衡，节能，以及只在部分设备开放的高性能）由power-profiles-daemon<sup>pkg</sup>提供。</p><div class="note note-info">            <p>搜索时优先寻找<code>gtk3</code>后缀的AUR老版本，通常会跟cinnamon桌面的契合度更高。</p>           </div>    <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-a23856b1" role="button" aria-expanded="false" aria-controls="collapse-a23856b1">        <div class="fold-arrow">▶</div>2025.02.21编译gnome-system-monitor-linuxmint报错      </div>      <div class="fold-collapse collapse" id="collapse-a23856b1">        <div class="fold-content">          <p>错误日志：</p> <figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs gams">Dependency glib<span class="hljs-number">-2.0</span> found: <span class="hljs-keyword">YES</span> <span class="hljs-number">2.82</span><span class="hljs-number">.4</span> (cached)<br>Program /usr/bin/glib-mkenums found: <span class="hljs-keyword">NO</span><br><br>gnome-<span class="hljs-keyword">system</span>-monitor/src/meson.build:<span class="hljs-number">79</span>:<span class="hljs-number">22</span>: ERROR: Dependency <span class="hljs-string">&#x27;glib-2.0&#x27;</span> tool <span class="hljs-keyword">variable</span> <span class="hljs-string">&#x27;glib_mkenums&#x27;</span> contains <span class="hljs-comment">erroneous value:</span> <span class="hljs-comment">&#x27;/usr/bin/glib-mkenums&#x27;</span><br><br>This <span class="hljs-comment">is a distributor issue -- please report it to your glib-2.0 provider.</span><br><br>A <span class="hljs-comment">full log can be found at</span> /home/<span class="hljs-comment">lasombra</span>/.cache/<span class="hljs-comment">paru</span>/clone/<span class="hljs-comment">gnome-system-monitor-linuxmint</span>/src/<span class="hljs-comment">build</span>/meson-logs/<span class="hljs-comment">meson-log.txt</span><br></code></pre></td></tr></table></figure> <p>解决方案：安装<code>glib2-devel</code><sup>pkg</sup></p>         </div>      </div>    </div><div class="note note-info">            <p>关于应用主题的另一种方案：AUR搜索<a href="https://aur.archlinux.org/packages?O=0&K=libadwaita-without-adwaita">libadwaita-without-adwaita</a>, 当前的包名为<a href="https://aur.archlinux.org/packages/libadwaita-without-adwaita-git">libadwaita-without-adwaita-git</a><sup>aur</sup>, 但是根据规范该包不应该带有<code>-git</code>后缀。因此后续可能改名。<br>用这个包替换掉原生的<code>libadwaita</code>后，直接安装gnome应用也能融入桌面，不再会出现“一股gnome味”的情况。<br>对比：</p> <table> <thead> <tr> <th>gnome-system-monitor</th> <th>without-adwaita</th> <th>gnome-system-monitor-linuxmint</th> </tr> </thead> <tbody><tr> <td><img src="/img/arch-cinnamon/monitor/gnome.png"></td> <td><img src="/img/arch-cinnamon/monitor/none-adwaita.png"></td> <td><img src="/img/arch-cinnamon/monitor/mint.png"></td> </tr> </tbody></table>           </div><h2 id="7-挂载硬盘设置"><a href="#7-挂载硬盘设置" class="headerlink" title="7.挂载硬盘设置"></a>7.挂载硬盘设置</h2><p>还有一个问题：Nemo挂载硬盘时，会要求输入密码，而不是像mint一样直接挂载：<br><img src="/img/arch-cinnamon/mount-need-passwd.png"><br>Nemo挂载硬盘使用的是<code>udisks2</code>服务，因此在Nemo中挂载硬盘等价于：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">udisksctl mount -b /dev/nvme0n1p1<br></code></pre></td></tr></table></figure><p>需要配置一项放行规则：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">/* 把这个文件复制到 /etc/polkit-1/rules.d/allow-mount-internal.rules */</span><br><br>polkit.<span class="hljs-title function_">addRule</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params">action, subject</span>) &#123;<br>    <span class="hljs-keyword">if</span> ((action.<span class="hljs-property">id</span> == <span class="hljs-string">&quot;org.freedesktop.udisks2.filesystem-mount-system&quot;</span> ||<br>         action.<span class="hljs-property">id</span> == <span class="hljs-string">&quot;org.freedesktop.udisks.filesystem-mount-system-internal&quot;</span>) &amp;&amp;<br>        subject.<span class="hljs-property">local</span> &amp;&amp; subject.<span class="hljs-property">active</span> &amp;&amp; subject.<span class="hljs-title function_">isInGroup</span>(<span class="hljs-string">&quot;storage&quot;</span>))<br>    &#123;<br>            <span class="hljs-keyword">return</span> polkit.<span class="hljs-property">Result</span>.<span class="hljs-property">YES</span>;<br>    &#125;<br>&#125;);<br></code></pre></td></tr></table></figure><p>这个规则会给用户组<code>storage</code>中的用户放行挂载；然后还需要把用户添加到<code>storage</code>组中。把规则中的用户组直接给<code>wheel</code>组也行。<br>或者，按需取用<a href="https://gist.github.com/Scrumplex/8f528c1f63b5f4bfabe14b0804adaba7">这份更详细的规则</a></p><p>此外，默认会把硬盘挂载到<code>/run/media/&lt;username&gt;</code>目录下，路径太长很不优雅，添加这份配置可以把挂载目录改为<code>/media</code>：<br>文件路径：<code>/etc/udev/rules.d/99-udisks2.rules</code></p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs dockerfile"><span class="hljs-comment"># UDISKS_FILESYSTEM_SHARED</span><br><span class="hljs-comment"># ==1: mount filesystem to a shared directory (/media/VolumeName)</span><br><span class="hljs-comment"># ==0: mount filesystem to a private directory (/run/media/$USER/VolumeName)</span><br><span class="hljs-comment"># See udisks(8)</span><br><span class="hljs-keyword">ENV</span>&#123;ID_FS_USAGE&#125;==<span class="hljs-string">&quot;filesystem|other|crypto&quot;</span>, <span class="hljs-keyword">ENV</span>&#123;UDISKS_FILESYSTEM_SHARED&#125;=<span class="hljs-string">&quot;1&quot;</span><br></code></pre></td></tr></table></figure><p>更多设置项参见<a href="https://wiki.archlinux.org/title/Udisks">ArchWiki-udisks</a></p><h2 id="8-家目录"><a href="#8-家目录" class="headerlink" title="8.家目录"></a>8.家目录</h2><p>现在的家目录下只有“桌面”一个文件夹。<br>xdg-user-dirs<sup>pkg</sup>可以帮助创建一套默认的家目录文件夹。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S xdg-user-dirs<br>LC_ALL=C.UTF-8 xdg-user-dirs-update --force <span class="hljs-comment">#强制让目录为英文命名；不然当心在特殊情况下输不了中文路径</span><br></code></pre></td></tr></table></figure><p>此外还有一些少量的自定义配置参见<a href="https://wiki.archlinux.org/title/XDG_user_directories">ArchWiki</a><br>Nemo的侧边栏可以一个一个手动添加，并且可以把侧边栏单独重命名成中文而不影响实际路径。</p><h1 id="安装桌面之后"><a href="#安装桌面之后" class="headerlink" title="安装桌面之后"></a>安装桌面之后</h1><h2 id="1-常用软件"><a href="#1-常用软件" class="headerlink" title="1.常用软件"></a>1.常用软件</h2><h3 id="写在前面：Tauri与NVIDIA的犯冲"><a href="#写在前面：Tauri与NVIDIA的犯冲" class="headerlink" title="写在前面：Tauri与NVIDIA的犯冲"></a>写在前面：Tauri与NVIDIA的犯冲</h3><p>Nvidia显卡在启动几乎全部Tauri应用时都可能会遇到只显示边框，内容无法显示的问题，经测试属于Tauri UI的通病（参见<a href="https://github.com/tauri-apps/tauri/issues/13493">issue</a>）<br>解决方案：为应用启动器添加环境变量<code>WEBKIT_DISABLE_DMABUF_RENDERER=1</code>或者<code>WEBKIT_DISABLE_COMPOSITING_MODR=1</code>。</p><h3 id="浏览器"><a href="#浏览器" class="headerlink" title="浏览器"></a>浏览器</h3><p>开源的：firefox<sup>pkg</sup>，chromium<sup>pkg</sup><br>闭源但是常用的：microsoft-edge-stable-bin<sup>edu</sup>，google-chrome<sup>edu</sup>，或者在AUR上获取更新更快的版本以及beta测试版等等（arch4edu源通常同步会慢些）</p><h3 id="下载器"><a href="#下载器" class="headerlink" title="下载器"></a>下载器</h3><p>主力：<a href="https://abdownloadmanager.com/?lang=zh-CN">AB download manager</a><br>目前咱双系统都作为主力用的下载器，开源的，UI也很舒服（比IDM好看一点）。<br>浏览器插件：<a href="https://chromewebstore.google.com/detail/ab-download-manager-brows/bbobopahenonfdgjgaleledndnnfhooj">edge&#x2F;chrome</a> (需要科学上网)，<a href="https://addons.mozilla.org/en-US/firefox/addon/ab-download-manager/">firefox</a><br>安装完之后，去浏览器插件的设置里面，有一个选项是控制接管下载的文件类型，把经常下载的文件类型补进去。</p><div class="note note-info">            <p>fdm免费但不开源，找到好用的开源替代品它就该下班了。</p>           </div><p><del>主力</del>已弃用: <a href="https://aur.archlinux.org/packages/freedownloadmanager">freedownloadmanager</a><sup>aur</sup><br>FDM浏览器插件: <a href="https://chrome.google.com/webstore/detail/free-download-manager-chr/ahmpjcflkgiildlgicmcieglgoilbfdp">edge&#x2F;chrome</a> (需要科学上网)，<a href="https://addons.mozilla.org/en-US/firefox/addon/free-download-manager-addon/">firefox</a></p><div class="note note-info">            <p>2025年6月8日更新：fdm无法启动，在命令行中输入<code>fdm</code>执行，报错日志为：</p> <figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs livecodeserver">qt.qpa.plugin: From <span class="hljs-number">6.5</span><span class="hljs-number">.0</span>, xcb-cursor0 <span class="hljs-keyword">or</span> libxcb-cursor0 is needed <span class="hljs-built_in">to</span> <span class="hljs-built_in">load</span> <span class="hljs-keyword">the</span> Qt xcb <span class="hljs-built_in">platform</span> plugin.<br>qt.qpa.plugin: Could <span class="hljs-keyword">not</span> <span class="hljs-built_in">load</span> <span class="hljs-keyword">the</span> Qt <span class="hljs-built_in">platform</span> plugin <span class="hljs-string">&quot;xcb&quot;</span> <span class="hljs-keyword">in</span> <span class="hljs-string">&quot;&quot;</span> even though <span class="hljs-keyword">it</span> was found.<br>This application failed <span class="hljs-built_in">to</span> <span class="hljs-built_in">start</span> because no Qt <span class="hljs-built_in">platform</span> plugin could be initialized. Reinstalling <span class="hljs-keyword">the</span> application may fix this problem.<br><br>Available <span class="hljs-built_in">platform</span> plugins are: eglfs, linuxfb, minimal, minimalegl, offscreen, wayland-egl, wayland, xcb.<br></code></pre></td></tr></table></figure> <p>解决方法：安装xcb-util-cursor<sup>pkg</sup></p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S xcb-util-cursor<br></code></pre></td></tr></table></figure>          </div><p>备用：挑选原则是找一个aria2套壳的GUI下载器，目前挑定的是<a href="https://github.com/AnInsomniacy/motrix-next">motrix-next</a><br>它基于Tauri UI，内核使用优化过的<a href="https://github.com/AnInsomniacy/aria2-next">aria2-next</a>下载引擎，使用体验尚可。<br><br>与此同时老版motrix咱其实还没卸载但是已经几乎不用了（electron做这种东西是真的卡啊）<br><br><del>备用</del>已弃用：<a href="https://aur.archlinux.org/packages/motrix">motrix</a><sup>aur</sup>和<a href="https://aur.archlinux.org/packages/imfile">imfile</a><sup>aur</sup><br>下面的删除线内容记录了这俩兄弟的长期咕咕咕历史，不过今年(2026)它们都突然开始活跃更新，且发版频率大幅上升，大抵是AI的功劳，再观望观望。<br><del>Motrix上次更新时间已经是2023年5月，有一个现在持续维护的fork版<a href="https://aur.archlinux.org/packages/imfile">imfile</a><sup>aur</sup>(imfile最近一个版本也是2024年8月了，官网域名都丢了，并且软件稳定性还不如motrix原版)</del></p><h3 id="微信-QQ"><a href="#微信-QQ" class="headerlink" title="微信&#x2F;QQ"></a>微信&#x2F;QQ</h3><p>为了避免扫盘，可以使用flatpak精细地控制权限。 <a href="https://flatpak.org/setup/Arch">flatpak配置指南</a> 以及<a href="https://mirror.sjtu.edu.cn/docs/flathub">SJTU镜像源</a><br>个人喜欢直接使用简单版本。</p><table><thead><tr><th>软件</th><th>纯净版（简单破事少）</th><th>沙盒版（使用bubblewarp做简单的沙盒隔离，但是系统文件访问可能不便，例如收发其它目录的文件）</th><th>备注</th></tr></thead><tbody><tr><td>QQ</td><td><a href="https://aur.archlinux.org/packages/linuxqq">linuxqq</a><sup>aur</sup></td><td><a href="https://aur.archlinux.org/packages/linuxqq-nt-bwrap">linuxqq-nt-bwrap</a><sup>aur</sup></td><td>直接去官网下载appimage版也可运行；还有 <a href="https://aur.archlinux.org/packages/linuxqq-portable">基于portable的另一种沙盒版</a> 可选</td></tr><tr><td>微信</td><td><a href="https://gitee.com/evine/wechat-bin">gitee</a> &#x2F; <a href="https://github.com/devome/wechat-bin">github</a></td><td><a href="https://aur.archlinux.org/packages/wechat-universal-bwrap">wechat-universal-bwrap</a><sup>aur</sup>以及<a href="https://github.com/7Ji-PKGBUILDs/wechat-universal-bwrap/">备用的github仓库</a></td><td>直接去官网下载appimage版也可运行; 2024年12月，AUR上的微信包所有权争夺撕了很久，最终留下了一地鸡毛；现在的Kimiblock维护者维护的版本引入了一大堆无用依赖，不建议使用</td></tr></tbody></table><h3 id="文本编辑器"><a href="#文本编辑器" class="headerlink" title="文本编辑器"></a>文本编辑器</h3><p>cinnamon桌面配套的<code>xed</code>已经可以满足基本的需求；<br><br>官方仓库里的code<sup>pkg</sup>是对 <a href="https://github.com/microsoft/vscode.git">开源vscode仓库</a> 编译而来，可能不包含一些微软增加的功能；由微软发布的版本可以通过<a href="https://aur.archlinux.org/packages/visual-studio-code-bin">visual-studio-code-bin</a><sup>aur</sup>安装。<br><br><del>轻量的文本编辑器，notepadqq<sup>pkg</sup>本来是个好选择，但是该项目最后一次有实质的更新已经是2021年的事了，而现在的版本在ArchLinux中会频繁闪退；降级之后似乎可以稳定运行，或者根据 <a href="https://github.com/notepadqq/notepadqq/issues/1108#issuecomment-2143405678">github issue</a> ，禁用自动保存可以解决闪退问题，但这需要在启动后15s之内完成设置；<br><br>notepad--( <a href="https://gitee.com/cxasm/notepad--">gitee</a> &#x2F; <a href="https://github.com/cxasm/notepad--">github</a> )曾经是个开源的文本编辑器，但是现版本已经转向闭源；<a href="https://aur.archlinux.org/packages/notepad---git">notepad---git</a><sup>aur</sup>从git拉取开源的版本然后编译安装，目前还算好用，但是UI内鼠标指针会变，跟系统鼠标指针不一致；<br></del><br>轻量文本编辑器推荐使用<a href="https://aur.archlinux.org/packages/notepadqq">notepadqq</a><sup>aur</sup>。目前该项目经历了大型现代化重构，已经可以直接正常使用。<br><br><a href="https://github.com/dail8859/NotepadNext">notepadnext</a> 是个很有潜力的好项目，可惜现在还不支持转换文件编码。<br></p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-a7d599f5" role="button" aria-expanded="false" aria-controls="collapse-a7d599f5">        <div class="fold-arrow">▶</div>降级notepadqq(现在不再需要)      </div>      <div class="fold-collapse collapse" id="collapse-a7d599f5">        <div class="fold-content">          <ul> <li>降级到2.0.0beta-1：可以直接使用下文<a href="#6-%E8%BD%AF%E4%BB%B6%E5%8C%85%E9%99%8D%E7%BA%A7">软件包降级</a>的downgrade脚本。以root权限运行：  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">downgrade notepadqq<br></code></pre></td></tr></table></figure>   然后在跳出的页面选择2.0.0beta-1版本即可。下载可能有困难，准备好网络加速或者多尝试几次；<br>  最后downgrade脚本会提示是否把降级后的包加入<code>IgnorePkg</code>，选择<code>y</code>加入，阻止滚动更新的时候被升级回去。这个修改会保存在<code>/etc/pacman.conf</code>里面。</li> <li>降级到1.4.8-6或者更早的版本（2.0.0beta-1如果上述方法失效也可以采用这种方式）：旧版的包由于年代过于久远，签名已经失效，因此获取官方的PKGBUILD仓库自己重新编译打包：  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> https://gitlab.archlinux.org/archlinux/packaging/packages/notepadqq.git <span class="hljs-comment">#获取仓库</span><br><span class="hljs-built_in">cd</span> notepadqq<br>git checkout 1.4.8-6 <span class="hljs-comment">#切换到需要的版本</span><br>makepkg -si <span class="hljs-comment">#获取依赖，打包，安装</span><br></code></pre></td></tr></table></figure>   为了避免失效，咱在 <a href="https://gitee.com/silvertuanzi/notepadqq-archpkgbuild">gitee</a> 上镜像了一份这个仓库，作为备份地址。以及原始仓库的备份：<a href="https://gitee.com/silvertuanzi/notepadqq">notepadqq</a> <a href="https://gitee.com/silvertuanzi/CodeMirror/">CodeMirror</a></li> </ul>         </div>      </div>    </div><h3 id="任务管理器"><a href="#任务管理器" class="headerlink" title="任务管理器"></a>任务管理器</h3><p>安装mission-center<sup>pkg</sup>，它具有类似Windows任务管理器的界面：[<a href="https://missioncenter.io/">官网</a>]<br><img src="/img/arch-cinnamon/mission-center.png"></p><div class="note note-info">            <p>截图使用了<a href="https://aur.archlinux.org/packages/libadwaita-without-adwaita">libadwaita-without-adwaita</a><sup>aur</sup></p>           </div><p>此外，安装nethogs<sup>pkg</sup>之后，应用页可以显示每进程的网络占用情况。</p><hr><p>其它软件多在AUR上面搜索基本都能搜到。</p><h2 id="2-脚本update-grub"><a href="#2-脚本update-grub" class="headerlink" title="2.脚本update-grub"></a>2.脚本update-grub</h2><p>在Debian&#x2F;Ubuntu系统上，修改了grub配置后只需要简单地以<code>sudo</code>权限运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">update-grub<br></code></pre></td></tr></table></figure><p>就可以完成配置更新，但是Arch上面却需要：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">grub-mkconfig -o /boot/grub/grub.cfg<br></code></pre></td></tr></table></figure><p>实际上<code>update-grub</code>就是一个简单的脚本，因此可以很简单地移植过来：<br>可以直接安装<a href="https://aur.archlinux.org/packages/update-grub">update-grub</a><sup>aur</sup>包来实现，或者手动创建这个脚本：<br>在<code>/usr/local/bin</code>目录下，以root权限创建文本文件，命名为<code>update-grub</code>，然后把权限设为755（所有者可读、写、执行，其他用户可读和执行）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> 755 /usr/local/bin/update-grub<br></code></pre></td></tr></table></figure><p>然后填入以下内容：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-meta">#!/bin/sh</span><br><span class="hljs-built_in">set</span> -e<br><span class="hljs-built_in">exec</span> grub-mkconfig -o /boot/grub/grub.cfg <span class="hljs-string">&quot;<span class="hljs-variable">$@</span>&quot;</span><br></code></pre></td></tr></table></figure><p>保存即可。</p><h2 id="3-timeshift快照"><a href="#3-timeshift快照" class="headerlink" title="3.timeshift快照"></a>3.timeshift快照</h2><p>安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S timeshift<br></code></pre></td></tr></table></figure><p>timeshift快照分为两种类型，在页面上都有介绍，这里提取其中的几个关键要素：</p><table><thead><tr><th>快照类型</th><th>特点</th></tr></thead><tbody><tr><td>RSYNC</td><td>适用于任何文件系统，不依赖文件系统特性；基于全盘复制，因此占用空间较大，但是可以把快照储存在非系统盘上；第一次快照的时候是完全复制整个系统，此后的快照中与前面的快照相同的文件使用硬链接保存，不同的文件直接复制。快照占用的空间一经创建就确定下来；可以精细地选择需要快照和不需要快照的文件夹</td></tr><tr><td>BTRFS</td><td>仅适用于btrfs文件系统，且仅支持<code>@</code>和<code>@home</code>的子卷布局；由于基于文件系统，因此只能把快照储存在同一个盘上；快照在刚刚创建的时候不占据任何空间，随着系统本身的文件和快照的差距越来越大，占用的空间也越来越大，但是占用的空间一定不会大于RSYNC类型。</td></tr></tbody></table><p>按照页面向导操作即可，其中定期快照需要启用<code>cron</code>服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl <span class="hljs-built_in">enable</span> --now cronie.service<br></code></pre></td></tr></table></figure><p>或者，根据 <a href="https://wiki.archlinux.org/title/Timeshift">ArchWiki</a> ，安装<a href="https://aur.archlinux.org/packages/timeshift-systemd-timer">timeshift-systemd-timer</a><sup>aur</sup>可以替代<code>cron</code>服务。</p><p>此外，<a href="https://aur.archlinux.org/packages/timeshift-autosnap">timeshift-autosnap</a><sup>aur</sup>提供了一个钩子，可以在检测到<code>pacman</code>执行了更新操作时，在更新前创建一份快照。<br>但是注意，使用AUR助手更新时，来自AUR的软件包是逐一打包安装的，因此每有一个AUR包被更新就会创建一个快照；不过不必担心空间不足，两份快照由于绝大部分内容是一致的，因此几乎不会占用额外的空间。</p><div class="note note-info">            <p><a href="https://codeberg.org/racehd/timeshift-autosnap">timeshift-autosnap</a>在0.10.0版本增加了一个功能，现在每当触发自动快照时，如果和上一次自动快照间隔的时间很短，就不会进行本次自动快照。<br>这个间隔时间默认为18小时，可以通过修改配置文件调整，修改方法参见<a href="https://codeberg.org/racehd/timeshift-autosnap">上游文档</a>。</p>           </div><h3 id="btrfs快照"><a href="#btrfs快照" class="headerlink" title="btrfs快照"></a>btrfs快照</h3><ul><li>配置btrfs快照之后，检查<code>/etc/fstab</code>文件，如果存在<code>subvolid</code>项则去除：回滚快照后<code>subvolid</code>会发生变化，这一项的存在会导致系统盘无法正确挂载；只靠<code>subvol</code>名称已经足以让系统分辨子卷；  <div class="note note-info">            <p>在2025.02.01镜像中，<code>genfstab</code>脚本生成的fstab文件已经不再会包含<code>subvolid</code>项。</p>           </div></li><li>由于<code>/boot</code>分区并不属于btrfs文件系统的一部分，因此回滚快照的时候不会跟着回滚；有时候这会导致<code>/boot</code>分区的initramfs内核版本与系统linux内核版本不一致，从而导致进不去系统。  <div class="note note-info">            <p>内核日志会输出<code>Failed to mount /boot</code>一类的日志，如果使用<code>journalctl -xb</code>查看具体日志，会看到<code>unknown filesystem type &#39;vfat&#39; error</code>报错。通常情况下，这会导致无法进入图形界面，而是只能启动到emergency mode。</p>           </div>  解决办法是，在回滚快照后，重启计算机之前，立即执行以下两项中的一项：  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">mkinitcpio -P <span class="hljs-comment">#保守的选择：重新生成全部initramfs，这样就与系统内一致了；也可以使用mkinitcpio -p &lt;内核名,例如linux&gt;只生成其中一个内核的，然后启动的时候选择这个内核</span><br>pacman -Sy &lt;内核,例如linux&gt; &lt;相应的头文件,例如linux-headers&gt; <span class="hljs-comment">#激进的选择：先把其中一个内核更新了，这会在更新的同时为这个内核生成initramfs；然后启动的时候选择这个内核</span><br></code></pre></td></tr></table></figure>  如果重启的手速太快，忘记执行这些，导致进不去系统的话，就只能用liveCD启动，挂载硬盘，然后<code>arch-chroot</code>进去执行。不过这时由于<code>/etc/fstab</code>是完好无损的，可以只挂载根分区，chroot后执行<code>mount -a</code>一次性挂载上所有分区。</li><li>grub-btrfs<sup>pkg</sup>提供了一项独特的功能：开机的时候直接引导进入快照。<br>  如果安装了<a href="https://aur.archlinux.org/packages/timeshift-autosnap">timeshift-autosnap</a><sup>aur</sup>，则这个包会在自动创建快照的同时，更新grub配置文件。<br>  手动增删快照后也要刷新一次grub配置以让启动列表中的快照列表与实际的快照一致。<br>  启动到快照的时候，同样要注意<code>/boot</code>分区挂载的问题；不同的是，<code>/boot</code>分区只有一份，不可能让其中的initramfs放弃跟随系统盘，去跟快照中一致。因此，要直接启动到快照，必须确保目标快照中的内核版本与当前系统的内核版本一致。<br>  一个保险的方法是在执行<code>pacman -Syu</code>之前，先单独更新一个内核（例如<code>pacman -Sy linux-lts linux-lts-headers</code>）；然后重启到这个内核；<br>  或者在滚的时候忽略其中一个内核的更新（例如<code>pacman -Syu --ignore linux-lts,linux-lts-headers</code>）；重启后确认无问题再启动到这个内核。  <div class="note note-success">            <p>通常其实没必要这么谨慎。Arch Linux现在的稳定性已经很强，滚挂进不去系统已经是非常少见的了；而且即使挂了，在手头有USB启动盘的情况下，启动到liveCD然后使用<code>arch-chroot</code>进入系统执行修复永远是最直接最有效的方法。</p>           </div></li><li>如果最初分区的时候，不为<code>/boot</code>单独分区，则以上两个问题均不存在；但是：<blockquote><p><code>GRUB_DEFAULT=saved</code>和<code>GRUB_SAVEDEFAULT=true</code>仅当 &#x2F;boot 不是 btrfs 文件系统的时候才能用，因为 GRUB 没法对 btrfs 进行写入操作。但它会生成一个容易误导人的错误信息：”sparse file not allowed. Press any key to continue.”</p></blockquote></li></ul><div class="note note-danger">            <p>如果一个子卷内存在活动的swap文件，btrfs会拒绝对这个子卷快照；所以务必确保为swap文件单独准备了一个子卷（例如<code>@swap</code>）</p>           </div><div class="note note-info">            <p>关于<a href="https://codeberg.org/racehd/timeshift-autosnap">timeshift-autosnap</a>和<a href="https://github.com/Antynea/grub-btrfs">grub-btrfs</a>的一些杂七杂八的tips:</p> <ol> <li>grub-btrfs提供了一个<code>grub-btrfsd.service</code>服务，可以在创建和删除btrfs快照时，后台静默重新安装grub并更新grub菜单。使用<code>systemctl enable grub-btrfsd</code>即可启用。</li> <li>当grub-btrfs与timeshift配合使用时，由于timeshift每次启动使用的目录都不一样，grub-btrfsd需要使用专门的模式来监控timeshift快照。将<code>--timeshift-auto</code>按照<a href="https://github.com/Antynea/grub-btrfs?tab=readme-ov-file#-using-timeshift-with-systemd">上游文档</a>提供的方法传递给<code>grub-btrfsd.service</code>。</li> <li>timeshift-autosnap的所有配置均由<code>/etc/timeshift-autosnap.conf</code>配置文件控制。<ol> <li>自动创建的快照默认状态下最多保留3个。设置<code>deleteSnapshots=false</code>阻止自动删除旧快照，设置<code>maxSnapshots</code>改变默认值；</li> <li>timeshift-autosnap会自动检测<code>grub-btrfsd.service</code>是否在后台运行；如果检测到，就会跳过自身的更新grub配置，以防止二者同时运行出现冲突。但是上游仍然建议手动设置<code>updateGrub=false</code>以保险；</li> <li>前面提到“每当触发自动快照时，如果和上一次自动快照间隔的时间很短，就不会进行本次自动快照”，这个时间间隔由<code>minHoursBetweenSnapshots</code>控制，如果设置为0则会每次都执行快照，相当于回退到旧版本的行为；</li> <li>其它参数参看<a href="https://codeberg.org/racehd/timeshift-autosnap">上游文档</a>。</li> </ol> </li> <li>咱的自用配置：不启用<code>grub-btrfsd.service</code>，timeshift-autosnap全默认。这是最简单的方法，只有在手动增删快照的时候需要专门更新grub配置，其它时候都能自动完成。</li> </ol>           </div><h2 id="4-缩短关机等待时间"><a href="#4-缩短关机等待时间" class="headerlink" title="4.缩短关机等待时间"></a>4.缩短关机等待时间</h2><p>有时候关机时可能会出现类似<code>A stop job is running for...(1m30s)</code>这样的提示，这是因为有的进程在关机的时候没有主动停止，需要等待90秒超时强制关闭。<br>90秒的时间太长了，可以缩短到30s；这个时间足以多数应用正常关闭了。</p><div class="note note-info">            <p>Linux Mint 22.1版本把这个时间缩短到了10s”，非常激进。这里附上相应的配置文件，Arch Linux也可以考虑用这种在<code>/etc/systemd/system.conf.d/</code>目录下新建文件的方式，避免直接修改系统文件。</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-cde6ffc9" role="button" aria-expanded="false" aria-controls="collapse-cde6ffc9">        <div class="fold-arrow">▶</div>/etc/systemd/system.conf.d/50_linuxmint.conf      </div>      <div class="fold-collapse collapse" id="collapse-cde6ffc9">        <div class="fold-content">          <figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-comment"># To override these values, create your own file in /etc/systemd/systemd.conf.d/60_custom.conf.</span><br><span class="hljs-comment"># Reload configuration with &quot;sudo systemctl daemon-reload&quot;</span><br><span class="hljs-comment"># Test with &quot;systemctl show&quot;</span><br><br><span class="hljs-section">[Manager]</span><br><br><span class="hljs-comment"># Reduce shutdown timeout from 90s to 10s.</span><br><span class="hljs-comment"># If you rely on important tasks to successfully finish during the shutdown sequence, set the timeout to something greater than 90s.</span><br><span class="hljs-attr">DefaultTimeoutStopSec</span>=<span class="hljs-number">10</span>s<br><br><span class="hljs-comment"># Rationale:</span><br><span class="hljs-comment"># This is done for the following reasons:</span><br><span class="hljs-comment"># - 90s is too long for users to wait (they think it&#x27;s hanging indefinitely, and they eventually just use their power button)</span><br><span class="hljs-comment"># - Cups, minidlna have made this a buggy mess for years now and there&#x27;s no sign of improvement for the near future</span><br><span class="hljs-comment"># - This is a workaround many people have used in Mint, Ubuntu, Arch, Fedora and many other distributions, although reducing the start</span><br><span class="hljs-comment"># timeout is known to possibly affect the journal, no adverse effects were reported after reducing the stop timeout.</span><br><span class="hljs-comment"># - For our audience (desktop users), this makes a lot of sense. It would be different for a server, or a machine which relies on shutdown</span><br><span class="hljs-comment"># scripts to perform important tasks, but for most people this fixes a very important issue and is unlikely to create new ones.</span><br></code></pre></td></tr></table></figure>        </div>      </div>    </div><p>修改方法：编辑<code>/etc/systemd/system.conf</code>，找到<code>DefaultTimeoutStopSec</code>项，取消注释并改为想要的时间（30s）。<br><img src="/img/arch-cinnamon/timeout.png"><br>然后执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl daemon-reload<br></code></pre></td></tr></table></figure><h2 id="5-command-not-found提示"><a href="#5-command-not-found提示" class="headerlink" title="5.command-not-found提示"></a>5.command-not-found提示</h2><p>pkgfile<sup>pkg</sup>可以查询某个命令属于哪个软件包；使用root权限运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S pkgfile <span class="hljs-comment">#安装</span><br>pkgfile -u <span class="hljs-comment">#更新数据库</span><br></code></pre></td></tr></table></figure><p><code>pkgfile</code>可以手动使用，用法参见<a href="https://wiki.archlinux.org/title/Pkgfile">ArchWiki</a><br>也可以在输入未安装的命令时，提示需要安装的包：</p><ol><li>bash<br> 在<code>~/.bashrc</code>中加入一行： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">source</span> /usr/share/doc/pkgfile/command-not-found.bash<br></code></pre></td></tr></table></figure></li><li>zsh<br> 在<code>~/.zshrc</code>中加入一行： <figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh"><span class="hljs-built_in">source</span> /usr/share/doc/pkgfile/command-not-found.zsh<br></code></pre></td></tr></table></figure></li><li>fish<br> 无需额外配置，安装后会被自动调用。</li></ol><h2 id="6-软件包降级"><a href="#6-软件包降级" class="headerlink" title="6.软件包降级"></a>6.软件包降级</h2><p>pacman并不支持包降级，并且arch社区也不对降级包的行为作支持。部分升级是不受支持的。<br>但是，如果有软件包确实出现了问题不得不降级，也有工具提供了这一功能。<br>安装<a href="https://aur.archlinux.org/packages/downgrade">downgrade</a><sup>cn&#x2F;aur</sup></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S downgrade<br></code></pre></td></tr></table></figure><p>然后使用root权限运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">downgrade &lt;想降级的包&gt;<br></code></pre></td></tr></table></figure><p>选择想要降级的版本即可。注意标注的来源，来自<code>/var/cache/pacman/pkg</code>的是本地缓存的包，优先使用；远程包则来自 <a href="https://wiki.archlinux.org/title/Arch_Linux_Archive">Arch Linux Archive</a> ，没有镜像源，可能需要下载比较长时间。官方提供了 <a href="https://asia.archive.pkgbuild.com/">亚洲</a> , <a href="https://europe.archive.pkgbuild.com/">欧洲</a> , <a href="https://america.archive.pkgbuild.com/">美洲</a> 三个地区的镜像，可以手动下载然后用<code>pacman -U</code>安装，可能会稍微快一点。</p><p>Arch Linux还支持把整个系统完全降级到某个时间点的状态。有关降级的更多内容，参见<a href="https://wiki.archlinux.org/title/Downgrading_packages">ArchWiki</a></p><div class="note note-info">            <p>这些方法只支持降级官方源中的软件包；第三方源（如archlinuxcn源或者arch4edu源）的软件包，远程源似乎并没有旧版本的存档；因此如果缓存里面存有，就可以提取缓存降级（从<code>/var/cache/pacman/pkg</code>目录）；如果没有就是没法降级了。AUR中的包可以尝试使用<code>git reset</code>回退到之前的某个版本然后手动<code>makepkg</code>打个旧版本的包降级。</p>           </div><h2 id="7-镜像源列表优化"><a href="#7-镜像源列表优化" class="headerlink" title="7.镜像源列表优化"></a>7.镜像源列表优化</h2><p>目前pacman源仅有唯一的cernet源；pacman支持配置多源，因此可以优化一下：</p><ol><li>官方源<br> sudo执行下面命令来获得一份位于中国大陆，且使用HTTPS协议的镜像站列表，然后覆盖 <code>/etc/pacman.d/mirrorlist</code> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">curl -L <span class="hljs-string">&#x27;https://archlinux.org/mirrorlist/?country=CN&amp;protocol=https&#x27;</span> -o /etc/pacman.d/mirrorlist<br></code></pre></td></tr></table></figure> 之后编辑<code>/etc/pacman.d/mirrorlist</code>，做两件事：将想要使用的镜像源取消注释，并且排序。<br> 咱的喜好：cernet最先，然后各个企业镜像。（别碰163源，慢过蜗牛）<br> 另外还有少量镜像源未被收录在官方镜像列表： <figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">Server = https:<span class="hljs-regexp">//</span>repo.huaweicloud.com<span class="hljs-regexp">/archlinux/</span><span class="hljs-variable">$repo</span><span class="hljs-regexp">/os/</span><span class="hljs-variable">$arch</span><span class="hljs-comment">#华为云</span><br>Server = https:<span class="hljs-regexp">//mi</span>rrors.cloud.tencent.com<span class="hljs-regexp">/archlinux/</span><span class="hljs-variable">$repo</span><span class="hljs-regexp">/os/</span><span class="hljs-variable">$arch</span><span class="hljs-comment">#腾讯云</span><br></code></pre></td></tr></table></figure></li><li>archlinuxcn源和arch4edu源<br> 安装archlinuxcn-mirrorlist-git<sup>cn</sup>和mirrorlist.arch4edu<sup>edu</sup>，然后编辑<code>/etc/pacman.d/archlinuxcn-mirrorlist</code>和<code>/etc/pacman.d/mirrorlist.arch4edu</code>，将想要使用的镜像源取消注释，并且排序。<br> 最后编辑<code>/etc/pacman.conf</code>，把原先这里的镜像源注释掉，改为： <figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-section">[archlinuxcn]</span><br><span class="hljs-attr">Include</span> = /etc/pacman.d/archlinuxcn-mirrorlist<br><br><span class="hljs-section">[arch4edu]</span><br><span class="hljs-attr">Include</span> = /etc/pacman.d/mirrorlist.arch4edu<br></code></pre></td></tr></table></figure> <img src="/img/arch-cinnamon/mirror.png"><br> 最后，使用两个y强制刷新镜像源列表：<code>sudo pacman -Syyu</code></li></ol><hr><p>到这里咱已经把目前能想到的，日用的Arch Linux系统必需的配置（除了显卡驱动）之外都配置完成了。由于目前还是在虚拟机上面整，暂时没有条件配置显卡驱动；不知道是虚拟机的问题还是其它什么问题，休眠目前也没配置成功。这两个问题以后在物理机上解决了另开一篇。<br>显卡驱动参考<a href="#3-%E5%85%B3%E4%BA%8E%E6%98%BE%E5%8D%A1%E9%A9%B1%E5%8A%A8">前述章节</a>根据显卡品牌不同分列的wiki；<br>休眠配置参考 <a href="https://wiki.archlinux.org/title/Power_management/Suspend_and_hibernate#Hibernation">ArchWiki</a> 。<br>2025年4月6日更新：休眠在Virtualbox虚拟机配置完成，参见<a href="/2025/04/06/arch-hibernate/" title="Arch Linux配置休眠">这里</a><br>2025年6月8日更新：显卡驱动配置完成，参见<a href="#3-5-%E5%A4%A7%E6%88%98NVIDIA%E9%A9%B1%E5%8A%A8-2025%E5%B9%B46%E6%9C%888%E6%97%A5%E6%9B%B4%E6%96%B0">3.5节</a>；休眠在物理机上验证正常工作。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Windows+Arch Linux双系统基本安装</title>
    <link href="/2025/02/10/windows-with-arch/"/>
    <url>/2025/02/10/windows-with-arch/</url>
    
    <content type="html"><![CDATA[<div class="note note-success">            <p>2025年2月10日在VMware Workstation 17上测试，暂未将配置搬上物理机，但是步骤是相同的。</p>           </div><div class="note note-info">            <p>2025年6月8日在物理机安装成功，步骤仍然可用。</p>           </div><div class="note note-warning">            <p>只介绍UEFI&#x2F;GPT启动方式，不建议在BIOS&#x2F;MBR平台上安装双系统，引导项极易打架；<br>（不过虚拟机单Linux安装的话BIOS&#x2F;MBR分区简单很多）</p>           </div><div class="note note-info">            <p>本文引用ArchWiki链接时同时提供中文和英文链接，建议优先阅读英文版，中文翻译可能存在滞后性；</p>           </div><h1 id="1-准备工作"><a href="#1-准备工作" class="headerlink" title="1. 准备工作"></a>1. 准备工作</h1><h2 id="1-检查是否为UEFI启动："><a href="#1-检查是否为UEFI启动：" class="headerlink" title="(1)检查是否为UEFI启动："></a>(1)检查是否为UEFI启动：</h2><div class="note note-info">            <p>Win11仅支持UEFI启动，无需检查；<br>开箱状态下为预装Win10，或者空盘安装Win10的通常也是UEFI启动；<br>只有从Win7升级上Win10的，传统BIOS启动的概率才比较大。</p>           </div><p>按下<code>Win+R</code>组合键，输入<code>msinfo32</code>确定，在打开的窗口中找到<code>BIOS模式</code>一条，若为UEFI则是UEFI启动，若为Legacy则是传统BIOS启动。<br><img src="/img/windows-with-arch/uefi.png"><br>如果是传统BIOS启动的话，理论上进入PE环境下，把硬盘分区表从MBR转为GPT，划分一个EFI分区出来，再修复一下引导，就能无损转换；如果没有重要的数据，还是重装更方便吧</p><h2 id="2-介质准备："><a href="#2-介质准备：" class="headerlink" title="(2)介质准备："></a>(2)介质准备：</h2><p>物理机安装需要一枚容量至少2GB的U盘，建议4G以上；支持USB3.0以上速率的最好，没有问题也不大。</p><div class="note note-danger">            <p>务必备份好U盘的资料，U盘在稍后将被格式化；</p>           </div><h2 id="3-检查bitlocker状态："><a href="#3-检查bitlocker状态：" class="headerlink" title="(3)检查bitlocker状态："></a>(3)检查bitlocker状态：</h2><p>双系统平台上个人强烈不建议使用bitlocker。<br>检查bitlocker状态的最简单的方法是打开“此电脑”，观察硬盘图标。<br>如下图所示，有加密的磁盘会在图标右侧显示出锁的标志，就像图中D盘这样；未加密的则会像图中C盘这样；<br><img src="/img/windows-with-arch/bitlocker.png"></p><p>如果一定需要bitlocker加密硬盘，为了减少麻烦，建议留一个未被加密的分区，用于在双系统间共享数据；并且请提前<a href="https://support.microsoft.com/zh-cn/windows/%E6%9F%A5%E6%89%BE-bitlocker-%E6%81%A2%E5%A4%8D%E5%AF%86%E9%92%A5-6b71ad27-0b89-ea08-f143-056f5ab347d6">根据Windows官方文档备份bitlocker恢复密钥</a> <br><br>关闭bitlocker的方式：</p><ol><li>在开始菜单中搜索bitlocker：<br> <img src="/img/windows-with-arch/search-bitlocker.png"></li><li>点击关闭即可：<br> <img src="/img/windows-with-arch/disable-bitlocker.png"></li></ol><div class="note note-warning">            <p>bitlocker解密可能会花费极长时间（数个小时）；在此期间不影响电脑正常使用，所以可以继续执行其它准备工作，但是在彻底解密完成之前最好不要关机。<br>解密的过程中应该是有进度条的，但也可以通过命令行检查解锁的进度:<br><br>使用管理员权限打开命令提示符或者Powershell。<br><br>cmd输入：</p> <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmd">manage-bde -status<br></code></pre></td></tr></table></figure> <p>输出类似这样（关键词：<code>转换状态:完全解密</code> <code>已加密百分比:0.0%</code>）说明完全解密了：<br><img src="/img/windows-with-arch/cmd-unlocked.png"><br><br>Powershell输入：</p> <figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs Powershell"><span class="hljs-built_in">Get-BitLockerVolume</span><br></code></pre></td></tr></table></figure> <p>输出类似这样（关键词：<code>FullyDecrypted</code> <code>Encryption Percentage 0</code>）说明完全解密了：<br><img src="/img/windows-with-arch/psh-unlocked.png"></p>           </div><h2 id="4-下载："><a href="#4-下载：" class="headerlink" title="(4)下载："></a>(4)下载：</h2><ol><li>从 <a href="https://archlinux.org/download/">archlinux官网</a> 下载系统镜像。<br> 站点提供bt种子下载和镜像站下载，若通过镜像站下载，则推荐校验sha256与pgp签名；     <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-b18702a1" role="button" aria-expanded="false" aria-controls="collapse-b18702a1">        <div class="fold-arrow">▶</div>校验sha256的方式      </div>      <div class="fold-collapse collapse" id="collapse-b18702a1">        <div class="fold-content">          <p>如果有一个能运行bash的环境，例如Git Bash，进入bash，执行如下命令：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sha256sum</span> /path/to/***.iso<br></code></pre></td></tr></table></figure> <p>如果没有，进入cmd，执行如下命令：</p> <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmd">certutil -hashfile /<span class="hljs-built_in">path</span>/to/***.iso sha256<br></code></pre></td></tr></table></figure> <p>将输出与arch官网对比，一致说明文件完好无损；</p>         </div>      </div>    </div>     <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-8af3cad2" role="button" aria-expanded="false" aria-controls="collapse-8af3cad2">        <div class="fold-arrow">▶</div>校验签名的方式      </div>      <div class="fold-collapse collapse" id="collapse-8af3cad2">        <div class="fold-content">          <p>如果有一个能运行bash的环境，例如Git Bash，进入bash，直接复制Arch官网的命令(以2025.01.01版为例)：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">gpg --auto-key-locate clear,wkd -v --locate-external-key pierre@archlinux.org<br>gpg --keyserver-options auto-key-retrieve --verify archlinux-2025.01.01-x86_64.iso.sig archlinux-2025.01.01-x86_64.iso<br></code></pre></td></tr></table></figure> <p>输出类似这样（注意高亮的地方）说明签名一致，镜像未被篡改：<br><img src="/img/windows-with-arch/pgp.png"><br>如果没有bash环境，可以尝试<a href="https://gpg4win.org/index.html">Gpg4win</a></p>         </div>      </div>    </div></li><li>下载刻录U盘的工具，强烈推荐<a href="https://www.ventoy.net/cn/download.html">Ventoy</a> ，想在安装的时候使用图形化分区工具偷懒的话这是唯一的选择；</li><li>图形化分区工具<a href="https://gparted.org/download.php">gparted</a><br> 可选，咱现在熟练之后感觉直接走正统的ArchLinux分区方式(<code>fdisk</code>&#x2F;<code>cfdisk</code>)效率还更高，但是新手谨防在命令行中误操作导致数据损失。     <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-4e1637b0" role="button" aria-expanded="false" aria-controls="collapse-4e1637b0">        <div class="fold-arrow">▶</div>因为网络问题gparted下载困难?      </div>      <div class="fold-collapse collapse" id="collapse-4e1637b0">        <div class="fold-content">          <p>   gparted实际上在世界各地有多个下载服务器，如果默认自动选择的服务器下载困难，可以手动更换下载服务器：<br>在5s倒计时结束后，不要立刻下载，此时应该进入了<code>Thank you for downloading GParted</code>页面：<br><img src="/img/windows-with-arch/download-gparted/thk.png"><br>选择后退，立即点击<code>Problems Downloading?</code>，就能弹出一个镜像选择的界面：</p> <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/download-gparted/problems.png"></div><div class="group-image-wrap"><img src="/img/windows-with-arch/download-gparted/mirrors.png"></div></div></div> 默认选择的是位于香港的zenlayer镜像，这个源在咱这下载网速很慢并且还不支持断点续传；用日本的下载服务器可以跑满速度。其它服务器也可以尝试，有其他人说肯尼亚的Liquid Telecom速度也不错。        </div>      </div>    </div></li></ol><h2 id="5-关闭Windows快速启动"><a href="#5-关闭Windows快速启动" class="headerlink" title="(5)关闭Windows快速启动"></a>(5)关闭Windows快速启动</h2><div class="note note-info">            <p>详细信息参考 <a href="https://wiki.archlinux.org/title/Dual_boot_with_Windows#Fast_Startup_and_hibernation">ArchWiki</a> &#x2F; <a href="https://wiki.archlinuxcn.org/wiki/Arch_%2B_Windows_%E5%8F%8C%E7%B3%BB%E7%BB%9F#%E5%BF%AB%E9%80%9F%E5%90%AF%E5%8A%A8%E5%92%8C%E4%BC%91%E7%9C%A0">中文Wiki</a><br>快速启动会导致Windows关机的时候实质变成进入休眠状态，这样在Linux开机的时候就不能挂载已经在Windows中被挂载的硬盘，否则会导致文件系统错误；<br>同样，如果手动休眠了双系统中的任意系统，然后启动了另一个系统，那么在休眠的系统中已经被挂载的硬盘，不应该被挂载在另一个系统上；如果担心自己误操作，同样可以禁用Windows的休眠功能。<br>虽然ArchWiki指出：</p> <blockquote> <p>如果 Windows 和 Linux 使用同一个 EFI 系统分区，如果休眠（或在启用快速启动时关机）Windows 并启动 Linux，或者休眠 Linux 并启动 Windows， EFI 系统分区可能遭到破坏。</p> </blockquote> <p>但是咱这么干过，EFI分区没出问题；只是在Linux上面挂载的Windows硬盘出现了文件系统错误，需要在Windows上使用<code>chkdsk</code>修复</p>           </div><ol><li>进入控制面板；如果找不到入口，使用开始菜单中的搜索功能；然后选择电源选项：<br> <img src="/img/windows-with-arch/control.png"></li><li>左侧的选择功能任选一个进入：<br> <img src="/img/windows-with-arch/poweroption.png"></li><li>更改当前不可用的设置，然后取消勾选“启用快速启动”；休眠是否关闭随意，不过感觉笔记本用户应该不需要休眠？最后记得保存修改。<br> <img src="/img/windows-with-arch/disablefastboot.png"></li></ol><h2 id="6-将Windows的硬件时间标准改为UTC"><a href="#6-将Windows的硬件时间标准改为UTC" class="headerlink" title="(6)将Windows的硬件时间标准改为UTC"></a>(6)将Windows的硬件时间标准改为UTC</h2><p>在任意位置新建任意文本文档，输入以下内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs reg">Windows Registry Editor Version 5.00<br><br>[HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\TimeZoneInformation]<br>&quot;RealTimeIsUniversal&quot;=dword:00000001<br></code></pre></td></tr></table></figure><p>将后缀从<code>.txt</code>改为<code>.reg</code>双击执行导入注册表。</p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-7a643cb4" role="button" aria-expanded="false" aria-controls="collapse-7a643cb4">        <div class="fold-arrow">▶</div>碎碎念：这样做的理由      </div>      <div class="fold-collapse collapse" id="collapse-7a643cb4">        <div class="fold-content">          <p>硬件时间标准目前有两套方案: </p> <ol> <li>Local: 直接把硬件时间设为本地时间，然后系统显示的时间与硬件时间同步；</li> <li>UTC: 把硬件时间固定设为UTC时间，然后系统根据时区设置，将硬件时间转换为本地时间。</li> </ol> <p>Windows默认使用1，而Linux默认使用2，两者必须统一否则硬件时间会乱套；<br>对于Ubuntu等常见的发行版，其会在检测到硬盘存在Windows时，自动将Linux的时间标准也改成1，从而避免修改Windows注册表；<br>而 <a href="https://wiki.archlinux.org/title/Dual_boot_with_Windows#Time_standard">ArchWiki</a> &#x2F; <a href="https://wiki.archlinuxcn.org/wiki/Arch_%2B_Windows_%E5%8F%8C%E7%B3%BB%E7%BB%9F#%E6%97%B6%E9%97%B4%E6%A0%87%E5%87%86">中文Wiki</a> 更建议将Windows设为2，这样的目的主要是为了在使用夏令时&#x2F;冬令时的时区，切换时同步次数混乱，导致时间快&#x2F;慢1小时；<br>对北京时区来说无影响，但是如果将Linux的时间标准设为1，Linux的时间管理进程会给出警告，并采取较为消极的联网时间同步策略。<br>所以咱觉得还是统一为UTC更好。</p>         </div>      </div>    </div><h2 id="7-了解至少一种命令行文本编辑器的使用方法"><a href="#7-了解至少一种命令行文本编辑器的使用方法" class="headerlink" title="(7)了解至少一种命令行文本编辑器的使用方法"></a>(7)了解至少一种命令行文本编辑器的使用方法</h2><p>ArchLinux的安装镜像里面提供了3种文本编辑器，分别是 <a href="https://man.archlinux.org/man/mcedit.1">mcedit</a> , <a href="https://wiki.archlinux.org/title/Nano#Usage">nano</a> 和 <a href="https://wiki.archlinux.org/title/Vim#Usage">vim</a> 。至少了解其中一种的使用方式，推荐vim，下文的安装教程中，需要编辑文件时，咱会给出咱偏好的vim操作。</p><h2 id="8-为Linux系统腾出一块空闲的硬盘分区"><a href="#8-为Linux系统腾出一块空闲的硬盘分区" class="headerlink" title="(8)为Linux系统腾出一块空闲的硬盘分区"></a>(8)为Linux系统腾出一块空闲的硬盘分区</h2><p>按下<code>Win+X</code>组合键，选择磁盘管理。<br>采用Windows和Linux装在同一块硬盘上不同分区，然后共享EFI分区的方案。<br>这里以虚拟机中新安装的Windows10 22H2版为例，如果分了C和D两个分区，分区结构应该类似左图；在D盘上右键压缩卷，腾出一定数量的空闲空间给Linux使用，不要新建分区，不要格式化。</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/before-part.png"></div><div class="group-image-wrap"><img src="/img/windows-with-arch/after-part.png"></div></div></div><div class="note note-info">            <p>实际分区的情况可能存在差异；例如：</p> <ol> <li>Win10 LTSC 2019 的恢复分区会在硬盘的开始而非末尾；</li> <li>Win10 LTSC 2021 没有恢复分区；</li> <li>对于部分使用过一段时间的设备，以及一些出厂预装Windows的设备，如果已经提前分过了区，可能C盘和其它数据分区中间会间隔另一个恢复分区。</li> </ol>           </div><div class="note note-info">            <p>建议至少留出一个非系统分区作为双系统间共享数据的分区，防止在Linux直接挂载Windows系统盘时因为误操作或者Linux的灵车NTFS驱动导致损坏Windows系统；<br>如果有多块硬盘，直接拿其它硬盘共享也行，否则最好分一下CD两个分区，D盘作为共享数据盘。</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-af3d30ad" role="button" aria-expanded="false" aria-controls="collapse-af3d30ad">        <div class="fold-arrow">▶</div>碎碎念：对分区位置的考量      </div>      <div class="fold-collapse collapse" id="collapse-af3d30ad">        <div class="fold-content">          <p>无论实际的分区情况是什么，如果硬盘最后有恢复分区，就把Linux占据的分区放在恢复分区的前面；否则把Linux放在硬盘的末尾。这样做的考量有：</p> <ol> <li><p>减少对Windows磁盘的操作：从数据盘右侧压缩一块分区出来，使用自带的磁盘管理就能完成，速度快还能减少数据损失的风险；</p> </li> <li><p>调整容量方便：考虑3种需要调整分区大小的情况：</p> <ol> <li>Windows的C盘&#x2F;数据盘空间不足：直接用Diskgenius等分区工具从数据盘&#x2F;C盘拿空间，不操作Linux分区；</li> <li>Windows空间不足：向右缩小Linux分区；</li> <li>Linux空间不足：向左缩小Windows分区；</li> </ol> <p> 这三种情况都能最大限度的减小操作成本。</p> </li> </ol>         </div>      </div>    </div><h2 id="9-刻录启动U盘"><a href="#9-刻录启动U盘" class="headerlink" title="(9)刻录启动U盘"></a>(9)刻录启动U盘</h2><p>解压ventoy，启动<code>ventoy2disk.exe</code>；<br>插入准备好的U盘，选择该U盘，分区表选择GPT(对UEFI的兼容性更好)，格式随意，点击安装；</p><div class="note note-danger">            <p>将会清空U盘上的所有数据！</p>           </div><p>然后将archlinux和gparted的两个iso文件都直接放进U盘即可。</p><h2 id="10-准备BIOS"><a href="#10-准备BIOS" class="headerlink" title="(10)准备BIOS"></a>(10)准备BIOS</h2><p>重启计算机，进入bios，关闭安全启动(Secure Boot): archlinux并不支持安全启动，如果确实需要这个功能，请在安装完archlinux之后，参考 <a href="https://wiki.archlinux.org/title/Unified_Extensible_Firmware_Interface/Secure_Boot#Implementing_Secure_Boot">ArchWiki</a> &#x2F; <a href="https://wiki.archlinuxcn.org/wiki/UEFI/%E5%AE%89%E5%85%A8%E5%90%AF%E5%8A%A8#%E5%AE%9E%E6%96%BD%E5%AE%89%E5%85%A8%E5%90%AF%E5%8A%A8">中文Wiki</a> 自己准备签名文件自行配置（属高阶操作，不介绍，因为咱也不会）</p><div class="note note-warning">            <p>如果Windows系统盘开启了bitlocker，在关闭安全启动后，重启进入Windows时可能会要求输入bitlocker密钥。</p>           </div><h1 id="2-分区"><a href="#2-分区" class="headerlink" title="2. 分区"></a>2. 分区</h1><p>这里使用前述准备好的GParted工具进行分区，图形化操作的好处是不易误操作；<br>在ArchLinux命令行中分区直接<a href="#3-%E8%BF%9B%E5%85%A5Arch%E5%AE%89%E8%A3%85%E4%BB%8B%E8%B4%A8%E5%AE%89%E8%A3%85">进入下一步</a>。</p><h2 id="1-启动进入GParted界面"><a href="#1-启动进入GParted界面" class="headerlink" title="(1)启动进入GParted界面"></a>(1)启动进入GParted界面</h2><ol><li>关闭电脑，然后重新开机并从U盘启动（根据电脑品牌，进入启动菜单boot menu的按键可能是F12,Delete,Esc或者其它）。 <div class="note note-info">            <p>部分设备开机状态直接重启可能无法在启动菜单中正确识别到U盘，所以建议是使用关机再手动开机的方式。</p>           </div></li><li>在Ventoy操作菜单中，选择gparted的镜像，然后<code>Boot in normal mode</code>即可： <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/part/ventoy-gparted.png"></div><div class="group-image-wrap"><img src="/img/windows-with-arch/ventoy-normal.png"></div></div></div></li><li>选择gparted的启动选项:<br> <img src="/img/windows-with-arch/part/gparted-uefi.png"> <div class="note note-info">            <p>如果启动界面是这样的，说明进BIOS模式了，没进UEFI：<br><img src="/img/windows-with-arch/part/gparted-bios.png"></p>           </div> 通常来说，默认选择第一项就行；<br> 如果对U盘的稳定性没有信心，选择第2项，将GParted加载进内存再运行，启动时间会稍慢一些，但是运行更稳定。</li><li>接下来依次处理几个选单: <ol><li>键盘布局，直接默认，回车:<br> <img src="/img/windows-with-arch/part/keymap.png"></li><li>语言，输入26以选择简体中文，回车:<br> <img src="/img/windows-with-arch/part/locale.png"></li><li>图形选项，直接默认启动图形界面，回车:<br> <img src="/img/windows-with-arch/part/launch.png"><br> 稍微等待片刻之后就进入了gparted的图形界面。</li></ol></li></ol><h2 id="2-在GParted中完成分区"><a href="#2-在GParted中完成分区" class="headerlink" title="(2)在GParted中完成分区"></a>(2)在GParted中完成分区</h2><p>在空闲部分从左到右建立3个分区：<br><img src="/img/windows-with-arch/part/before.png"></p><table><thead><tr><th>大小</th><th>格式化为</th><th>挂载点</th></tr></thead><tbody><tr><td>除下面两个分区外的所有空间</td><td>btrfs</td><td>&#x2F;</td></tr><tr><td>适量，这里示例2G</td><td>linux swap</td><td>[SWAP]</td></tr><tr><td>1G</td><td>ext4&#x2F;fat32</td><td>&#x2F;boot</td></tr></tbody></table><p><img src="/img/windows-with-arch/part/parting.png"></p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-9e67c2e5" role="button" aria-expanded="false" aria-controls="collapse-9e67c2e5">        <div class="fold-arrow">▶</div>为什么这样分区?      </div>      <div class="fold-collapse collapse" id="collapse-9e67c2e5">        <div class="fold-content">          <ol> <li>主目录使用btrfs: btrfs具有很多优良的性质：透明压缩(节省空间),子卷(把根目录和home直接用子卷管理，既能隔离，又防止出现系统分区和home分区一个容量不够的时候从另一个分区挪的尴尬),快照(图形化快照工具timeshift除了基于全盘复制的RSYNC之外，只支持创建btrfs文件系统的快照)<br> btrfs的介绍可以参考<a href="https://arch.icekylin.online/guide/advanced/btrfs.html">这篇文章</a> ,原文使用<a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh">CC BY-SA 4.0</a> 许可，因此不作引用；</li> <li>使用swap分区而非swap文件：swap分区配置起来相对更加简便，如果需要配置休眠，swap分区的配置难度更低；尤其是btrfs的swap文件，配置的时候需要额外注意。参考 <a href="https://wiki.archlinux.org/title/Btrfs#Swap_file">ArchWiki-Btrfs</a> &#x2F; <a href="https://wiki.archlinuxcn.org/wiki/Btrfs#%E4%BA%A4%E6%8D%A2%E6%96%87%E4%BB%B6">中文Wiki</a> 以及 <a href="https://wiki.archlinux.org/title/Power_management/Suspend_and_hibernate#Hibernation">ArchWiki-休眠</a> &#x2F; <a href="https://wiki.archlinuxcn.org/wiki/%E7%94%B5%E6%BA%90%E7%AE%A1%E7%90%86/%E6%8C%82%E8%B5%B7%E4%B8%8E%E4%BC%91%E7%9C%A0#%E4%BC%91%E7%9C%A0">中文Wiki</a></li> <li>/boot目录单独分区：咱希望安装完双系统之后，每次开机时grub引导可以记住咱上一次选择的选项，而grub无法写入btrfs格式的分区；<br> 如果&#x2F;boot目录不单独分区，也有一条好处：快照可以把&#x2F;boot目录包含进去，回滚时可以一起回滚，防止出现&#x2F;boot分区内的内核版本与系统实际内核版本不匹配; 这可能导致回滚快照时，如果不进行额外的操作，可能会出现进不去系统的情况。按照现行的分区方案，以后配置快照的时候需要单独考虑一下这个问题，到那时再说</li> <li>关于&#x2F;boot分区的格式：fat32理论上具有更好的兼容性，而ext4更多的其实是防止隔壁的Windows乱来</li> <li>关于&#x2F;boot分区的大小：咱习惯安装3份内核，在安装了<code>linux-firmware</code>固件包后，默认状态下，在虚拟机中每份内核会在&#x2F;boot分区产生约100~150MB的initramfs镜像，物理机由于<code>autodetect</code>机制会探测到比虚拟机更多的硬件，这个数值会略大一点；但是1GB也完全够用了。如果根据<a href="https://wiki.archlinux.org/title/Mkinitcpio#COMPRESSION">ArchWiki</a> &#x2F;<a href="https://wiki.archlinuxcn.org/wiki/Mkinitcpio#%E5%8E%8B%E7%BC%A9%E6%96%B9%E5%BC%8F(COMPRESSION)">中文Wiki</a> 的方法把压缩开到最极端，占用的总空间会从约450MB下降到约320MB，似乎意义并不大。<br> 2025年6月8日更新：物理机boot分区主要文件大小：<br> <img src="/img/windows-with-arch/boot.png"><br> 2025年7月6日更新：即使完全关闭initramfs的压缩(在<code>/etc/mkinitcpio.conf</code>中使用<code>COMPRESSION=&quot;cat&quot;</code>)，大小也仅增加一点点(如下图)，但是节约了压缩和解压的时间，对于CPU性能弱的机器会有一定帮助<br> <img src="/img/windows-with-arch/boot-update.png"><br> 2025年11月5日更新：mkinitcpio发布v40版本，默认设置下不再生成fallback镜像，对于6.17版本内核，占用在默认的zstd压缩下约为120MB，在关闭压缩下约为130MB。因此&#x2F;boot分区宽松很多，500MB都可能完全够用。</li> </ol>         </div>      </div>    </div><p>然后确认，等待分区完成。最终的情况应该是这样的：</p><table><thead><tr><th>设备名</th><th>分区描述</th><th>文件系统</th><th>挂载点</th></tr></thead><tbody><tr><td>&#x2F;dev&#x2F;nvme0n1p1</td><td>EFI分区</td><td>fat32</td><td>&#x2F;efi</td></tr><tr><td>&#x2F;dev&#x2F;nvme0n1p2</td><td>MSR分区</td><td>msr</td><td></td></tr><tr><td>&#x2F;dev&#x2F;nvme0n1p3</td><td>Windows C:</td><td>ntfs</td><td></td></tr><tr><td>&#x2F;dev&#x2F;nvme0n1p4</td><td>Windows D:</td><td>ntfs</td><td></td></tr><tr><td>&#x2F;dev&#x2F;nvme0n1p6</td><td>Linux系统盘</td><td>btrfs</td><td>&#x2F;</td></tr><tr><td>&#x2F;dev&#x2F;nvme0n1p7</td><td>Linux swap</td><td>swap</td><td>[SWAP]</td></tr><tr><td>&#x2F;dev&#x2F;nvme0n1p8</td><td>Linux boot</td><td>ext4</td><td>&#x2F;boot</td></tr><tr><td>&#x2F;dev&#x2F;nvme0n1p5</td><td>Windows恢复分区</td><td>ntfs</td><td></td></tr></tbody></table><p><img src="/img/windows-with-arch/part/after.png"></p><div class="note note-info">            <p>有大量的发行版都会选择把EFI分区挂载在<code>/boot/efi</code>，但是根据<a href="https://wiki.archlinux.org/title/EFI_system_partition#Typical_mount_points">ArchWiki</a> &#x2F;<a href="https://wiki.archlinuxcn.org/wiki/EFI_%E7%B3%BB%E7%BB%9F%E5%88%86%E5%8C%BA#%E5%85%B8%E5%9E%8B%E6%8C%82%E8%BD%BD%E7%82%B9">中文Wiki</a> , 现在更建议直接挂载在<code>/efi</code>，<code>/boot/efi</code>不再推荐。</p>           </div><p>完成分区后退出GParted，重启进入Arch安装介质，准备开始安装系统。</p><h1 id="3-进入Arch安装介质安装"><a href="#3-进入Arch安装介质安装" class="headerlink" title="3. 进入Arch安装介质安装"></a>3. 进入Arch安装介质安装</h1><div class="note note-info">            <p>Tips: 善用tab自动补全命令不仅能节省输入时间，还能防止误输。</p>           </div><h2 id="0-联网准备"><a href="#0-联网准备" class="headerlink" title="(0)联网准备"></a>(0)联网准备</h2><p>有线网和虚拟机无需额外配置，无线网需要确保BIOS中网卡未被禁用，WiFI的名称和密码均只由英文字母,数字和键盘上能直接输入的符号组成，否则无法连接。安装过程需要全程联网。</p><h2 id="1-启动Arch-Linux安装介质"><a href="#1-启动Arch-Linux安装介质" class="headerlink" title="(1)启动Arch Linux安装介质"></a>(1)启动Arch Linux安装介质</h2><p>从U盘启动到Arch Linux:</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/install/ventoy.png"></div><div class="group-image-wrap"><img src="/img/windows-with-arch/ventoy-normal.png"></div></div></div><p>看到这个界面后回车：<br><img src="/img/windows-with-arch/install/boot-uefi.png"></p><div class="note note-info">            <p>如果看到的启动界面是这样的，并且启动项的结尾是BIOS，说明是用传统BIOS启动的，没有启动到UEFI模式；立即关机（或者启动到安装介质后输入<code>poweroff</code>关机），然后去BIOS中更改启动类型为<code>UEFI only</code><br><img src="/img/windows-with-arch/install/boot-bios.png"></p>           </div><h2 id="1-5-为当前登录的实例设置密码"><a href="#1-5-为当前登录的实例设置密码" class="headerlink" title="(1.5)为当前登录的实例设置密码"></a>(1.5)为当前登录的实例设置密码</h2><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">passwd<br></code></pre></td></tr></table></figure><p>为root用户设置密码之后，即可从其它设备，以root用户的身份使用ssh远程连接本机。</p><h2 id="2-最后一次验证启动模式"><a href="#2-最后一次验证启动模式" class="headerlink" title="(2)最后一次验证启动模式"></a>(2)最后一次验证启动模式</h2><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh"><span class="hljs-built_in">cat</span> /sys/firmware/efi/fw_platform_size<br></code></pre></td></tr></table></figure><p>如果输出<code>64</code>，说明系统以64位UEFI模式启动，是期望中的情况；<br>如果输出<code>32</code>，说明系统以UEFI模式启动，但是UEFI仅支持32位。这种平台极为少见，仍然可以正常安装，但是可选择的引导程序比较有限，并且如果选用grub作为引导程序的话需要修改部分参数；<br>如果找不到该文件，说明系统以BIOS模式启动，重启更改启动模式为UEFI。</p><h2 id="3-停用reflector服务"><a href="#3-停用reflector服务" class="headerlink" title="(3)停用reflector服务"></a>(3)停用reflector服务</h2><p>reflector会在最新的20个镜像源中测速，并根据速度排序镜像；由于大陆的特殊网络环境，只考虑最新的20个镜像源往往得不到理想的结果，同时它还会覆盖自行配置的源信息，所以先将其禁用：</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs zsh">systemctl stop reflector.service <span class="hljs-comment">#停用</span><br>systemctl status reflector.service <span class="hljs-comment">#复查，然后按q退出</span><br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/stop-reflector.png"></p>    <div class="fold">      <div class="fold-title fold-success collapsed" data-toggle="collapse" href="#collapse-9ec15554" role="button" aria-expanded="false" aria-controls="collapse-9ec15554">        <div class="fold-arrow">▶</div>reflector的效果      </div>      <div class="fold-collapse collapse" id="collapse-9ec15554">        <div class="fold-content">          <p>这里是直接用reflector得到的镜像源列表，20个源一个国内的都没有：<br><img src="/img/windows-with-arch/install/reflector-bad.png"><br>硬要用reflector排序镜像源也是可以的，只需要给它加上亿点点限制并手动运行：</p> <figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">reflector --latest 10 --country China --protocol https --<span class="hljs-built_in">sort</span> rate --save /etc/pacman.d/mirrorlist<br></code></pre></td></tr></table></figure> <p>这个命令依次指定了 最新的10个 地区位于中国 使用https协议 的镜像，按下载速度排序，并把结果覆盖保存在<code>/etc/pacman.d/mirrorlist</code>中。<br>不使用最后的<code>--save</code>参数，可以在控制台先预览输出的结果。<br>这样出来的结果至少都是国内的源，还算可观:<br><img src="/img/windows-with-arch/install/reflector-cn.png"><br>更多的用法参见 <a href="https://man.archlinux.org/man/reflector.1#EXAMPLES">reflector man page</a> 以及 <a href="https://wiki.archlinux.org/title/Reflector">ArchWiki</a> &#x2F; <a href="https://wiki.archlinuxcn.org/wiki/Reflector">中文Wiki</a><br>新安装的系统中并不会默认包含<code>reflector</code>软件包，需要可以自行安装配置。</p>         </div>      </div>    </div><h2 id="4-连接网络"><a href="#4-连接网络" class="headerlink" title="(4)连接网络"></a>(4)连接网络</h2><p>虚拟机和有线网无需额外配置；无线网按照以下步骤联网。</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">iwctl <span class="hljs-comment">#进入iwctl交互式命令行</span><br></code></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs iwc">device list # 列出无线网卡设备名，比如无线网卡看到叫 wlan0<br>station wlan0 scan # 扫描网络<br>station wlan0 get-networks # 列出所有 wifi 网络<br>station wlan0 connect wifi-name # 进行连接，注意这里无法输入中文。wifi-name替换为需要连接的wifi名称。如果wifi需要密码，回车后会提示，输入密码即可<br>exit # 连接成功后退出<br></code></pre></td></tr></table></figure><p>测试是否成功联网:</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">ping www.baidu.com<br></code></pre></td></tr></table></figure><p>按<code>Ctrl+C</code>手动终止<code>ping</code>命令。<br><img src="/img/windows-with-arch/install/ping.png"></p><h2 id="5-校准时间"><a href="#5-校准时间" class="headerlink" title="(5)校准时间"></a>(5)校准时间</h2><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">timedatectl<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/test-time.png"><br><code>NTP service</code>显示为<code>active</code>状态说明时间同步服务正常运行。正确的系统时间对于安装部分软件包是<strong>必须</strong>的。<br>如果没有正常运行(显示为<code>inactive</code>)，则:</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">timedatectl set-ntp 1<br></code></pre></td></tr></table></figure><h2 id="6-分区与格式化"><a href="#6-分区与格式化" class="headerlink" title="(6)分区与格式化"></a>(6)分区与格式化</h2><p>已经使用GParted工具完成分区和格式化的直接跳到<a href="#7-%E5%88%9B%E5%BB%BAbtrfs%E5%AD%90%E5%8D%B7">下一步</a><br>命令行分区工具中<code>fdisk/cfdisk</code>无需配置即自带4k对齐支持，因此推荐使用。<br>分区方案的理由在<a href="#2-%E5%9C%A8GParted%E4%B8%AD%E5%AE%8C%E6%88%90%E5%88%86%E5%8C%BA">GParted分区章节</a>中。</p><div class="note note-danger">            <p>不要用<code>fdisk/cfdisk</code>工具调整已经格式化并且存放有数据的分区大小！这样极可能收获一个数据损坏的分区。<br>调整Windows相关的分区大小（NTFS分区）在Windows下调整（或者用PE）；<br>调整Linux相关的分区大小在Linux下调整（或者用GParted liveCD，可以起类似PE的作用）。</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-8a844849" role="button" aria-expanded="false" aria-controls="collapse-8a844849">        <div class="fold-arrow">▶</div>分区选项一：使用fdisk工具分区      </div>      <div class="fold-collapse collapse" id="collapse-8a844849">        <div class="fold-content">          <p>优点：可调参数多，熟练后操作可以更快。<br>使用<code>lsblk</code>命令，先确认即将分区的硬盘。<br><img src="/img/windows-with-arch/install/lsblk1.png"><br>这里即将分区的盘是<code>/dev/nvme0n1</code>,这时没显示出空闲空间是正常的，这里只显示已经分配的空间。<br>然后进入fdisk:</p> <figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">fdisk /dev/nvme0n1<br></code></pre></td></tr></table></figure> <p><img src="/img/windows-with-arch/install/fdisk-start.png"><br>可以输入<code>m</code>查看所有可用的命令。<br>依次执行以下操作：</p> <ol> <li><code>n</code>新建一个分区；</li> <li>分区号默认，直接回车；</li> <li>起始位置直接回车，自动选择空闲分区的开头；</li> <li>终止位置：输入<code>-3G</code>设定留出3G空闲空间，或者输入<code>+17G</code>设定分区大小为17G。如果该选项留空则是建立分区直到空闲空间的末尾。</li> </ol> <p><img src="/img/windows-with-arch/install/fdisk1.png"><br>然后重复上述操作，新建另外两个分区；<br>7号分区是swap分区，因此打个标签：按<code>t</code>进入修改分区类型，输入<code>7</code>选择7号分区，输入<code>Linux swap</code>设定分区类型。<br>完成后按<code>w</code>写入并退出。<br><img src="/img/windows-with-arch/install/fdisk2.png"><br>最后使用<code>lsblk</code>命令复查一下分区结果：<br><img src="/img/windows-with-arch/install/fdisk-lsblk.png"></p>         </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-e4337b87" role="button" aria-expanded="false" aria-controls="collapse-e4337b87">        <div class="fold-arrow">▶</div>分区选项二：使用cfdisk工具分区      </div>      <div class="fold-collapse collapse" id="collapse-e4337b87">        <div class="fold-content">          <p>优点：有TUI界面，更易操作。<br>使用<code>lsblk</code>命令，先确认即将分区的硬盘。<br><img src="/img/windows-with-arch/install/lsblk1.png"><br>这里即将分区的盘是<code>/dev/nvme0n1</code>,这时没显示出空闲空间是正常的，这里只显示已经分配的空间。<br>然后进入cfdisk:</p> <figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">cfdisk /dev/nvme0n1<br></code></pre></td></tr></table></figure> <p><img src="/img/windows-with-arch/install/cfdisk-start.png"><br>光标定位到<code>Free space</code>处，选择<code>New</code>，输入新建分区的大小（这里是17G），回车，并定位到剩余的<code>Free space</code>；<br><img src="/img/windows-with-arch/install/cfdisk1.png"><br>用同样的方法再新建一个2G的分区，把<code>Type</code>修改为<code>Linux Swap</code>，然后再新建一个占据剩余所有空间的分区；<br><img src="/img/windows-with-arch/install/cfdisk2.png"><br>使用左右方向键选择<code>Write</code>，输入<code>yes</code>回车，然后<code>Quit</code>；<br>最后使用<code>lsblk</code>命令复查一下分区结果：<br><img src="/img/windows-with-arch/install/cfdisk-lsblk.png"></p>         </div>      </div>    </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-55eaed39" role="button" aria-expanded="false" aria-controls="collapse-55eaed39">        <div class="fold-arrow">▶</div>格式化      </div>      <div class="fold-collapse collapse" id="collapse-55eaed39">        <div class="fold-content">          <p>用<code>lsblk</code>确认需要格式化的分区名称：<br><img src="/img/windows-with-arch/install/format-lsblk.png"></p> <figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs zsh">mkfs.btrfs /dev/nvme0n1p6<br>mkswap /dev/nvme0n1p7<br>mkfs.ext4 /dev/nvme0n1p8<br></code></pre></td></tr></table></figure> <p><img src="/img/windows-with-arch/install/format.png"></p>         </div>      </div>    </div><h2 id="7-创建btrfs子卷"><a href="#7-创建btrfs子卷" class="headerlink" title="(7)创建btrfs子卷"></a>(7)创建btrfs子卷</h2><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs zsh">mount -t btrfs -o compress=zstd /dev/nvme0n1p6 /mnt <span class="hljs-comment">#把分区挂载到/mnt</span><br>btrfs subvolume create /mnt/@ <span class="hljs-comment">#创建@子卷作为根目录</span><br>btrfs subvolume create /mnt/@home <span class="hljs-comment">#创建@home子卷作为家目录</span><br>btrfs subvolume create /mnt/@swap <span class="hljs-comment">#可选，但是如果想使用swap文件，就为它单独创建一个子卷</span><br>btrfs subvolume list /mnt <span class="hljs-comment">#复查一遍子卷创建的情况</span><br>umount /mnt <span class="hljs-comment">#卸载分区</span><br></code></pre></td></tr></table></figure><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/install/subvolume-create.png"></div></div><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/install/subvolume-list.png"></div></div></div><h2 id="8-挂载"><a href="#8-挂载" class="headerlink" title="(8)挂载"></a>(8)挂载</h2><div class="note note-info">            <p>作为系统根目录的分区必须最先挂载，其它分区依次挂载即可。</p>           </div><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs zsh">mount -t btrfs -o compress=zstd,subvol=@ /dev/nvme0n1p6 /mnt <span class="hljs-comment">#挂载根目录</span><br>mount -t btrfs -o compress=zstd,subvol=@home /dev/nvme0n1p6 /mnt/home --<span class="hljs-built_in">mkdir</span> <span class="hljs-comment">#挂载家目录</span><br>mount -t btrfs -o compress=zstd,subvol=@swap /dev/nvme0n1p6 /mnt/swap --<span class="hljs-built_in">mkdir</span> <span class="hljs-comment">#可选，挂载未来可能用到的swap文件</span><br>mount /dev/nvme0n1p1 /mnt/efi --<span class="hljs-built_in">mkdir</span> <span class="hljs-comment">#挂载efi分区</span><br>mount /dev/nvme0n1p8 /mnt/boot --<span class="hljs-built_in">mkdir</span> <span class="hljs-comment">#挂载/boot分区</span><br>swapon /dev/nvme0n1p7 <span class="hljs-comment">#启用swap分区</span><br></code></pre></td></tr></table></figure><p>然后复查挂载情况：</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs zsh">lsblk <span class="hljs-comment">#复查硬盘分区挂载情况</span><br>free -h <span class="hljs-comment">#复查swap分区挂载情况</span><br></code></pre></td></tr></table></figure><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/install/mount.png"></div></div><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/install/mount-check.png"></div></div></div><div class="note note-info">            <p>想使用swap文件的话安装完进系统了再配置。</p>           </div><h2 id="9-配置镜像源"><a href="#9-配置镜像源" class="headerlink" title="(9)配置镜像源"></a>(9)配置镜像源</h2><p>编辑<code>/etc/pacman.d/mirrorlist</code>，在顶端添加国内源，这里推荐cernet校园网联合镜像：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">Server = https:<span class="hljs-regexp">//mi</span>rrors.cernet.edu.cn<span class="hljs-regexp">/archlinux/</span><span class="hljs-variable">$repo</span><span class="hljs-regexp">/os/</span><span class="hljs-variable">$arch</span><br></code></pre></td></tr></table></figure><div class="note note-info">            <p>这是唯一一个安装系统的过程中会从liveCD复制到新系统的文件。</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-3b8afe6b" role="button" aria-expanded="false" aria-controls="collapse-3b8afe6b">        <div class="fold-arrow">▶</div>偏好的vim操作      </div>      <div class="fold-collapse collapse" id="collapse-3b8afe6b">        <div class="fold-content">          <figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">vim /etc/pacman.d/mirrorlist<br></code></pre></td></tr></table></figure> <p>光标下移到镜像列表中的第二个链接（下移7行，可输入<code>7j</code>），按<code>yy</code>复制整行；<br>输入<code>3k</code>上移3行，然后按<code>p</code>粘贴；<br>输入<code>4w</code>向右移动4个单词，输入<code>c7w</code>，然后键入<code>mirrors.cernet.edu.cn</code>；<br><code>Esc</code>退出编辑模式，<code>:wq</code>保存退出。<br><img src="/img/windows-with-arch/install/vim-mirrorlist.png"></p>         </div>      </div>    </div><h2 id="9-5-并行下载"><a href="#9-5-并行下载" class="headerlink" title="(9.5)并行下载"></a>(9.5)并行下载</h2><p>编辑<code>/etc/pacman.conf</code>，将<code>#ParallelDownloads = 5</code>行取消注释，并把5改成20。</p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-4ba356bb" role="button" aria-expanded="false" aria-controls="collapse-4ba356bb">        <div class="fold-arrow">▶</div>偏好的vim操作      </div>      <div class="fold-collapse collapse" id="collapse-4ba356bb">        <div class="fold-content">          <figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">vim /etc/pacman.conf<br></code></pre></td></tr></table></figure> <p>输入<code>/Par</code>（<code>/</code>表示顺序查找）即可查找到并行选项，回车；<br><code>i</code>进入编辑模式，删除开头的<code>#</code>；<code>Esc</code>退出编辑模式；<br><code>A</code>（大写）跳到行尾编辑，把5改成20，<code>Esc</code>退出编辑模式；<br><code>:wq</code>保存退出。<br><img src="/img/windows-with-arch/install/vim-parallel.png"></p>         </div>      </div>    </div><h2 id="10-安装系统"><a href="#10-安装系统" class="headerlink" title="(10)安装系统"></a>(10)安装系统</h2><p>即将安装的软件包：</p><table><thead><tr><th>包名</th><th>作用</th><th>重要性</th></tr></thead><tbody><tr><td><code>base</code></td><td>系统最基础的组件</td><td>必需</td></tr><tr><td><code>base-devel</code></td><td>打包软件包必需的编译和打包工具等等，是安装AUR包必需的工具；由于<code>sudo</code>包含在这个包里面，因此直接现在装</td><td>几乎必需</td></tr><tr><td><code>linux</code> <code>linux-lts</code> <code>linux-zen</code></td><td>内核，不同内核之间的区别参见<a href="https://wiki.archlinux.org/title/Kernel">ArchWiki</a> &#x2F;<a href="https://wiki.archlinuxcn.org/wiki/%E5%86%85%E6%A0%B8">中文Wiki</a> 。对于物理机安装的系统，<code>linux-zen</code>会比<code>linux</code>更适合日用一些，但是<code>linux-zen</code>内核不太适合虚拟机；<code>linux-lts</code>内核版本相对旧一些，作为后备内核。</td><td>至少安装其一</td></tr><tr><td><code>linux-headers</code> <code>linux-lts-headers</code> <code>linux-zen-headers</code></td><td>内核相应的头文件</td><td>非必需，但是对NVIDIA显卡以及其它硬件安装的<code>dkms</code>驱动是必需的</td></tr><tr><td><code>btrfs-progs</code></td><td>btrfs工具包</td><td>对于btrfs格式的分区是必需的</td></tr><tr><td><code>networkmanager</code></td><td>联网工具</td><td>几乎必需</td></tr><tr><td><code>vim</code></td><td>终端文本编辑器</td><td>可选，替换为<code>nano</code>等也可，但是至少安装一个终端文本编辑器</td></tr><tr><td><code>bash-completion</code></td><td>bash的tab补全支持</td><td>建议，如果用<code>zsh</code>的话安装<code>zsh</code>和<code>zsh-completions</code></td></tr><tr><td><code>linux-firmware</code></td><td>固件包</td><td>物理机必需，虚拟机无需</td></tr><tr><td><code>intel-ucode</code> <code>amd-ucode</code></td><td>微码更新</td><td>物理机必需，根据CPU品牌安装对应的；虚拟机无需</td></tr><tr><td><code>fish</code></td><td>带有强大补全功能的shell</td><td>纯个人喜好</td></tr></tbody></table>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-d49276d6" role="button" aria-expanded="false" aria-controls="collapse-d49276d6">        <div class="fold-arrow">▶</div>关于内核的选择      </div>      <div class="fold-collapse collapse" id="collapse-d49276d6">        <div class="fold-content">          <p>个人喜好安装3个不同版本的内核，中间版本的作为主力，旧版本的作为后备，更新的测试版本的内核用在”当前版本的内核有bug但是新版已经修了”的情况。<br><span class="label label-warning">2025年6月8号更新：物理机N卡驱动和mainline内核不兼容，已放弃使用该内核作为备用内核，改为zen+lts双内核。</span><br>目前内核版本的情况：</p> <table> <thead> <tr> <th>内核</th> <th>当前版本号</th> <th>补充说明</th> </tr> </thead> <tbody><tr> <td><code>linux</code></td> <td>6.13.2.arch1-1</td> <td>主线内核，稳步推进版本</td> </tr> <tr> <td><code>linux-zen</code></td> <td>6.13.2.zen1-1</td> <td>更新频率跟随主线</td> </tr> <tr> <td><code>linux-lts</code></td> <td>6.12.13-1</td> <td>在2024年的时候保持6.6, 2025年改到6.12并将在全年保持维护6.12版本</td> </tr> <tr> <td><code>linux-mainline</code></td> <td>6.14rc2-1</td> <td>比主线内核更新一版的rc版内核，官方源没有，AUR上的是源码编译，或者使用社区源的版本</td> </tr> <tr> <td><code>linux-lily</code></td> <td>lts内核在6.12.18-1时,lily内核在6.12.13-1</td> <td>cn社区站长自用内核，更新频率较低，也可以作为后备内核；通过补丁支持在tty上显示中文；需要在archlinuxcn社区源添加。</td> </tr> </tbody></table>         </div>      </div>    </div><p>安装命令：<br>虚拟机：</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">pacstrap -K /mnt base base-devel linux linux-headers linux-lts linux-lts-headers vim bash-completion networkmanager btrfs-progs fish<br></code></pre></td></tr></table></figure><p>Intel物理机：</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">pacstrap -K /mnt base base-devel linux-zen linux-zen-headers linux-lts linux-lts-headers vim bash-completion networkmanager btrfs-progs fish linux-firmware intel-ucode<br></code></pre></td></tr></table></figure><p>AMD物理机：</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">pacstrap -K /mnt base base-devel linux-zen linux-zen-headers linux-lts linux-lts-headers vim bash-completion networkmanager btrfs-progs fish linux-firmware amd-ucode<br></code></pre></td></tr></table></figure><div class="note note-info">            <p>有些教程里面未添加<code>-K</code>参数; 根据<a href="https://man.archlinux.org/man/pacstrap.8">pacstrap man page</a> </p> <blockquote> <p>-G<br>Avoid copying the host’s pacman keyring to the target.<br>-K<br>Initialize an empty pacman keyring in the target (implies -G).</p> </blockquote> <p><code>-K</code>参数的作用是在目标中初始化新的密钥，而不是从liveCD中复制一份；注意到下载软件包之前有类似输出：<br><img src="/img/windows-with-arch/install/pacman-key.png"></p>           </div><h2 id="11-生成fstab文件"><a href="#11-生成fstab文件" class="headerlink" title="(11)生成fstab文件"></a>(11)生成fstab文件</h2><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs zsh">genfstab -U /mnt &gt; /mnt/etc/fstab <span class="hljs-comment"># -U表示根据硬盘的UUID生成，分区的UUID只要不重新格式化就不会改变，是推荐的标识</span><br><span class="hljs-built_in">cat</span> /mnt/etc/fstab <span class="hljs-comment">#复查一下生成结果是否正确</span><br></code></pre></td></tr></table></figure><p>结果应该类似这样：<br><img src="/img/windows-with-arch/install/fstab.png"></p><h2 id="12-切换到新安装的系统"><a href="#12-切换到新安装的系统" class="headerlink" title="(12)切换到新安装的系统"></a>(12)切换到新安装的系统</h2><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs zsh">arch-chroot /mnt <span class="hljs-comment">#切换后终端左侧字体的显示应该会发生改变</span><br>fish <span class="hljs-comment">#chroot进来之后，使用的是新系统的bash了，咱还是喜欢切换到刚刚装的fish来操作</span><br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/chroot.png"><br>zsh,bash和fish同台（</p><div class="note note-info">            <p>当系统崩溃到无法进入系统的时候，使用liveCD启动，挂载硬盘然后<code>arch-chroot</code>就可以回到系统进行修复了，这是修系统最有力的武器；<br>所以理论上ArchLinux永远不需要重装系统~</p>           </div><h2 id="12-5-并行下载"><a href="#12-5-并行下载" class="headerlink" title="(12.5)并行下载"></a>(12.5)并行下载</h2><p>liveCD环境里面修改过的配置文件并不会复制到新安装的系统中来，所以需要重新设置一下pacman的并行下载，这里直接复制前面的步骤了。<br>编辑<code>/etc/pacman.conf</code>，将<code>#ParallelDownloads = 5</code>行取消注释，并把5改成20。</p>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-2d7d5c38" role="button" aria-expanded="false" aria-controls="collapse-2d7d5c38">        <div class="fold-arrow">▶</div>偏好的vim操作      </div>      <div class="fold-collapse collapse" id="collapse-2d7d5c38">        <div class="fold-content">          <figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs zsh">vim /etc/pacman.conf<br></code></pre></td></tr></table></figure> <p>输入<code>/Par</code>（<code>/</code>表示顺序查找）即可查找到并行选项，回车；<br><code>i</code>进入编辑模式，删除开头的<code>#</code>；<code>Esc</code>退出编辑模式；<br><code>A</code>（大写）跳到行尾编辑，把5改成20，<code>Esc</code>退出编辑模式；<br><code>:wq</code>保存退出。<br><img src="/img/windows-with-arch/install/vim-parallel.png"></p>         </div>      </div>    </div><h2 id="13-设置主机名"><a href="#13-设置主机名" class="headerlink" title="(13)设置主机名"></a>(13)设置主机名</h2><p>这里因为是在虚拟机测试双系统，就给主机名取名叫<code>dualarch</code>了。</p><div class="note note-warning">            <p>主机名不要包含大写字母，空格和点。</p>           </div><div class="note note-info">            <p>任何写入文件的操作过后，都可以用<code>cat</code>命令或者文本编辑器复查一下写入的是否正确。</p>           </div><p>编辑<code>/etc/hostname</code>（这个文件原先是不存在的），填入主机名：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">echo</span> <span class="hljs-string">&#x27;dualarch&#x27;</span> &gt; /etc/hostname<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/hostname.png"><br>然后编辑<code>/etc/hosts</code>，填入以下几行内容（可以用tab对齐）:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs hosts">127.0.0.1localhost<br>::1localhost<br>127.0.1.1dualarch.localdomaindualarch<br></code></pre></td></tr></table></figure><p>（因为是直接在文件末尾附加新行，vim没什么特别的操作，就不附了；<code>o</code>在光标下方插入一行，<code>O</code>在光标上方插入一行）<br><img src="/img/windows-with-arch/install/hosts.png"></p><div class="note note-info">            <p>设置hosts似乎不是必需的，在Windows的hosts文件里有提到：</p> <blockquote> <p>localhost name resolution is handled within DNS itself.</p> </blockquote>           </div><h2 id="14-设置时区"><a href="#14-设置时区" class="headerlink" title="(14)设置时区"></a>(14)设置时区</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ln</span> -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime<br>hwclock --systohc<br></code></pre></td></tr></table></figure><div class="note note-info">            <p>在运行中的系统中更改时区可以简单一些：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">timedatectl set-timezone Asia/Shanghai<br></code></pre></td></tr></table></figure> <p>这里手动链接是因为<code>timedatectl</code>命令在chroot环境下不能正常工作。</p>           </div><h2 id="15-配置本地化"><a href="#15-配置本地化" class="headerlink" title="(15)配置本地化"></a>(15)配置本地化</h2><p>编辑<code>/etc/locale.gen</code>，去除<code>en_US.UTF-8 UTF-8</code>行和<code>zh_CN.UTF-8 UTF-8</code>行的注释；</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/install/locale-gen1.png"></div><div class="group-image-wrap"><img src="/img/windows-with-arch/install/locale-gen2.png"></div></div></div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-124d914b" role="button" aria-expanded="false" aria-controls="collapse-124d914b">        <div class="fold-arrow">▶</div>vim操作      </div>      <div class="fold-collapse collapse" id="collapse-124d914b">        <div class="fold-content">          <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">vim /etc/locale.gen<br></code></pre></td></tr></table></figure> <p>输入<code>/en_US</code>查找，<code>/</code>表示顺序查找；回车，然后<code>i</code>进入编辑模式，删除开头的<code>#</code>；<code>Esc</code>退出编辑模式；<br>输入<code>?zh_CN</code>查找，<code>?</code>表示逆序查找；回车，然后上移一行（或者按<code>n</code>表示查找下一项），<code>i</code>进入编辑模式，删除开头的<code>#</code>；<code>Esc</code>退出编辑模式；<br><code>:wq</code>保存退出。</p>         </div>      </div>    </div><p>然后执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">locale-gen<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/locale-gen.png"><br>接下来创建<code>/etc/locale.conf</code>文件，填入<code>LANG=en_US.UTF-8</code>；</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">echo</span> <span class="hljs-string">&#x27;LANG=en_US.UTF-8&#x27;</span> &gt; /etc/locale.conf<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/locale-conf.png"></p><div class="note note-info">            <blockquote> <p>将系统 locale 设置为 en_US.UTF-8 ，系统的 log 就会用英文显示，这样更容易判断和处理问题；<br>也可以设置为 en_GB.UTF-8 或 en_SG.UTF-8，附带以下优点：</p> <ul> <li>进入桌面环境后以 24 小时制显示时间；</li> <li>LibreOffice 等办公软件的纸张尺寸会默认为 A4 而非 Letter(US)；</li> <li>可尽量避免不必要且可能造成处理麻烦的英制单位。</li> </ul> </blockquote>           </div><div class="note note-warning">            <ul> <li>设置的<code>LANG</code>变量必须是使用<code>locale-gen</code>生成过的，否则会报错<code>Cannot set LC_CTYPE to default locale: No such file or directory</code></li> <li>不要在这里设置成任何中文Locale；由于tty没有中文字体，这会导致tty上显示为方块；如果需要安装桌面环境，可以在安装桌面环境之后再修改Locale为中文，并通过合理的配置让tty保持显示英文（咱的<a href="/2025/01/15/some-rc-files/" title="一些配置文件存档">另一篇blog</a>里面有收录其中一份这样的配置）；另外，archlinuxcn社区的站长自用的内核<code>linux-lily</code>里面打了让tty支持显示中文的补丁，这个内核可以在archlinuxcn社区源获取，这是后话。</li> </ul>           </div><h2 id="16-设置root密码"><a href="#16-设置root密码" class="headerlink" title="(16)设置root密码"></a>(16)设置root密码</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">passwd<br></code></pre></td></tr></table></figure><div class="note note-success">            <p>没有回显是正常的，键盘没有坏</p>           </div><h2 id="17-安装引导"><a href="#17-安装引导" class="headerlink" title="(17)安装引导"></a>(17)安装引导</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S grub efibootmgr os-prober<br></code></pre></td></tr></table></figure><table><thead><tr><th>软件包</th><th>功能</th></tr></thead><tbody><tr><td><code>grub</code></td><td>引导程序本身</td></tr><tr><td><code>efibootmgr</code></td><td>管理UEFI启动项</td></tr><tr><td><code>os-prober</code></td><td>探测硬盘上存在的其它系统启动项，比如Windows Boot Manager</td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">grub-install --target=x86_64-efi --efi-directory=/efi --bootloader-id=Arch\ Linux<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/grub-install.png"><br>此外还要编辑<code>/etc/default/grub</code>，作以下修改：</p><ul><li>顶部添加一行<code>LANG=C</code>；<br>  让grub使用的语言保持英文，既方便做其它可能的自定义配置，还能让显示整体更一致；<br>  后续在locale设为中文的情况下，grub菜单可能出现汉化了但没完全汉化的情况，观感还不如全英文。</li><li><code>GRUB_DEFAULT</code>改为<code>saved</code>，下面的<code>GRUB_SAVEDEFAULT=true</code>取消注释；<br>  这能让grub记住上次选择的选项；</li><li><code>GRUB_CMDLINE_LINUX_DEFAULT</code>中，去掉<code>quiet</code>，并且把<code>loglevel</code>提高到<code>5</code>；<br>  这样能看到更加完整的内核日志，便于排查可能出现的问题；</li><li><code>GRUB_DISABLE_OS_PROBER=false</code>取消注释，启用<code>os-prober</code>探测硬盘上存在的Windows启动项;</li><li>最后增加一行<code>GRUB_TOP_LEVEL=&quot;/boot/vmlinuz-linux&quot;</code>：<br>  默认情况下，<code>linux-lts</code>内核排序会排在<code>linux</code>内核的上面，导致默认启动<code>linux-lts</code>内核；<br>  如果希望默认启动其它内核，也要改成对应的名字，比如默认启动<code>linux-zen</code>内核需要写<code>GRUB_TOP_LEVEL=&quot;/boot/vmlinuz-linux-zen&quot;</code></li></ul><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/install/grub1.png"></div><div class="group-image-wrap"><img src="/img/windows-with-arch/install/grub2.png"></div></div></div><p>另外：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /etc/grub.d/<br><span class="hljs-built_in">ls</span><br><span class="hljs-built_in">mv</span> 30_os-prober 09_os-prober<br><span class="hljs-built_in">ls</span><br><span class="hljs-built_in">cd</span> ../..<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/grub-prober.png"><br>把os-prober的位置提前，让Windows的启动项位于第一项，这样与Linux启动项相邻，操作更方便；</p><p>最后生成grub配置文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">grub-mkconfig -o /boot/grub/grub.cfg<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/grub-config.png"></p><div class="note note-info">            <p>这里可以看到正确输出了包含Windows启动项在内的条目；<br>但是有报告称<code>os-prober</code>在chroot环境下不一定会正常工作，这种情况下直接先忽略，重启进入Linux系统后再执行一次<code>grub-mkconfig</code>就好了。</p>           </div><h2 id="18-退出系统，重启"><a href="#18-退出系统，重启" class="headerlink" title="(18)退出系统，重启"></a>(18)退出系统，重启</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">exit</span> <span class="hljs-comment">#退出fish</span><br><span class="hljs-built_in">exit</span> <span class="hljs-comment">#退出chroot</span><br>umount -R /mnt <span class="hljs-comment">#可选，手动解挂载，可以帮助发现被占用的分区</span><br>reboot <span class="hljs-comment">#重启计算机</span><br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/install/exit.png"></p><h2 id="19-新系统开机联网"><a href="#19-新系统开机联网" class="headerlink" title="(19)新系统开机联网"></a>(19)新系统开机联网</h2><p><img src="/img/windows-with-arch/final/boot.png"><br>引导菜单已经正常出来了；</p><div class="note note-info">            <p>bitlocker如果开启，首次从grub菜单启动至Windows时可能要求输入恢复密钥。</p>           </div><p>使用root用户登录：<br><img src="/img/windows-with-arch/final/login.png"></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl <span class="hljs-built_in">enable</span> --now NetworkManager <span class="hljs-comment">#将联网服务设为开机自启，并立即启动</span><br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/final/enable-net.png"><br>有线网和虚拟机通常就已经连上网了，无线网需要在以下两种方式选择一种连接：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">nmcli dev wifi list <span class="hljs-comment"># 显示附近的 Wi-Fi 网络</span><br>nmcli dev wifi connect <span class="hljs-string">&quot;Wi-Fi名&quot;</span> password <span class="hljs-string">&quot;网络密码&quot;</span> <span class="hljs-comment"># 连接指定的无线网络</span><br></code></pre></td></tr></table></figure><p>或者：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nmtui <span class="hljs-comment">#进入tui之后按照指引连接</span><br></code></pre></td></tr></table></figure><p>然后使用<code>ping</code>命令测试网络连接如前。</p><h3 id="DNS服务"><a href="#DNS服务" class="headerlink" title="DNS服务"></a>DNS服务</h3><p>NetworkManager默认的自带DNS管理功能比较简单粗糙，可以开启<code>systemd-resolved</code>配合使用，这也是很多其它发行版的默认做法：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl <span class="hljs-built_in">enable</span> systemd-resolved.service<br></code></pre></td></tr></table></figure><p>接下来设置配置文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ln</span> -sf ../run/systemd/resolve/stub-resolv.conf /etc/resolv.conf<br></code></pre></td></tr></table></figure><div class="note note-info">            <ol> <li>注意这里使用<code>..</code>开头的路径创建相对符号链接；</li> <li>chroot环境中无法这样创建，为了简化问题就留到现在重启之后再做。</li> </ol>           </div><p>配置完成之后，重启计算机以确保<code>NetworkManager</code>和<code>systemd-resolved</code>顺序开启并正确识别到对方。同时还可以检查一下符号链接是否正常：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> -l <span class="hljs-string">&#x27;/etc/resolv.conf&#x27;</span><br></code></pre></td></tr></table></figure><p>应该见到类似这样的输出：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">&lt;前面是各种权限和日期信息&gt; <span class="hljs-regexp">/etc/</span>resolv.conf -&gt; ..<span class="hljs-regexp">/run/</span>systemd<span class="hljs-regexp">/resolve/</span>stub-resolv.conf<br></code></pre></td></tr></table></figure><p>如果符号链接正确，那<code>systemd-resolved</code>就应该被正确识别并启用了。如果不放心，可以显式启用：创建<code>/etc/NetworkManager/conf.d/dns.conf</code>，填入：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs conf">[main]<br>dns=systemd-resolved<br></code></pre></td></tr></table></figure><h2 id="20-添加软件源"><a href="#20-添加软件源" class="headerlink" title="(20)添加软件源"></a>(20)添加软件源</h2><p>编辑<code>/etc/pacman.conf</code>，把<code>multilib</code>所在的两行注释掉，开启32位库的支持；然后添加[archlinuxcn]和[arch4edu]社区源（仍然推荐使用cernet校园网联合镜像站）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs config">[multilib]<br>Include = /etc/pacman.d/mirrorlist<br><br>......<br><br>[archlinuxcn]<br>Server = https://mirrors.cernet.edu.cn/archlinuxcn/$arch<br><br>[arch4edu]<br>Server = https://mirrors.cernet.edu.cn/arch4edu/$arch<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/final/pacman-conf.png"></p>    <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-857172eb" role="button" aria-expanded="false" aria-controls="collapse-857172eb">        <div class="fold-arrow">▶</div>以下内容的一点小技巧      </div>      <div class="fold-collapse collapse" id="collapse-857172eb">        <div class="fold-content">          <p>对于archlinuxcn源，可以先尝试单独安装<code>archlinuxcn-keyring</code>；如果gpg验证失败，再执行下面的步骤。<br>对于arch4edu源，手动输入key稍显繁琐，虽然可以通过开启bash的vi模式来加速操作，但还是略显笨拙；这里提供一个可能有用的快捷方法：<br>首先安装<code>arch4edu-keyring</code>；安装过程会询问是否import导入key，选择是，但是由于信任不足会验证失败，选择保留软件包：<br><img src="/img/windows-with-arch/final/edu-key1.png"><br>这份key其实就是我们手动导入的那份key，只需要再为它添加信任就可以了（输入到<code>i@j</code>就可以让tab帮你补全了）：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman-key --lsign-key <span class="hljs-string">&quot;i@jingbei.li&quot;</span><br></code></pre></td></tr></table></figure> <p>之后再安装一遍<code>arch4edu-keyring</code>就没问题了：<br><img src="/img/windows-with-arch/final/edu-key2.png"></p>         </div>      </div>    </div><p>对于archlinuxcn源，需要为本地的一份GPG key添加信任，原因参考<a href="https://www.archlinuxcn.org/archlinuxcn-keyring-manually-trust-farseerfc-key/">这里</a> ：</p><div class="note note-info">            <p>2025年6月8日更新：该key的信任数恢复至正常状态，无需再手动添加信任。</p>           </div><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman-key --lsign-key <span class="hljs-string">&quot;farseerfc@archlinux.org&quot;</span><br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/final/cn-key.png"><br>对于arch4edu源，需要额外导入GPG key：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman-key --recv-keys 7931B6D628C8D3BA<br>pacman-key --finger 7931B6D628C8D3BA<br>pacman-key --lsign-key 7931B6D628C8D3BA<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/final/edu-key.png"><br>然后刷新一下pacman数据库，并安装上keyring：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -Sy archlinuxcn-keyring arch4edu-keyring<br></code></pre></td></tr></table></figure><h2 id="21-增加内核"><a href="#21-增加内核" class="headerlink" title="(21)增加内核"></a>(21)增加内核</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S linux-mainline linux-mainline-headers<br></code></pre></td></tr></table></figure><p><code>linux-mainline</code>内核也是计划安装的内核，但是官方仓库没有，这里安装的是[archlinuxcn]社区源提供的打包好的版本；</p><div class="note note-warning">            <p>2025年6月8日更新：NVIDIA并不对mainline内核提供支持，因此如果mainline内核版本过新，弃用了某些内核调用，会导致N卡驱动安装失败。<br>所以最终咱的物理机敲定的内核选择是<del>zen主力+lts备用</del>的配置。为了减少在备用内核上浪费过多编译dkms驱动的时间已改用<code>linux-lily</code>作为备用内核。</p>           </div><p>然后刷新grub配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">grub-mkconfig -o /boot/grub/grub.cfg<br></code></pre></td></tr></table></figure><h2 id="21-5-swap文件"><a href="#21-5-swap文件" class="headerlink" title="(21.5)swap文件"></a>(21.5)swap文件</h2><p>使用swap文件的话，前面挂载好的<code>@swap</code>子卷就派上用场了：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">btrfs filesystem mkswapfile --size 2g --uuid clear /swap/swapfile <span class="hljs-comment">#创建一个2G的swap文件；由于btrfs文件系统的特殊属性，不要使用通常的fallocate或者dd命令创建</span><br>swapon /swap/swapfile<br></code></pre></td></tr></table></figure><p>然后编辑<code>/etc/fstab</code>，开机自动启用交换文件：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs fstab">/swap/swapfile none swap defaults 0 0<br></code></pre></td></tr></table></figure><h2 id="22-完成安装"><a href="#22-完成安装" class="headerlink" title="(22)完成安装"></a>(22)完成安装</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">pacman -S fastfetch<br>fastfetch<br></code></pre></td></tr></table></figure><p><img src="/img/windows-with-arch/final/fastfetch.png"><br>观赏一下版本信息吧。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">reboot <span class="hljs-comment">#重启</span><br>poweroff <span class="hljs-comment">#或者关机</span><br></code></pre></td></tr></table></figure><p>再开机看看Windows是否正常启动：</p><div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/final/boot-win.png"></div></div><div class="group-image-row"><div class="group-image-wrap"><img src="/img/windows-with-arch/final/win.png"></div></div></div><p>一切正常。</p><h2 id="更新-固态硬盘trim"><a href="#更新-固态硬盘trim" class="headerlink" title="更新:固态硬盘trim"></a>更新:固态硬盘trim</h2><blockquote><p>Btrfs 文件系统能够从支持 TRIM 命令的 SSD 驱动器中释放未使用的块。可使用挂载参数 discard&#x3D;async 启用异步丢弃（asynchronous discard）支持，linux包 6.2 版本已经默认启用该功能。已释放的空间范围不会被马上丢弃，它们会被集中起来并在稍后由一个单独的工作线程进行 TRIM，这将能改善提交延迟。</p></blockquote><p>上述功能已经可以保证基本的trim操作，但是如果需要，也可以手动开启定期trim(与上述功能并不冲突)。各个Linux社区均不推荐持续性trim：</p><blockquote><p><a href="https://askubuntu.com/questions/1034169/is-trim-enabled-on-my-ubuntu-18-04-installation">Ubuntu默认使用定期TRIM</a>，<a href="https://wiki.debian.org/SSDOptimization#Mounting_SSD_filesystems">Debian不建议使用持续性TRIM</a>，<a href="https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/html/managing_file_systems/discarding-unused-blocks_managing-file-systems">红帽建议尽可能选择使用定期TRIM</a>。</p></blockquote><blockquote><p>要检查 TRIM 支持，以根用户执行<code>lsblk --discard</code><br>若 DISC-GRAN（discard granularity）和 DISC-MAX（discard max bytes）列上的数值不为零，则表示对应设备支持 TRIM。<br>对于 SATA SSD，可通过hdparm包检测 TRIM 支持：以根用户执行<code>hdparm -I /dev/sda | grep TRIM</code>。注意：hdparm不支持 NVMe SSD。</p></blockquote><p>开启定期trim：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl <span class="hljs-built_in">enable</span> fstrim.timer<br></code></pre></td></tr></table></figure><p>该定时器会根据时间戳每周执行一次trim操作。</p><p>更多内容参照<a href="https://wiki.archlinux.org/title/Solid_state_drive#TRIM">ArchWiki</a>&#x2F;<a href="https://wiki.archlinuxcn.org/wiki/%E5%9B%BA%E6%80%81%E7%A1%AC%E7%9B%98#TRIM">中文Wiki</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>更改Linux Mint在Boot Menu里面的启动项名称</title>
    <link href="/2025/01/19/mintboot/"/>
    <url>/2025/01/19/mintboot/</url>
    
    <content type="html"><![CDATA[<p>直接使用UEFI Linux系统一定会有的<code>efibootmgr</code>来完成就行。<br><br>开始前先看一眼Boot Menu，确实叫Ubuntu:<br><img src="/img/mintboot/begin-boot.png"><br>系统也确实是如假包换的Linux Mint:<br><img src="/img/mintboot/neofetch.png"></p><p>接下来开始操作，建议开两个终端窗口。</p><ol><li><p>准备信息<br> 首先需要知道efi分区的位置:</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">lsblk<br></code></pre></td></tr></table></figure><p> <img src="/img/mintboot/lsblk.png"><br> 从结果看到<code>/boot/efi</code>对应的分区位于<strong>硬盘nvme0n1</strong>上面的<strong>第1个</strong>分区；<br> 然后还需要知道启动项的信息:</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">efibootmgr<br></code></pre></td></tr></table></figure><p> <img src="/img/mintboot/begin-mgr.png"><br> 其中有两个信息，一个是启动项编号<strong>0005</strong>，另一个是高亮的启动项位置</p>     <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-659c5e2f" role="button" aria-expanded="false" aria-controls="collapse-659c5e2f">        <div class="fold-arrow">▶</div>强迫症可能还要解决的一件事      </div>      <div class="fold-collapse collapse" id="collapse-659c5e2f">        <div class="fold-content">          <p>对于大部分情况下，这个路径其实应该是<code>\EFI\ubuntu\shimx64.efi</code>，当然咱的强迫症也受不了这件事，必须纠一下：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=Linux\ Mint<br></code></pre></td></tr></table></figure> <p>然后检查一遍现在的启动项位置：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">efibootmgr<br></code></pre></td></tr></table></figure> <p>嗯，很好，现在才是合适的路径<code>\EFI\Linux Mint\shimx64.efi</code><br>确认无误之后就可以把ubuntu的启动文件夹干掉了：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> -rf /boot/efi/EFI/ubuntu/<br></code></pre></td></tr></table></figure>        </div>      </div>    </div></li><li><p>处理启动项</p> <div class="note note-info">            <p>建议开第二个终端窗口；第一个终端的输出留着，后面有用</p>           </div> <div class="note note-danger">            <p>不要盲目复制！千万记得替换成自己的实际情况！</p>           </div><p> 删除旧的启动项<span class="label label-danger">（注意编号！）</span></p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> efibootmgr -b 0005 -B<br></code></pre></td></tr></table></figure><p> <code>-b</code>是指定操作的启动项编号，<code>-B</code>是删除；<br><br> 然后添加新的启动项<span class="label label-danger">（注意分区情况和文件路径，千万不要套错！）</span><br> 最后一项路径可以从前面<code>efibootmgr</code>的输出里面直接复制过来，避免误输</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> efibootmgr -c -d /dev/nvme0n1 -p 1 -L <span class="hljs-string">&quot;Linux Mint&quot;</span> -l <span class="hljs-string">&#x27;\EFI\Linux Mint\shimx64.efi&#x27;</span><br></code></pre></td></tr></table></figure>     <div class="fold">      <div class="fold-title fold-info collapsed" data-toggle="collapse" href="#collapse-db75c721" role="button" aria-expanded="false" aria-controls="collapse-db75c721">        <div class="fold-arrow">▶</div>参数解释      </div>      <div class="fold-collapse collapse" id="collapse-db75c721">        <div class="fold-content">          <p>参考 <a href="https://wiki.gentoo.org/wiki/Efibootmgr#Creating_a_boot_entry">Gentoo wiki – efibootmgr</a><br><code>-c</code>: 等价于<code>--create</code>，指定操作是创建启动项；<br><code>-d</code>: 等价于<code>--disk</code>，指定efi分区所在的硬盘；<br><code>-p</code>: 等价于<code>--part</code>，指定efi分区是所在硬盘的第几个分区；<br><code>-L</code>: 等价于<code>--label</code>，为添加的启动项添加标签（也就是名称）；<br><code>-l</code>: 等价于<code>--loader</code>，指定启动文件的路径（用单引号括起来，并且路径必须以反斜杠<code>\</code>分隔，而非斜杠<code>/</code>。</p>         </div>      </div>    </div><p> 添加完毕之后我们可以对比一下现在的启动项信息和原来的：<br> 现在的应该类似这样：<br> <img src="/img/mintboot/after-mgr.png"></p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">Boot0005* Linux MintHD(1,GPT,8d39f939-e7bd-4a84-8b3e-fbaeb1852557,0x800,0x32000)/File(\EFI\Linux Mint\shimx64.efi)<br></code></pre></td></tr></table></figure><p> 对比一下原来的：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">Boot0005* UbuntuHD(1,GPT,8d39f939-e7bd-4a84-8b3e-fbaeb1852557,0x800,0x32000)/File(\EFI\Linux Mint\shimx64.efi)<br></code></pre></td></tr></table></figure><p> 如果除了名字以外，其它内容都像这样和原来完全一致，说明启动项没有问题了。</p></li><li><p>重启看看效果<br> Boot Menu里面的启动项名称改过来了：<br> <img src="/img/mintboot/after-boot.png"><br> 并且能正常进入系统，修改完成</p></li></ol><div class="note note-info">            <p>不必担心启动项<code>Linux Mint</code>里面有空格会不会出现什么兼容性问题，如果真的不能有空格的话，<code>Windows Boot Manager</code>早炸了</p>           </div>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Linux安装搜狗输入法的二三事</title>
    <link href="/2025/01/18/sogou-linux/"/>
    <url>/2025/01/18/sogou-linux/</url>
    
    <content type="html"><![CDATA[<p>不想折腾 <a href="https://rime.im/">Rime</a> 输入法，fcitx自带的输入法感觉也不好使，试了一圈还是搜狗用的习惯。</p><div class="note note-info">            <p>已全设备全系统改用rime，彻底弃用搜狗（搜狗的Windows版现在简直是大奋）。</p>           </div><h1 id="apt-deb系安装"><a href="#apt-deb系安装" class="headerlink" title="apt&#x2F;deb系安装"></a>apt&#x2F;deb系安装</h1><h2 id="官方版"><a href="#官方版" class="headerlink" title="官方版"></a>官方版</h2><div class="note note-info">            <p>测试平台：Linux Mint 20.3&#x2F;21.3&#x2F;22, LMDE 6<br>由于Mint系二进制兼容上游，因此应该同样适用于Ubuntu 20.04&#x2F;22.04&#x2F;24.04, Debian 12</p>           </div><div class="note note-warning">            <p>兼容情况：Mint 20.3完美运行，其它版本均无法打开输入法设置界面，但是通过修改相关的配置文件(<code>~/.config/sogoupinyin/conf/env.ini</code>)也能实现修改设置的效果。<br>另：不原生支持Wayland，但是在Wayland桌面下似乎也可用，应该是自动调用了XWayland?</p>           </div><h3 id="1-安装依赖"><a href="#1-安装依赖" class="headerlink" title="1. 安装依赖"></a>1. 安装依赖</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt install fcitx<br></code></pre></td></tr></table></figure><p>搜狗输入法基于fcitx4框架，该框架与fcitx5冲突，因此会提示卸载fcitx5；如果系统还自带了ibus可以考虑删除，保留也无所谓；</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt install libqt5qml5 libqt5quick5 libqt5quickwidgets5 qml-module-qtquick2<br><span class="hljs-built_in">sudo</span> apt install libgsettings-qt1<br></code></pre></td></tr></table></figure><p>这些是必需的依赖；</p><h3 id="2-安装输入法本体"><a href="#2-安装输入法本体" class="headerlink" title="2. 安装输入法本体"></a>2. 安装输入法本体</h3><p>前往 <a href="https://shurufa.sogou.com/linux">搜狗输入法Linux官网</a> 点击“立即下载”，对于大部分常规计算机，选择“x86_64”即可；<br>下载完毕后，进入下载文件夹，通常双击deb包即可安装，或者通过命令行安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> dpkg -i /path/to/***.deb <span class="hljs-comment">#将路径替换为下载deb包的路径</span><br></code></pre></td></tr></table></figure><p>完成后可以运行一下依赖检查，看看是否还有依赖缺失：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt install --fix<br></code></pre></td></tr></table></figure><h3 id="3-修复输入法的依赖库"><a href="#3-修复输入法的依赖库" class="headerlink" title="3. 修复输入法的依赖库"></a>3. 修复输入法的依赖库</h3><p>搜狗输入法自带的Qt5库是有问题的，无法正常运行；因此使用脚本删除输入法自带的库，然后链接到系统正常的库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># !/bin/sh</span><br><br><span class="hljs-built_in">cd</span> /opt/sogoupinyin/files/lib/qt5/lib/<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5Core.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5Core.so.5 libQt5Core.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5Gui.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5Gui.so.5 libQt5Gui.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5Widgets.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5Widgets.so.5 libQt5Widgets.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5DBus.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5DBus.so.5 libQt5DBus.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5Qml.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5Qml.so.5 libQt5Qml.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5Network.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5Network.so.5 libQt5Network.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5Quick.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5Quick.so.5 libQt5Quick.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5QuickWidgets.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5QuickWidgets.so.5 libQt5QuickWidgets.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5Svg.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5Svg.so.5 libQt5Svg.so.5<br><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> libQt5XcbQpa.so.5 <br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /usr/lib/x86_64-linux-gnu/libQt5XcbQpa.so.5 libQt5XcbQpa.so.5<br></code></pre></td></tr></table></figure><h3 id="4-完成安装"><a href="#4-完成安装" class="headerlink" title="4. 完成安装"></a>4. 完成安装</h3><p>到这里就已经完成了，注销用户或者重启系统之后，切换到fcitx框架，就能找到搜狗输入法了；<br>效果图：<br>托盘菜单：<br><img src="/img/sogou-linux/fcitx-menu.png"><br>输入效果：<br><img src="/img/sogou-linux/fcitx-input.png"></p><h1 id="pacman系安装"><a href="#pacman系安装" class="headerlink" title="pacman系安装"></a>pacman系安装</h1><div class="note note-info">            <p>均在Arch Linux上测试，未在Manjaro等其它发行版测试过兼容性；<br><br>pacman系发行版由于AUR的加持，安装起来会简单很多；<br>AUR提供了两个版本：一个是移植自开放麒麟系统的 <a href="https://aur.archlinux.org/packages/fcitx5-sogou">fcitx5-sogou</a>, 另一个是从官方deb包移植的 <a href="https://aur.archlinux.org/packages/fcitx-sogoupinyin">fcitx-sogoupinyin</a><br>前者基于 <a href="https://wiki.archlinux.org/title/Fcitx5">Fcitx5</a> 框架，后者则基于 <a href="https://wiki.archlinux.org/title/Fcitx">Fcitx</a> 框架。<br>Fcitx5被定为Fcitx的继任者，因此Fcitx现已不再有新功能更新；Fcitx5正在积极开发中。<br>Fcitx5目前能感知到的优势只有原生支持Wayland，其它的完全没感知；<br>但是Fcitx5版本的搜狗，外观相对比较丑，并且移植不完美，存在设置页无法打开的问题；而基于Fcitx版本的官方搜狗能完美运行。</p>           </div><div class="note note-warning">            <p>Arch系在安装前强烈建议将系统滚到最新：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -Syu<br></code></pre></td></tr></table></figure>          </div><h2 id="Fcitx5版"><a href="#Fcitx5版" class="headerlink" title="Fcitx5版"></a>Fcitx5版</h2><div class="note note-info">            <p>以下假设使用的AUR助手是paru，如果使用的是yay或者其它请自行替换。</p>           </div><h3 id="1-配置Fcitx5"><a href="#1-配置Fcitx5" class="headerlink" title="1. 配置Fcitx5"></a>1. 配置Fcitx5</h3><div class="note note-info">            <p>确认已经按照 <a href="https://wiki.archlinuxcn.org/wiki/Fcitx5">中文wiki</a> 配置好了Fcitx5框架，已经配置好的可以直接进入下一步。<br>Fcitx5相关的中文Wiki是Arch Linux中文社区单独维护的内容，并非英文原文的翻译，因此这里贴出中文帖。</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-a60d5a04" role="button" aria-expanded="false" aria-controls="collapse-a60d5a04">        <div class="fold-arrow">▶</div>配置Fcitx5      </div>      <div class="fold-collapse collapse" id="collapse-a60d5a04">        <div class="fold-content">          <p>安装 <a href="https://archlinux.org/groups/x86_64/fcitx5-im/">fcitx5-im</a> 包组；可以保险起见全部安装，也可以先不安装其中的<code>fcitx5-gtk</code>和<code>fcitx5-qt</code>，搜狗并不依赖它们；<code>fcitx5-configtool</code>是配置工具建议保留。</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S fcitx5-im<br></code></pre></td></tr></table></figure> <p>然后根据使用的是X11还是Wayland，需要有不同的配置方法：<br>X11配置（参考 <a href="https://wiki.archlinuxcn.org/wiki/Fcitx5#X11">中文wiki</a> 和 <a href="https://wiki.archlinux.org/title/Fcitx5#IM_modules">wiki</a> ）<br>编辑<code>/etc/environment</code>，添加以下内容：</p> <figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-attr">GTK_IM_MODULE</span>=fcitx<br><span class="hljs-attr">QT_IM_MODULE</span>=fcitx<br><span class="hljs-attr">XMODIFIERS</span>=@im=fcitx<br><span class="hljs-attr">SDL_IM_MODULE</span>=fcitx<br><span class="hljs-attr">GLFW_IM_MODULE</span>=ibus<br></code></pre></td></tr></table></figure> <p>Wayland在不同桌面下的配置方式不同，因此自行查阅 <a href="https://wiki.archlinuxcn.org/wiki/Fcitx5#Wayland">中文wiki</a> 和 <a href="https://wiki.archlinux.org/title/Fcitx5#Wayland">wiki</a></p>         </div>      </div>    </div><h3 id="2-安装输入法本体-1"><a href="#2-安装输入法本体-1" class="headerlink" title="2. 安装输入法本体"></a>2. 安装输入法本体</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">paru -S fcitx5-sogou<br></code></pre></td></tr></table></figure><div class="note note-info">            <p>如果下载出现问题，编辑<code>~/.cache/paru/clone/fcitx5-sogou/PKGBUILD</code>，将阿里源注释掉，改用开放麒麟官方源：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">mirrors=<span class="hljs-string">&quot;http://archive.build.openkylin.top&quot;</span><br><span class="hljs-comment"># mirrors=&quot;https://mirrors.aliyun.com&quot;</span><br></code></pre></td></tr></table></figure> <p>然后重新运行一次安装。</p>           </div><h3 id="3-完成"><a href="#3-完成" class="headerlink" title="3. 完成"></a>3. 完成</h3><p>注销或者重启即可使用。效果图如下：<br>托盘菜单：<br><img src="/img/sogou-linux/fcitx5-menu.png"><br>输入效果：<br><img src="/img/sogou-linux/fcitx5-input.png"></p><h2 id="Fcitx版-官方版"><a href="#Fcitx版-官方版" class="headerlink" title="Fcitx版(官方版)"></a>Fcitx版(官方版)</h2><div class="note note-info">            <p>建议不使用AUR助手。</p>           </div><h3 id="1-配置Fcitx"><a href="#1-配置Fcitx" class="headerlink" title="1. 配置Fcitx"></a>1. 配置Fcitx</h3><div class="note note-info">            <p>请确认已经按照 <a href="https://wiki.archlinux.org/title/Fcitx">wiki</a> 或者 <a href="https://wiki.archlinuxcn.org/wiki/Fcitx">中译wiki</a> 配置好了Fcitx框架，已经配置好的可以直接进入下一步。</p>           </div>    <div class="fold">      <div class="fold-title fold-default collapsed" data-toggle="collapse" href="#collapse-c844edc0" role="button" aria-expanded="false" aria-controls="collapse-c844edc0">        <div class="fold-arrow">▶</div>配置Fcitx      </div>      <div class="fold-collapse collapse" id="collapse-c844edc0">        <div class="fold-content">          <p>安装<code>fcitx</code>包以及图形化配置工具<code>fcitx-configtool</code>。KDE用户也可以用另一个配置工具<code>kcm-fcitx</code>：</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S fcitx fcitx-configtool<br></code></pre></td></tr></table></figure> <div class="note note-info">             <p>这里不直接安装 <a href="https://archlinux.org/groups/x86_64/fcitx-im/">fcitx-im</a> 包组，因为这个包组并不含配置工具，且咱需要的<code>fcitx-qt5</code>被搜狗显式依赖了，<code>fcitx-qt6</code>又不需要。</p>            </div> 然后编辑`/etc/environment`，添加以下内容： <figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-attr">GTK_IM_MODULE</span>=fcitx<br><span class="hljs-attr">QT_IM_MODULE</span>=fcitx<br><span class="hljs-attr">XMODIFIERS</span>=@im=fcitx<br></code></pre></td></tr></table></figure>        </div>      </div>    </div><h3 id="2-拉取AUR仓库"><a href="#2-拉取AUR仓库" class="headerlink" title="2. 拉取AUR仓库"></a>2. 拉取AUR仓库</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> https://aur.archlinux.org/fcitx-sogoupinyin.git --depth 1<br></code></pre></td></tr></table></figure><h3 id="3-下载官方输入法安装包"><a href="#3-下载官方输入法安装包" class="headerlink" title="3. 下载官方输入法安装包"></a>3. 下载官方输入法安装包</h3><p>前往 <a href="https://shurufa.sogou.com/linux">搜狗输入法Linux官网</a> 点击“立即下载”，选择“x86_64”；</p><div class="note note-warning">            <p>AUR的打包者并没有维护其它架构的包</p>           </div><p>然后把下载好的.deb包移动到AUR仓库的目录中：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mv</span> /path/to/***.deb /path/to/fcitx-sogoupinyin/ <span class="hljs-comment">#自行修改路径</span><br></code></pre></td></tr></table></figure><div class="note note-info">            <p>搜狗官网包下载的方式是点击下载按钮时才生成一个动态链接，下载后就会失效；所以AUR的打包脚本无法通过直链来下载需要的安装包，需要我们手动解决；这也正是不推荐使用AUR助手安装的原因。</p>           </div><h3 id="4-启动打包安装"><a href="#4-启动打包安装" class="headerlink" title="4. 启动打包安装"></a>4. 启动打包安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> fcitx-sogoupinyin<br>makepkg -si<br></code></pre></td></tr></table></figure><p>稍等片刻即可安装完成。</p><h3 id="5-完成安装"><a href="#5-完成安装" class="headerlink" title="5. 完成安装"></a>5. 完成安装</h3><p>注销用户或者重启系统之后，切换到fcitx框架，就能找到搜狗输入法了；<br>效果图与deb系相同，再贴一遍：<br>托盘菜单：<br><img src="/img/sogou-linux/fcitx-menu.png"><br>输入效果：<br><img src="/img/sogou-linux/fcitx-input.png"></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>一些配置文件存档</title>
    <link href="/2025/01/15/some-rc-files/"/>
    <url>/2025/01/15/some-rc-files/</url>
    
    <content type="html"><![CDATA[<h1 id="通用"><a href="#通用" class="headerlink" title="通用"></a>通用</h1><h2 id="ssh-config"><a href="#ssh-config" class="headerlink" title="ssh config"></a>ssh config</h2><p><code>~/.ssh/config</code></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs config">Host github.com<br>User git<br>Hostname ssh.github.com<br>PreferredAuthentications publickey<br>IdentityFile ~/.ssh/id_rsa<br></code></pre></td></tr></table></figure><p>当ssh难连接<a href="mailto:&#x67;&#x69;&#x74;&#64;&#103;&#x69;&#x74;&#104;&#117;&#x62;&#46;&#99;&#x6f;&#109;">git@github.com</a>时，重定向到ssh.github.com<br>可以在此之前先分别测试一下两个链接的连通性：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">ssh</span> -T git<span class="hljs-variable">@github</span>.com<br>ssh -T git<span class="hljs-variable">@ssh</span>.github.com<br></code></pre></td></tr></table></figure><h1 id="Windows"><a href="#Windows" class="headerlink" title="Windows"></a>Windows</h1><h2 id="inputrc"><a href="#inputrc" class="headerlink" title=".inputrc"></a>.inputrc</h2><p><code>~/.inputrc</code></p><figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gams"><span class="hljs-keyword">set</span> bell-style <span class="hljs-comment">none</span><br></code></pre></td></tr></table></figure><p>这份文件用来解决使用Windows Terminal打开git bash的时候，无效按键会闪屏的问题。</p><h2 id="vimrc"><a href="#vimrc" class="headerlink" title=".vimrc"></a>.vimrc</h2><p><code>~/.vimrc</code></p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs routeros"><span class="hljs-built_in">set</span> relativenumber<br><span class="hljs-built_in">set</span> number<br><span class="hljs-built_in">set</span> vb t_vb=<br><span class="hljs-built_in">set</span> <span class="hljs-attribute">encoding</span>=utf-8<br><span class="hljs-built_in">set</span> <span class="hljs-attribute">fileencodings</span>=utf8,cp936,gb18030,big5<br>unlet! skip_defaults_vim<br>source <span class="hljs-variable">$VIMRUNTIME</span>/defaults.vim<br></code></pre></td></tr></table></figure><p>这份文件作为git bash里面附带的vim的配置文件；<br>前两行设置，当前行显示绝对行号，其它行显示相对行号；<br>第三行解决Windows Terminal打开vim的时候，无效按键会闪屏的问题；<br>四五两行修改编码方式以确保正确显示中文（主要是确保用cmd强行调用git自带vim的邪门用法能够正常显示）；<br>最后两行启用vim的默认配置(当vim检测到.vimrc文件存在时，会禁用默认配置，这里将其恢复)。</p><h1 id="Linux"><a href="#Linux" class="headerlink" title="Linux"></a>Linux</h1><h2 id="tty-en-sh"><a href="#tty-en-sh" class="headerlink" title="tty-en.sh"></a>tty-en.sh</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># Set tty English</span><br><br><span class="hljs-keyword">for</span> ttyi <span class="hljs-keyword">in</span> &#123;1..6&#125;; <span class="hljs-keyword">do</span><br>    <span class="hljs-keyword">if</span> [ <span class="hljs-string">&quot;<span class="hljs-subst">$(tty)</span>&quot;</span> = <span class="hljs-string">&quot;/dev/tty<span class="hljs-variable">$ttyi</span>&quot;</span> ];<br>    <span class="hljs-keyword">then</span><br>        <span class="hljs-built_in">export</span> LC_ALL=<span class="hljs-string">&quot;en_US.UTF-8&quot;</span><br>        <span class="hljs-built_in">export</span> LANGUAGE=<span class="hljs-string">&quot;en_US.UTF-8&quot;</span><br>        <span class="hljs-built_in">export</span> LANG=<span class="hljs-string">&quot;en_US.UTF-8&quot;</span><br>    <span class="hljs-keyword">fi</span><br><span class="hljs-keyword">done</span><br></code></pre></td></tr></table></figure><p>对于桌面环境跟随系统<code>/etc/locale.conf</code>的Linux系统，当桌面崩溃需要tty救急的时候，tty上面的中文全部显示为菱形会导致几乎无法查看输出。<br>这段代码检测如果处于tty环境下就将系统全局语音设置为英文(Linux默认开6个tty所以这里设置成从tty1到tty6)；<br>如果需要只对单用户生效，将这段代码追加到<code>~/.bashrc</code>中(或者根据你的默认终端换成<code>~/.zshrc</code>等)，或者追加到<code>~/.profile</code>中(未测试)；<br>如果需要全局生效，将这段代码追加到<code>/etc/profile</code>的末尾，或者在<code>/etc/profile.d/</code>目录下创建一个新的文件，取一个你喜欢的名字(例如咱的路径是<code>/etc/profile.d/tty-en.sh</code>)，填入这段代码。<br>全局配置更推荐后者方案，因为前者直接修改系统预设，如果系统更新覆盖了这个文件，你可能需要重新处理(<code>pacman</code>系用户会需要处理<code>/etc/profile.pacnew</code>)，而后者是自定义文件因此不会出现此问题。</p><h2 id="fish-prompt-fish-git"><a href="#fish-prompt-fish-git" class="headerlink" title="fish_prompt.fish(git)"></a>fish_prompt.fish(git)</h2><p><code>~/.config/fish/functions/fish_prompt.fish</code></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><code class="hljs fish">function fish_prompt<br>  set -l last_status $status<br><br>  if not set -q __fish_git_prompt_show_informative_status<br>    set -g __fish_git_prompt_show_informative_status 1<br>  end<br>  if not set -q __fish_git_prompt_color_branch<br>    set -g __fish_git_prompt_color_branch brmagenta<br>  end<br>  if not set -q __fish_git_prompt_showupstream<br>    set -g __fish_git_prompt_showupstream &quot;informative&quot;<br>  end<br>  if not set -q __fish_git_prompt_showdirtystate<br>    set -g __fish_git_prompt_showdirtystate &quot;yes&quot;<br>  end<br>  if not set -q __fish_git_prompt_color_stagedstate<br>    set -g __fish_git_prompt_color_stagedstate yellow<br>  end<br>  if not set -q __fish_git_prompt_color_invalidstate<br>    set -g __fish_git_prompt_color_invalidstate red<br>  end<br>  if not set -q __fish_git_prompt_color_cleanstate<br>    set -g __fish_git_prompt_color_cleanstate brgreen<br>  end<br><br>  printf &#x27;%s%s %s%s%s%s &#x27; (set_color $fish_color_host) (prompt_hostname) (set_color $fish_color_cwd) (prompt_pwd) (set_color normal) (fish_git_prompt)<br><br>  if not test $last_status -eq 0<br>    set_color $fish_color_error<br>  end<br>  echo -n &#x27;$ &#x27;<br>  set_color normal<br>end<br></code></pre></td></tr></table></figure><p>给<code>fish</code>启用显示git状态的功能，但是个人感觉会变难看（不过<code>fish</code>默认状态也不太好看）<br>启用前(home目录)：<br><img src="/img/fish-git/before-home.png"><br>启用后(home目录)：<br><img src="/img/fish-git/after-home.png"><br>启用前(~&#x2F;.cache&#x2F;yay&#x2F;notepad—git&#x2F;):<br><img src="/img/fish-git/before-git.png"><br>启用后(~&#x2F;.cache&#x2F;yay&#x2F;notepad—git&#x2F;):<br><img src="/img/fish-git/after-git.png"></p><h1 id="特别篇：Scroll-Lock与键盘背光"><a href="#特别篇：Scroll-Lock与键盘背光" class="headerlink" title="特别篇：Scroll Lock与键盘背光"></a>特别篇：Scroll Lock与键盘背光</h1><p>有部分键盘（比如咱现在用的）会用Scroll Lock键来控制键盘的背光灯…也可以理解，毕竟这个键真没啥用。通常情况下这没啥问题，但是在两个环境下会出问题：Linux下会直接失效；Windows打开git bash(mintty)的时候会被强制关闭。对此分别有解决方案：</p><ol><li>Linux<br> 创建一个脚本，给它取一个合适的名字；然后手动把脚本绑定到Scroll Lock按键上： <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br>status=/tmp/keyboard_led_status  <br><span class="hljs-keyword">if</span> [[ <span class="hljs-variable">$&#123;1&#125;</span> == <span class="hljs-string">&quot;on&quot;</span> ]] || [[ ! -e <span class="hljs-variable">$&#123;status&#125;</span> ]]; <span class="hljs-keyword">then</span>  <br>   xset led named <span class="hljs-string">&#x27;Scroll Lock&#x27;</span> &amp;&amp; <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;on&quot;</span> &gt; <span class="hljs-variable">$&#123;status&#125;</span>  <br><span class="hljs-keyword">else</span>  <br>   xset -led named <span class="hljs-string">&#x27;Scroll Lock&#x27;</span> &amp;&amp; <span class="hljs-built_in">rm</span> -f <span class="hljs-variable">$&#123;status&#125;</span>  <br><span class="hljs-keyword">fi</span><br></code></pre></td></tr></table></figure> <img src="/img/ScrollLock.png"></li><li>Windows-mintty<br> 在用户文件夹下创建文件<code>.minttyrc</code>，填入： <figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs abnf"><span class="hljs-attribute">ManageLEDs</span><span class="hljs-operator">=</span><span class="hljs-number">3</span><br></code></pre></td></tr></table></figure></li></ol>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Windows允许普通用户创建符号链接</title>
    <link href="/2025/01/14/user-mklink/"/>
    <url>/2025/01/14/user-mklink/</url>
    
    <content type="html"><![CDATA[<div class="note note-warning">            <p>2026年6月更新：经测试，此法以普通用户创建的symlink，在Windows权限模型下存在细微差异。此差异通常不会影响日常使用，除非在一些比较边缘的权限控制场景。<br>比如，咱自己的使用场景：在Windows下安装OpenSSH Server，启动sshd服务，在其它设备上使用ssh远控Windows<br>在这个场景下，只要是普通用户创建的软链接，无论是以本文方法允许的symlink，还是无需管理员的<code>/j</code>Junction均会被拒绝解析，报错是“包含不受信任的装入点(os error 448)”<br>而以管理员提权创建的软链接，以上两种链接都能完全正常工作。<br>为了咱的正常使用ssh，不再使用本文组策略。</p>           </div><p>默认情况下，Windows允许普通用户创建硬链接(<code>mklink /h</code>)和引用链接(指使用<code>mklink /j</code>创建的链接，和符号链接相似但是似乎bug更多一些)，而符号链接需要管理员权限。<br><br>虽然Win11 24H2已经提供了sudo命令可以很方便地提权(其实旧版本系统也可以通过安装 <a href="https://github.com/gerardog/gsudo">gsudo</a> 来实现临时提权)，但是每次提权都需要鼠标点击UAC弹窗比较麻烦，并且有时我们会希望一个不以管理员权限启动的程序也能有创建符号链接的权限(例如huggingface使用符号链接管理缓存)。<br><br>所以考虑允许普通用户创建符号链接。由于此方法依赖组策略编辑器，因此不适用于Windows家庭版&#x2F;教育版。</p><div class="note note-warning">            <p><strong>warning</strong></p> <p>你可以在网络上搜索到很多给Windows家庭版启用组策略编辑器的方法，但是据部分反馈，该方法只是获得了一个表面上可以使用的“组策略编辑器”，在其中进行的更改没有实际效果。因此本文不介绍这种方式，使用相关的方法之前建议先做好系统备份工作，防止损坏系统。<br><br>建议使用专业版以上的系统来进行操作，家庭版还是考虑<code>sudo</code>提权的方案吧。</p>           </div><ol><li><code>Win+R</code>打开“运行”，然后输入<code>gpedit.msc</code>：<br> <img src="/img/mklink/gpedit.png"></li><li>如图所示，按以下层级找到策略：<br> <code>本地计算机策略</code>&gt;<code>计算机配置</code>&gt;<code>Windows 设置</code>&gt;<code>安全设置</code>&gt;<code>本地策略</code>&gt;<code>用户权限分配</code>&gt;<code>创建符号链接</code><br> <img src="/img/mklink/gp.png"></li><li>双击策略进入编辑窗口，点击<code>添加用户或组(U)...</code><br> <img src="/img/mklink/adduser.png"></li><li>输入你的用户名，这里咱的用户名是<code>pc</code>：<br> <img src="/img/mklink/username.png"><br> 然后你可以点击一下<code>检查名称</code>，如果没有弹出额外的弹窗，而是直接被补全成了类似图中这样(前面追加的设备名应该与上面的“查找位置”相同)，说明输入正确；否则请关闭弹出的弹窗重新输入：<br> <img src="/img/mklink/check.png"></li><li>一路确定，保存之后重启计算机；</li><li>现在打开一个cmd窗口，尝试一下建立符号链接： <div class="note note-info">            <p>Powershell建立符号链接的方法与cmd并不相同，而是通过<code>New-Item</code>函数创建；<code>mklink</code>命令在Powershell中无法被正确识别，所以请确保你打开的是cmd(命令提示符)</p>           </div> 测试目录符号链接： <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmd">mklink /d test Desktop<br></code></pre></td></tr></table></figure> 测试文件符号链接： <figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cmd"><span class="hljs-built_in">echo</span> &quot;test&quot; &gt; test1.txt<br>mklink test2.txt test1.txt<br></code></pre></td></tr></table></figure> 从输出可以看到符号链接正常建立了：<br> <img src="/img/mklink/test-cmd.png"><br> 我们进入用户文件夹查看一下属性： <div class="group-image-container"><div class="group-image-row"><div class="group-image-wrap"><img src="/img/mklink/test-dir.png"></div><div class="group-image-wrap"><img src="/img/mklink/test-file.png"></div></div></div> 可以看到这两个符号链接均指向了正确的文件。<br> 最后自行删除刚刚创建的两份符号链接以及<code>test1.txt</code>这份测试文件。</li></ol>]]></content>
    
    
    
    <tags>
      
      <tag>Windows</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
